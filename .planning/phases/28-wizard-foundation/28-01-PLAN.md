---
phase: 28-wizard-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/screens.ts
  - src/db/persist.ts
  - scripts/screens-parity-check.js
autonomous: true
requirements: [EV-05]
must_haves:
  truths:
    - "src/lib/screens.ts exports the 8 server screens runnable client-side and server-side"
    - "src/lib/screens.ts contains no db/audit imports (importable from a use-client module)"
    - "persist.ts re-imports the regexes/screens from screens.ts (single source, no drift)"
    - "/api/submit rejects identical inputs after the refactor (regression-checked)"
  artifacts:
    - path: "src/lib/screens.ts"
      provides: "Shared client/server validation screens + coarse-location blocker"
      contains: "runScreens"
    - path: "src/db/persist.ts"
      provides: "Server choke point re-importing pure screens"
      contains: "from \"@/lib/screens\""
    - path: "scripts/screens-parity-check.js"
      provides: "Standalone regression check proving screens == server order"
      contains: "runScreens"
  key_links:
    - from: "src/db/persist.ts"
      to: "src/lib/screens.ts"
      via: "import of BANNED_PATTERNS/HATE_PATTERNS/PRIVATE_DATA_PATTERNS/screen* + runScreens"
      pattern: "from ['\"]@/lib/screens['\"]"
    - from: "src/lib/screens.ts"
      to: "validateSubmission server order"
      via: "runScreens early-return cascade"
      pattern: "NO_SOURCE|WEAK_SOURCE|GROUP_TARGET|INCITEMENT|HATE_TONE|INNOCENT_PARTY|PRIVATE_TARGETING|MISMATCH"
---

<objective>
Lift the pure validation screens out of `src/db/persist.ts` into a new framework-free `src/lib/screens.ts` (EV-05), then refactor `persist.ts` to re-import them so client and server cannot drift. This is the keystone shared library that Phases 29–31 consume for inline advisory validation; the server remains the authoritative trust boundary.

Purpose: One source of truth for the eight rejection screens + the coarse-location (street-address) blocker, importable from a `"use client"` component without dragging in `db`/`audit`.
Output: `src/lib/screens.ts` (pure), refactored `src/db/persist.ts`, and a standalone Node regression check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/28-wizard-foundation/28-PATTERNS.md

<interfaces>
<!-- The result shape the screens return — copy verbatim from persist.ts:30-32 -->
From src/db/persist.ts:30-32 (keep this exact discriminated union):
```typescript
export type PersistResult<T> =
  | { ok: true; data: T }
  | { ok: false; code: string; message: string; field?: string };
```
The server's screen order (persist.ts validateSubmission, lines 120-216) that runScreens MUST reproduce by early-return:
NO_SOURCE → WEAK_SOURCE(<2) → GROUP_TARGET → INCITEMENT → HATE_TONE → INNOCENT_PARTY → PRIVATE_TARGETING → MISMATCH
Screens 3-6 run on the concatenation: entityName + " " + entityRole + " " + allegationDescription (persist.ts:149-155).
NOTE (source-count contract): runScreens' `sourceCount` MUST mirror the server's WEAK_SOURCE input, which counts `sourceLinks` ONLY — route.ts:32 passes `sourceCount: data.sourceLinks.length`; uploaded files do NOT count toward WEAK_SOURCE. The UI "≥2 surfaced rows" affordance (links+files) is a separate UX concern, NOT the screen input — document this in a comment in screens.ts so Phase 30 does not drift into "passes client, server rejects". Add a parity fixture asserting `2 files + 0 links → WEAK_SOURCE`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/api/submit` | Untrusted submission JSON crosses here; `validateSubmission` in persist.ts is the authoritative screen. |
| shared `screens.ts` consumed in browser | Client runs the SAME regexes as advisory hints only; it is NOT a security control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `screens.ts` regexes shipped to client | accept | Client screens are advisory; server `validateSubmission` re-runs identical screens (regression-checked here). A user editing client JS only loses their own hints. |
| T-28-02 | Repudiation | persist.ts behavior change during refactor | mitigate | `scripts/screens-parity-check.js` asserts each code still fires on the same fixtures and in the same order before/after the lift. |
| T-28-03 | Info disclosure | accidentally importing `db`/`audit` into a client-reachable lib | mitigate | `screens.ts` MUST contain zero `@/db`, `drizzle`, `./audit`, or `./index` imports; verified by grep in `<verify>`. |
| T-28-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; if any appears it is forbidden without a legitimacy checkpoint. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure screens into src/lib/screens.ts (verbatim lift)</name>
  <files>src/lib/screens.ts</files>
  <read_first>
    - src/db/persist.ts (the verbatim lift source: BANNED_PATTERNS :17-22, HATE_PATTERNS :24-26, PersistResult :30-32, screenText :36-67, incitement regex :64, INNOCENT_PROFESSIONS :83-85, ORG_TERMS :87-89, PRIVATE_DATA_PATTERNS :91-96, screenInnocentParty :98-100, screenPrivateTargeting :102-104, screenMismatch :106-118, validateSubmission order :120-216)
    - .planning/UI-SPEC.md §9 (client-side validation, server order, the four PRIVATE_DATA regexes, coarse-location blocker S5)
    - .planning/phases/28-wizard-foundation/28-PATTERNS.md ("src/lib/screens.ts" section, lines 255-276)
  </read_first>
  <behavior>
    - runScreens({ sourceCount: 0, ... }) returns { ok:false, code:"NO_SOURCE" }
    - runScreens({ sourceCount: 1, ... }) returns { ok:false, code:"WEAK_SOURCE" }
    - runScreens with a BANNED_PATTERNS term in the concatenation returns code:"GROUP_TARGET"
    - runScreens with an incitement verb (e.g. "اقتلوا") returns code:"INCITEMENT"
    - runScreens with a HATE_PATTERNS term returns code:"HATE_TONE"
    - runScreens with an INNOCENT_PROFESSIONS term (e.g. "طبيب") returns code:"INNOCENT_PARTY"
    - runScreens with a street address (e.g. "شارع الثورة") returns code:"PRIVATE_TARGETING"
    - runScreens with entityType="individual" + an org role token (e.g. "لواء") returns code:"MISMATCH"
    - runScreens on a clean named-actor + 2 sources returns { ok:true }
    - isCoarseLocationClean("شارع الثورة 5") returns false; isCoarseLocationClean("دمشق") returns true
  </behavior>
  <action>
    Create `src/lib/screens.ts` as a pure, framework-free module. Copy UNCHANGED from `persist.ts`: the four `BANNED_PATTERNS` regexes (:17-22), `HATE_PATTERNS` (:24-26), the standalone incitement test regex (:64), `INNOCENT_PROFESSIONS` (:83-85), `ORG_TERMS` (:87-89), `PRIVATE_DATA_PATTERNS` (:91-96), and the `PersistResult<T>` type (:30-32). Re-export `PersistResult`, all the pattern arrays, `screenText`, `screenInnocentParty`, `screenPrivateTargeting`, and `screenMismatch` with their bodies byte-identical to persist.ts. Add a new `runScreens(input)` that reproduces `validateSubmission`'s early-return cascade in the EXACT server order (NO_SOURCE→WEAK_SOURCE→GROUP_TARGET→INCITEMENT→HATE_TONE→INNOCENT_PARTY→PRIVATE_TARGETING→MISMATCH), screening screens 3-6 on the concatenation `entityName + " " + entityRole + " " + allegationDescription` and returning the SAME `{ ok:false, code, message, field }` objects persist.ts returns. Add a named helper `isCoarseLocationClean(value: string): boolean` that returns `false` when the third PRIVATE_DATA_PATTERN (the `\b(شارع|ساحة|حي|منطقة|بناية|طابق|شقة|زقاق|عمارة)\s+\w+` address regex) matches — this is the S5 coarse-location blocker for the governorate/city field. Add ZERO imports from `@/db`, `drizzle`, `./audit`, `./index`, `@/db/schema`, or any server module — only `zod` if a type is needed (prefer no runtime deps). Do NOT inline implementations beyond the verbatim lift.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/lib/screens.ts','utf8');const bad=/from ['\"](@\/db|\.\/audit|\.\/index|drizzle)/.test(s);if(bad){console.error('FORBIDDEN server import in screens.ts');process.exit(1)};['runScreens','isCoarseLocationClean','BANNED_PATTERNS','HATE_PATTERNS','PRIVATE_DATA_PATTERNS','screenText','screenMismatch','PersistResult'].forEach(n=>{if(!s.includes(n)){console.error('missing '+n);process.exit(1)}});console.log('screens.ts surface OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/screens.ts` exists and exports `runScreens`, `isCoarseLocationClean`, `PersistResult`, the four pattern arrays, and the four `screen*` functions (grep-asserted).
    - The file contains NO import from `@/db`, `drizzle`, `./audit`, `./index`, or `@/db/schema` (grep-asserted — UI-SPEC §9 "no server-only imports").
    - The regex literals are byte-identical to their persist.ts sources (manual diff against the line ranges above).
  </acceptance_criteria>
  <done>screens.ts exists, is server-import-free, and exposes runScreens (server order) + isCoarseLocationClean; the grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor persist.ts to re-import from screens.ts + add parity regression check</name>
  <files>src/db/persist.ts, scripts/screens-parity-check.js</files>
  <read_first>
    - src/db/persist.ts (delete the now-duplicated constants/functions; keep validateSubmission, validatePublication, withAudit, PersistContext)
    - src/lib/screens.ts (the new source of the pure pieces — written in Task 1)
    - src/app/api/submit/route.ts (confirms validateSubmission is called with sourceCount=data.sourceLinks.length, lines 27-41 — the regression must mirror this call shape)
  </read_first>
  <action>
    In `src/db/persist.ts`, remove the now-duplicated `BANNED_PATTERNS`, `HATE_PATTERNS`, `INNOCENT_PROFESSIONS`, `ORG_TERMS`, `PRIVATE_DATA_PATTERNS`, `PersistResult`, `screenText`, `screenInnocentParty`, `screenPrivateTargeting`, and `screenMismatch` definitions, and instead `import` them from `@/lib/screens`. Re-export `PersistResult` from screens so existing importers of `persist.ts`'s `PersistResult` keep working (`export type { PersistResult } from "@/lib/screens"`). Keep `validateSubmission` body intact but have it call the imported `screen*` helpers (its early-return order must NOT change). Keep `validatePublication`, `withAudit`, and `PersistContext` exactly as-is. Then create `scripts/screens-parity-check.js`: a standalone Node script (CommonJS, no test framework) that imports nothing from the app build — instead it requires a tiny transpile-free assertion: load fixtures (NO_SOURCE, WEAK_SOURCE, GROUP_TARGET, INCITEMENT, HATE_TONE, INNOCENT_PARTY, PRIVATE_TARGETING, MISMATCH, plus one clean pass) and assert each maps to the expected code by re-implementing the same expected-code table; mark FAIL with exit code 1 on any mismatch. Since `screens.ts` is TS, have the script shell out to `node --experimental-strip-types` if available, else assert via `npx tsc` having compiled — prefer driving the check through `npx tsc --noEmit` for type-safety AND a `node --import tsx`/`--experimental-strip-types src/lib/screens.ts` smoke run; if neither flag is supported in this Node, fall back to asserting the 8 expected codes against a JS mirror of the order constant exported from screens.ts. Keep the script self-contained and runnable via `node scripts/screens-parity-check.js`.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -q 'from "@/lib/screens"' src/db/persist.ts && ! grep -q 'const BANNED_PATTERNS' src/db/persist.ts && echo "persist.ts refactor + typecheck OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/persist.ts` imports the pure pieces `from "@/lib/screens"` and no longer declares `BANNED_PATTERNS`/`PRIVATE_DATA_PATTERNS`/`screenText` locally (grep-asserted).
    - `npx tsc --noEmit` passes with zero errors (the refactor preserves the public type surface).
    - `node scripts/screens-parity-check.js` exits 0, asserting all 8 codes fire on their fixtures in server order plus one clean pass — proving `/api/submit` rejects identically after the lift (28-PATTERNS.md regression flag).
  </acceptance_criteria>
  <done>persist.ts re-imports from screens.ts, the project typechecks, and the parity script exits 0 — server rejection behavior is provably unchanged.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 errors.
- `node scripts/screens-parity-check.js` → exit 0, all 8 codes + clean pass verified.
- `grep -RnE "from ['\"](@/db|drizzle|./audit)" src/lib/screens.ts` → no matches (screens.ts is server-import-free).
- `grep -q 'from "@/lib/screens"' src/db/persist.ts` → match (single source).
</verification>

<success_criteria>
- One pure `screens.ts` is the single source of the 8 screens + coarse-location blocker, importable from client and server.
- `persist.ts` consumes it; the project typechecks; the regression check proves `/api/submit` rejects identically.
- EV-05 foundation is ready for Phases 29–31 to import for inline advisory validation.
</success_criteria>

<output>
Create `.planning/phases/28-wizard-foundation/28-01-SUMMARY.md` when done.
</output>
