---
phase: 28-wizard-foundation
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - src/components/wizard/WizardProgress.tsx
  - src/components/wizard/WizardNav.tsx
  - src/components/wizard/WizardPanel.tsx
autonomous: true
requirements: [WIZ-03, WIZ-04]
must_haves:
  truths:
    - "Progress row shows done/current/upcoming pills with aria-current on the active step"
    - "Step counter renders Arabic-Indic digits in AR via Intl.NumberFormat(locale)"
    - "Completed pills are tappable buttons that route back; upcoming pills are inert"
    - "Back appears on every step except the first; no Next on choice archetype"
    - "WizardPanel moves focus to the step <h2 tabindex=-1> and announces via aria-live=polite"
  artifacts:
    - path: "src/components/wizard/WizardProgress.tsx"
      provides: "Progress pills + Step N of M counter (WIZ-04)"
      contains: "aria-current"
    - path: "src/components/wizard/WizardNav.tsx"
      provides: "Back (every step but first) + conditional Next (input only) (WIZ-03)"
      contains: "wizard-nav"
    - path: "src/components/wizard/WizardPanel.tsx"
      provides: "Step body shell with focus management + aria-live announcer"
      contains: "aria-live"
  key_links:
    - from: "src/components/wizard/WizardProgress.tsx"
      to: "Intl.NumberFormat(locale)"
      via: "Arabic-Indic step counter"
      pattern: "Intl.NumberFormat"
    - from: "src/components/wizard/WizardPanel.tsx"
      to: "step <h2>"
      via: "ref.focus() on step change"
      pattern: "tabIndex"
---

<objective>
Build the three leaf presentational wizard components — `WizardProgress` (pills + "Step N of M", WIZ-04), `WizardNav` (Back + conditional Next, WIZ-03), and `WizardPanel` (step-body shell with focus management + aria-live announcer). They consume the Plan 02 CSS classes and Plan 03 engine types but render no business state themselves; the root (Plan 05) wires them. Building them as a separate file set lets them ship in parallel with the archetype components plan order while owning distinct files.

Purpose: The reusable chrome every step renders inside — progress, navigation, and the accessible panel wrapper.
Output: three `src/components/wizard/*.tsx` components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/28-wizard-foundation/28-PATTERNS.md

<interfaces>
<!-- Engine types (Plan 03) and classes (Plan 02) these components consume -->
From src/lib/wizard/registry.ts: STEPS, StepId, isCountedStep/visibleStepCount, isReachable.
From src/lib/wizard/state.ts: WizardState (props), dispatch type.
CSS classes (Plan 02, src/components/hlshajara.css): .wizard-progress, .wizard-step-pill(.on/.done),
.wizard-count, .wizard-panel, .wizard-nav(.back/.next).
Reuse: Button from @/components (src/components/Button.tsx). i18n: useTranslations("submit"),
useLocale from next-intl. Router: useRouter from @/i18n/navigation (locale-aware, un-prefixed paths).
Shell i18n keys available (Plan 03): back, next, stepCounter ("Step {n} of {m}").
Analogs: RecordClient.tsx:128-146 (pagination bar = nav model), :21 (useLocale),
hlshajara.css :54 ([dir=rtl] .legal .t reset for Latin eyebrows).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered step titles → DOM | Titles come from i18n message keys (trusted, author-controlled), not from submitter input — no untrusted interpolation in these chrome components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Info disclosure / XSS | rendering draft values inside the announcer/panel | mitigate | The panel/announcer render i18n strings + step titles only (no `dangerouslySetInnerHTML`); any submitter value is rendered as React text (auto-escaped). XSS via rendered draft values is structurally prevented by never using innerHTML. |
| T-28-09 | DoS/UX | focus management traps keyboard users | mitigate | Focus moves to the step `<h2 tabindex=-1>` on change and never traps; verified against UI-SPEC §11 "never trap". |
| T-28-SC | Tampering | npm installs | mitigate | No packages installed; existing React/next-intl only. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WizardProgress.tsx — pills + Arabic-Indic Step N of M</name>
  <files>src/components/wizard/WizardProgress.tsx</files>
  <read_first>
    - .planning/UI-SPEC.md §7 (.wizard-progress/.wizard-step-pill/.wizard-count specs, aria-current on active pill, Intl.NumberFormat counter), §11 (progress conveyed by aria-current + text count, not color alone)
    - src/app/[locale]/record/RecordClient.tsx:21 (useLocale) + .chip usage (pill visual model)
    - src/components/hlshajara.css:54 ([dir=rtl] .legal .t reset — Latin label idiom to keep pills RTL-correct)
    - src/lib/wizard/registry.ts (STEPS, StepId, isCountedStep/visibleStepCount — Plan 03) for deriving M and which pills count
    - .planning/phases/28-wizard-foundation/28-PATTERNS.md (WizardProgress section :182-192)
  </read_first>
  <behavior>
    - Given current step index 0 of 2 visible steps, locale "en": counter text contains "1" and "2" (Latin)
    - Same in locale "ar": counter text contains Arabic-Indic digits (١/٢) via Intl.NumberFormat("ar")
    - The active pill has aria-current="step"; completed pills are <button>; upcoming pills are inert (no onClick / disabled)
    - Clicking a completed pill calls the provided onJump(stepId) (router push wired by parent)
  </behavior>
  <action>
    Create `src/components/wizard/WizardProgress.tsx` (`"use client"`). Props: the wizard state (or `currentStep: StepId` + visited list) and an `onJump(id: StepId)` callback. Use `useTranslations("submit")` and `useLocale()`. Render `.wizard-progress` containing a `.wizard-step-pill` per VISIBLE step (derive the visible list + M via the registry's `visibleStepCount`/`isCountedStep` so the Individual-branch skip is honored). Mark the current pill `.on` + `aria-current="step"`; completed pills get `.done` and are real `<button>`s that call `onJump(id)`; upcoming pills are inert (non-button, no handler). Render `.wizard-count` with the counter from `t("stepCounter", { n, m })` where `n` and `m` are produced by `new Intl.NumberFormat(locale).format(...)` so AR shows Arabic-Indic digits (UI-SPEC §7). Keep pill labels RTL-correct — no `text-transform:uppercase`/`letter-spacing` (handled by the CSS reset). Conveys progress via aria-current + text, never color alone (UI-SPEC §11). No inline color styles — classes only.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -q 'aria-current' src/components/wizard/WizardProgress.tsx && grep -q 'Intl.NumberFormat' src/components/wizard/WizardProgress.tsx && grep -q 'wizard-step-pill' src/components/wizard/WizardProgress.tsx && echo "WizardProgress OK"</automated>
  </verify>
  <acceptance_criteria>
    - The active pill renders `aria-current="step"`; completed pills are `<button>` calling `onJump`; upcoming pills are inert (grep + manual read — WIZ-04, UI-SPEC §7).
    - The step counter uses `Intl.NumberFormat(locale)` so AR shows Arabic-Indic digits (grep-asserted — UI-SPEC §7, INTL-02 foundation).
    - Visible step count M is derived from the registry (honors the Individual-branch skip), not hardcoded.
  </acceptance_criteria>
  <done>Progress pills + counter render with aria-current and locale-formatted digits; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: WizardNav.tsx — Back (every step but first) + conditional Next</name>
  <files>src/components/wizard/WizardNav.tsx</files>
  <read_first>
    - .planning/UI-SPEC.md §2.2 (choice = no Next; input = explicit Next gated on validation), §2.4 (persistent Back every step but first, inline-start, mirrors in RTL), §7 (.wizard-nav/.back/.next)
    - src/app/[locale]/record/RecordClient.tsx:128-146 (pagination bar: .btn secondary prev/next, disabled guards, .flex-between — the nav model)
    - src/components/Button.tsx (token-driven variant wrapper to use for the primary Next)
    - .planning/phases/28-wizard-foundation/28-PATTERNS.md (WizardNav section :196-212)
  </read_first>
  <action>
    Create `src/components/wizard/WizardNav.tsx` (`"use client"`). Props: `isFirst: boolean`, `archetype: "choice" | "input"`, `stepValid: boolean`, `onBack()`, `onNext()`. Render `.wizard-nav` with `.flex-between`. When `!isFirst`, render a `.btn ghost` Back button (class `back`) with a leading chevron expressed via a LOGICAL property so it mirrors in RTL (UI-SPEC §2.4, §7); label `t("back")`. Render the Next button ONLY when `archetype === "input"` — use `Button` from `@/components` (variant primary, class `next`), `disabled={!stepValid}`, label `t("next")`, `onClick={onNext}`. Choice archetype renders NO Next (auto-advance handles it — UI-SPEC §2.2, WIZ-02). No inline color; classes + Button only.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -q 'wizard-nav' src/components/wizard/WizardNav.tsx && grep -qE 'isFirst|!isFirst' src/components/wizard/WizardNav.tsx && grep -q 'archetype' src/components/wizard/WizardNav.tsx && echo "WizardNav OK"</automated>
  </verify>
  <acceptance_criteria>
    - Back renders only when `!isFirst` (grep + manual read — WIZ-03, UI-SPEC §2.4).
    - Next renders ONLY for `archetype === "input"` and is `disabled` until `stepValid` (manual read — UI-SPEC §2.2); choice steps have no Next.
    - The Back chevron uses a logical/RTL-mirroring property, not `left`/`right` (manual read — UI-SPEC §7).
  </acceptance_criteria>
  <done>WizardNav shows Back on all but the first step and Next only on input steps (gated on validity); typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: WizardPanel.tsx — step body + focus management + aria-live announcer</name>
  <files>src/components/wizard/WizardPanel.tsx</files>
  <read_first>
    - .planning/UI-SPEC.md §7 (.wizard-panel = .card + .card-pad-md; focus the step <h2 tabindex=-1> on change; aria-live="polite" "Step N of M, <title>"), §11 (focus moves to step heading, never trap; aria-live announcement)
    - src/components/PageShell.tsx:13-23 (children:ReactNode wrapper-with-conditional-class idiom to mirror)
    - .planning/phases/28-wizard-foundation/28-PATTERNS.md (WizardPanel section :216-220 — single persistent aria-live node, focus-to-heading)
  </read_first>
  <action>
    Create `src/components/wizard/WizardPanel.tsx` (`"use client"`). Props: `title: string`, `stepIndex`/`stepTotal` (for the announcement), `children: ReactNode`. Render `.wizard-panel` (= `.card` + `.card-pad-md` — both exist) containing an `<h2>` with `tabIndex={-1}` and a `ref`; in a `useEffect` keyed on the title/step, call `ref.current?.focus()` so focus moves to the heading on every step change (never trap — UI-SPEC §11). Render a SINGLE persistent visually-hidden `aria-live="polite"` region whose text content updates to "Step N of M, <title>" on change (one node, updated — not remounted — per 28-PATTERNS.md). Render `children` below the heading. Render only React text/children — NO `dangerouslySetInnerHTML` (XSS guard, T-28-08). Use the existing visually-hidden helper class if present, else an inline clip style is acceptable for the sr-only node only.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -q 'aria-live' src/components/wizard/WizardPanel.tsx && grep -q 'tabIndex' src/components/wizard/WizardPanel.tsx && grep -q 'wizard-panel' src/components/wizard/WizardPanel.tsx && ! grep -q 'dangerouslySetInnerHTML' src/components/wizard/WizardPanel.tsx && echo "WizardPanel OK"</automated>
  </verify>
  <acceptance_criteria>
    - The step `<h2>` has `tabIndex={-1}` and receives focus via a `useEffect` on step change (grep + manual read — UI-SPEC §11, INTL-03 foundation).
    - A single persistent `aria-live="polite"` region announces "Step N of M, <title>" (grep-asserted — UI-SPEC §7).
    - No `dangerouslySetInnerHTML` anywhere in the file (grep gate — XSS guard T-28-08).
  </acceptance_criteria>
  <done>WizardPanel wraps the step body, focuses the heading on change, and announces via a single aria-live region; XSS guard gate passes.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 errors.
- `WizardProgress` has `aria-current` + `Intl.NumberFormat`; `WizardNav` gates Back on `!isFirst` and Next on `archetype==="input"`; `WizardPanel` has `aria-live` + `tabIndex` and no `dangerouslySetInnerHTML`.
</verification>

<success_criteria>
- The three chrome components render the progress (WIZ-04), navigation (WIZ-03), and accessible panel, consuming Plan 02 classes + Plan 03 types.
- Accessibility foundations (aria-current, focus-to-heading, aria-live) are in place for Phase 32's full audit.
</success_criteria>

<output>
Create `.planning/phases/28-wizard-foundation/28-04-SUMMARY.md` when done.
</output>
