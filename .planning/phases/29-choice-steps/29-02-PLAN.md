---
phase: 29-choice-steps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/ar.json
  - scripts/i18n-submit-parity-check.js
autonomous: true
requirements: [STEP-01, STEP-03]

must_haves:
  truths:
    - "Every Phase-29 label and definition exists in EN with the exact UI-SPEC §3 key names."
    - "Every EN submit key has a 1:1 AR counterpart (full parity, never English-only)."
    - "Conduct and role definitions are terse, neutral legal-register one-liners in both languages."
    - "There is no i18n key naming or implying a sect, loyalty, opinion, or profession/sector card."
  artifacts:
    - path: "messages/en.json"
      provides: "EN Phase-29 keys under submit.* (q_actorClass, actor*, q_entitySubtype, q_conduct + 14×conduct_* + _def, q_roleInAct + 7×role_*)"
      contains: "q_conduct"
    - path: "messages/ar.json"
      provides: "AR mirror of every EN Phase-29 key with matching key set"
      contains: "q_conduct"
    - path: "scripts/i18n-submit-parity-check.js"
      provides: "Standalone EN↔AR submit-namespace key-parity check (no test framework)"
  key_links:
    - from: "messages/en.json submit.conduct_<slug>"
      to: "src/lib/wizard/encoding.ts CONDUCT_SLUGS"
      via: "key suffix equals slug (conduct_detention ↔ slug detention)"
      pattern: "conduct_detention"
    - from: "messages/en.json submit.role_<slug>"
      to: "src/lib/wizard/encoding.ts ROLE_SLUGS"
      via: "key suffix equals slug (role_perpetrator ↔ slug perpetrator)"
      pattern: "role_perpetrator"
---

<objective>
Author the full EN + AR string set for the four Phase-29 choice steps under the `submit`
namespace — every key name pinned VERBATIM by UI-SPEC §3 — and add a standalone EN↔AR
key-parity check (this repo has no `check:i18n` npm script; parity is gated by a Node parity
script following the `screens-parity-check.js` idiom, then audited by the `i18n-checker` agent).

Purpose: STEP-01 + STEP-03 require bilingual card content with full key parity from the start
(CLAUDE.md: never English-only). This plan is the content layer; it shares NO files with Plan
29-01 (pure logic) so both run in Wave 1 in parallel.
Output: ~23 new keys (× EN + AR), each conduct/role card with a one-line `_def`, plus the parity script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-choice-steps/29-CONTEXT.md
@.planning/phases/29-choice-steps/29-UI-SPEC.md

<interfaces>
<!-- Exact key names + sample translations. Extracted from UI-SPEC §3 + §10 — use VERBATIM. -->

Step 1 (UI-SPEC §3): submit.q_actorClass, submit.actorIndividual, submit.actorIndividualHint,
  submit.actorEntity, submit.actorEntityHint
  (§10 sample) q_actorClass EN "Who is this report about?" AR "عمّن يدور هذا البلاغ؟"
  actorIndividual EN "An individual" AR "فرد"  ·  actorEntity EN "An entity" AR "جهة"

Step 1b (UI-SPEC §3): submit.q_entitySubtype  (REUSE existing submit.typeOrganization /
  typeMilitaryUnit / typeSecurityBranch / typeOfficialBody — already present EN+AR, do NOT re-add)

Step 3 (UI-SPEC §3): submit.q_conduct + for each of the 14 slugs a label key AND a "_def" key:
  conduct_detention(+_def), conduct_torture(+_def), conduct_disappearance(+_def),
  conduct_killing(+_def), conduct_sexualViolence(+_def), conduct_financing(+_def),
  conduct_arms(+_def), conduct_laundering(+_def), conduct_propaganda(+_def),
  conduct_informing(+_def), conduct_seizure(+_def), conduct_detentionSite(+_def),
  conduct_command(+_def), conduct_other(+_def)
  (§10 samples) q_conduct EN "What did they do?" AR "ما الذي ارتكبوه؟"
  conduct_informing EN "Providing information to security" AR "تزويد الأجهزة الأمنية بمعلومات"

Step 4 (UI-SPEC §3): submit.q_roleInAct + role_perpetrator, role_commander, role_financier,
  role_supplier, role_informant, role_owner, role_other

Existing submit namespace already holds 41 keys (EN+AR parity verified in Phase 28) — APPEND only.
Existing repo parity precedent: scripts/screens-parity-check.js (standalone Node, no test framework).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author EN + AR keys for the four choice steps</name>
  <read_first>
    - messages/en.json (the file being modified — existing submit.* block, 41 keys)
    - messages/ar.json (the file being modified — existing submit.* block, 41 keys, incl. type* reused by Step 1b)
    - .planning/UI-SPEC.md §3 (exact card sets + pinned key names) and §10 (sample EN/AR rows + legal-register tone)
    - .planning/phases/29-choice-steps/29-CONTEXT.md (terse neutral legal-register, Other last, EN+AR from the start)
    - ./CLAUDE.md (bilingual full parity; non-sectarian serious legal/HR tone)
  </read_first>
  <action>
    Add to BOTH `messages/en.json` and `messages/ar.json` under the existing `submit` object, using
    the key names EXACTLY as pinned in UI-SPEC §3 (no renames, no abbreviations).
    Step 1 — q_actorClass, actorIndividual, actorIndividualHint, actorEntity, actorEntityHint
    (hints are the one-line `.desc` text: individual = "a named person documented in a public source";
    entity = "an organization, unit, body, company, or outlet" per UI-SPEC §3 Step 1 card copy).
    Step 1b — q_entitySubtype ONLY (REUSE the existing type* keys; do NOT duplicate them).
    Step 3 — q_conduct, and for each of the 14 conduct slugs BOTH a label key `conduct_<slug>` and a
    definition key `conduct_<slug>_def` (slugs: detention, torture, disappearance, killing,
    sexualViolence, financing, arms, laundering, propaganda, informing, seizure, detentionSite,
    command, other — Other LAST). Labels are the UI-SPEC §3 card names ("Arbitrary detention",
    "Torture", "Enforced disappearance", "Extrajudicial killing", "Sexual violence", "Material support
    (financing)", "Arms / logistics supply", "Money laundering", "Propaganda / whitewashing",
    "Providing information to security", "Property / asset seizure", "Operating a detention or torture
    site", "Command responsibility", "Other (describe)"). Each `_def` is a terse, neutral, legal-register
    one-liner (Claude's discretion on exact wording within that register); the AR `_def` mirrors the
    same register, non-sectarian. Step 4 — q_roleInAct, role_perpetrator, role_commander, role_financier,
    role_supplier, role_informant, role_owner, role_other (labels per UI-SPEC §3: "Direct perpetrator",
    "Ordered / commanded it", "Financed it", "Supplied it", "Informed / provided information",
    "Owned / controlled the implicated entity", "Other"). AR keeps machine-neutral, RTL-natural phrasing.
    Insert the EN and AR keys in the SAME order so the diff is reviewable. Do NOT add any key naming or
    implying sect/identity, loyalty/opinion, or profession/sector (the sets are CLOSED).
  </action>
  <verify>
    <automated>node -e "const en=require('./messages/en.json').submit, ar=require('./messages/ar.json').submit; const need=['q_actorClass','actorIndividual','actorIndividualHint','actorEntity','actorEntityHint','q_entitySubtype','q_conduct','q_roleInAct']; const conduct=['detention','torture','disappearance','killing','sexualViolence','financing','arms','laundering','propaganda','informing','seizure','detentionSite','command','other']; const role=['perpetrator','commander','financier','supplier','informant','owner','other']; const keys=[...need]; conduct.forEach(s=>{keys.push('conduct_'+s,'conduct_'+s+'_def')}); role.forEach(s=>keys.push('role_'+s)); const miss=keys.filter(k=>!(k in en)||!(k in ar)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('all',keys.length,'keys present EN+AR')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify node script exits 0 and prints "all 38 keys present EN+AR" (8 singletons + 14×2 conduct + 7 role = 8+28+7 = 43; assert ≥43 present across EN AND AR).
    - `node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));JSON.parse(require('fs').readFileSync('messages/ar.json','utf8'))"` exits 0 (both files valid JSON).
    - `grep -c "conduct_.*_def" messages/en.json` ≥ 14 and `grep -c "conduct_.*_def" messages/ar.json` ≥ 14 (every conduct card has a definition in both languages).
    - The existing type* keys are NOT duplicated: `grep -c "\"typeOrganization\"" messages/en.json` equals 1.
    - GUARDRAIL S2-S4 (grep-absence): `grep -inE "\"(sect|alawi|nusayr|sunni|shia|loyalt|opinion|profession|sector|religion|ethnic)[a-zA-Z_]*\"" messages/en.json messages/ar.json` returns NO match (no identity/loyalty/profession key).
    - The new keys appear in EN and AR in the same relative order (diff-reviewable).
  </acceptance_criteria>
  <done>All Phase-29 keys (q_actorClass/actor*, q_entitySubtype, q_conduct + 14×conduct_*+_def, q_roleInAct + 7×role_*) exist in EN and AR with matching key sets; both files valid JSON; no identity/loyalty/profession key.</done>
</task>

<task type="auto">
  <name>Task 2: Standalone EN↔AR submit-namespace parity check</name>
  <read_first>
    - scripts/screens-parity-check.js (the repo standalone-check idiom to mirror — CommonJS, node:assert, exit 1 on mismatch)
    - scripts/i18n-submit-parity-check.js (the file being created — confirm absent first)
    - messages/en.json, messages/ar.json (the two namespaces being compared)
  </read_first>
  <action>
    Create `scripts/i18n-submit-parity-check.js` as a standalone CommonJS script (no test framework,
    no new dependency) modeled on `scripts/screens-parity-check.js`. It reads both message files,
    extracts the `submit` object from each, and asserts FULL key parity: the EN submit key set equals
    the AR submit key set (report any key present in one but not the other), and no value is an empty
    string in either language. Additionally assert that every Phase-29 expected key (the 8 singletons +
    14 conduct labels + 14 conduct `_def` + 7 role keys, derived from the same slug lists used in
    Plan 29-01 so the two stay aligned) is present in BOTH. Exit 1 with a clear diff listing on any
    mismatch, 0 when parity holds. Add an npm script alias `"check:i18n": "node scripts/i18n-submit-parity-check.js"`
    in package.json `scripts` (so the milestone constraint "check:i18n must pass" maps to a real command;
    follows the repo's existing standalone-check convention).
  </action>
  <verify>
    <automated>node scripts/i18n-submit-parity-check.js && npm run check:i18n</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/i18n-submit-parity-check.js` exits 0 (full EN↔AR submit parity).
    - `npm run check:i18n` exits 0 (the alias resolves to the parity script).
    - The script exits 1 when an EN key is deleted from one file (verified by the executor temporarily removing one key, running, restoring) — parity failure is actually detected, not a no-op.
    - `git diff --stat package.json` shows ONLY the added `check:i18n` script line; no dependency change.
    - After the run, run the `i18n-checker` agent over messages/{en,ar}.json submit namespace and confirm it reports clean (CONTEXT: i18n-checker audits parity).
  </acceptance_criteria>
  <done>scripts/i18n-submit-parity-check.js gates full EN↔AR submit parity and is wired as `npm run check:i18n`; the check passes and actually fails on an induced mismatch; i18n-checker clean; zero new deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message files → rendered cards | Translation strings render as card titles/defs via next-intl `t()` (auto-escaped React text; no raw HTML). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Information Disclosure | sect/loyalty/profession key reintroduction | mitigate | grep-absence acceptance criterion on both message files (S2-S4); design-review reject. |
| T-29-05 | Tampering | malformed JSON breaks the locale bundle | mitigate | JSON.parse validity assertion on both files in acceptance criteria. |
| T-29-SC | Tampering | npm installs | mitigate | NO package installs (only a script alias added); parity check uses Node built-ins only — no install task, slopcheck N/A. |
</threat_model>

<verification>
- `node scripts/i18n-submit-parity-check.js` and `npm run check:i18n` exit 0.
- Both message files are valid JSON; EN and AR submit key sets are equal.
- S2-S4 grep-absence on both message files returns no identity/loyalty/profession key.
- `i18n-checker` agent reports clean over the submit namespace.
</verification>

<success_criteria>
- All Phase-29 keys exist VERBATIM (UI-SPEC §3 names) in EN and AR with full parity.
- Conduct/role cards each carry a terse neutral legal-register one-liner in both languages.
- `check:i18n` is a real, passing command; the closed sets contain no identity/loyalty/profession key.
- Zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/29-choice-steps/29-02-SUMMARY.md` when done.
</output>
