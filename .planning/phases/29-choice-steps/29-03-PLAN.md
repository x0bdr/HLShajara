---
phase: 29-choice-steps
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - src/app/[locale]/submit/WizardClient.tsx
autonomous: true
requirements: [STEP-01, STEP-03]

must_haves:
  truths:
    - "On each choice step, tapping a card shows the selected state then auto-advances (instant under reduced-motion), no Next button."
    - "Choosing 'An entity' reveals Step 1b and resolves entityType to one of the five enum literals; 'An individual' sets entityType='individual' and skips 1b."
    - "Conduct and role are each chosen from a closed card set with one-line definitions; no sect/loyalty/opinion/profession card exists."
    - "Returning via Back re-renders a choice step with the previously chosen card selected and focused, and does NOT re-auto-advance on mount; re-confirming the same card advances."
    - "Switching actor class on Back invalidates only the orphaned entity-subtype and shows a transient inline aria-live='polite' .legal notice."
  artifacts:
    - path: "src/app/[locale]/submit/WizardClient.tsx"
      provides: "Per-step choice rendering (actor-class/entity-subtype/conduct/role-in-act) + interim encoding dispatch + branch-switch invalidation notice"
      contains: "role-in-act"
  key_links:
    - from: "src/app/[locale]/submit/WizardClient.tsx"
      to: "src/lib/wizard/encoding.ts"
      via: "import CONDUCT_SLUGS/ROLE_SLUGS/encodeRoleClause/stripRoleClause"
      pattern: "from \"@/lib/wizard/encoding\""
    - from: "src/app/[locale]/submit/WizardClient.tsx"
      to: "src/components/wizard/ChoiceStep.tsx"
      via: "options + value + onConfirm props per step"
      pattern: "<ChoiceStep"
    - from: "WizardClient onConfirm (conduct)"
      to: "allegationClassification"
      via: "SET_FIELD with the conduct slug"
      pattern: "allegationClassification"
---

<objective>
Wire the root `WizardClient` to render the four REAL choice steps from Plan 29-01's registry,
feeding `ChoiceStep` the per-step option lists (resolved EN/AR labels + defs from Plan 29-02)
and dispatching the correct interim encoding on confirm: actor-class/entity-subtype →
`entityType`, conduct → `allegationClassification` slug, role-in-act → the `entityRole` clause.
Implement the Back-navigation contract (prior card selected + focused, NO re-auto-advance on
mount, re-confirm advances) and the branch-switch-on-Back behavior (changing actor class
invalidates ONLY the orphaned subtype with a transient inline `.legal` `aria-live="polite"` notice).

Purpose: This is the user-facing wiring for STEP-01 + STEP-03 — it turns the pure logic +
strings into the interactive card flow. It depends on both Wave-1 plans (registry/encoding +
i18n keys) and owns only WizardClient.tsx, so it runs alone in Wave 2.
Output: WizardClient renders all four choice steps with correct encoding and Back/branch behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-choice-steps/29-CONTEXT.md
@.planning/phases/29-choice-steps/29-UI-SPEC.md

<interfaces>
<!-- Contracts the executor consumes. Plan 29-01/29-02 outputs + existing scaffold. Use directly. -->

From src/lib/wizard/registry.ts (Plan 29-01): STEPS now = actor-class, entity-subtype, conduct,
  role-in-act (all archetype "choice"); StepId union; nextStep/prevStep/isReachable/
  firstIncompleteStep/visibleStepCount/visibleStepIndex unchanged in signature.

From src/lib/wizard/encoding.ts (Plan 29-01):
  CONDUCT_SLUGS: readonly tuple of 14 slugs (Other last); ROLE_SLUGS: readonly tuple of 7 (Other last);
  ROLE_CLAUSE_TOKEN: " — role in act: "; encodeRoleClause(baseRole, slug); stripRoleClause(value).

From src/lib/wizard/state.ts (Plan 29-01): reducer with SET_FIELD, COMPLETE_STEP, GOTO_STEP, and
  INVALIDATE_SUBTYPE (carries the new actor-class value; preserves allegationClassification + entityRole).
  initialWizardState.currentStep is now "actor-class".

From src/components/wizard/ChoiceStep.tsx (UNCHANGED — reuse as-is):
  props { ariaLabel: string; options: ReadonlyArray<{value,title,desc?}>; value: string; onConfirm:(value:string)=>void }
  Already: roving tabindex, Arrow move / Enter-Space confirm, .selected + brass .check, no Next button,
  focuses the matching card on mount ONLY when `value` is non-empty (the "selected + focused on Back" behavior).

From messages (Plan 29-02): submit.q_* headings; actorIndividual(+Hint)/actorEntity(+Hint);
  type* (reused for entity-subtype); conduct_<slug>(+_def); role_<slug>.

CURRENT WizardClient.tsx scaffold wiring to REPLACE:
  - hard-coded `choiceOptions` (individual vs organization toggle) → per-step option builders.
  - single `onChoiceConfirm` that always SET_FIELDs entityType → per-step confirm dispatch.
  - `value={state.form.entityType}` passed to ChoiceStep → per-step current value.
  - the input-archetype branch + InputStep import are now UNUSED for Phase 29 (no input step here);
    keep the import only if a step still needs it, otherwise remove to satisfy lint/tsc.
  - Auto-advance timer (ADVANCE_DELAY_MS / prefersReducedMotion) — REUSE unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-step option builders + interim-encoding confirm dispatch</name>
  <read_first>
    - src/app/[locale]/submit/WizardClient.tsx (the file being modified — scaffold choiceOptions + onChoiceConfirm + render block)
    - src/lib/wizard/registry.ts (Plan 29-01 new STEPS + StepId)
    - src/lib/wizard/encoding.ts (Plan 29-01 CONDUCT_SLUGS/ROLE_SLUGS/encodeRoleClause/stripRoleClause/ROLE_CLAUSE_TOKEN)
    - src/lib/wizard/state.ts (Plan 29-01 reducer actions incl. INVALIDATE_SUBTYPE)
    - src/components/wizard/ChoiceStep.tsx (props contract — value drives selected+focus; onConfirm fires on Enter/Space/pointer)
    - messages/en.json + messages/ar.json (Plan 29-02 keys consumed via t())
  </read_first>
  <behavior>
    - actor-class step: options [{value:"individual", title:t("actorIndividual"), desc:t("actorIndividualHint")}, {value:"entity-marker", title:t("actorEntity"), desc:t("actorEntityHint")}]; confirming individual SET_FIELDs entityType="individual"; confirming the entity card sets a NON-final marker so Step 1b resolves the concrete enum (entityType stays a valid enum: confirming entity routes to entity-subtype without committing a non-enum value — set entityType to a sentinel only via Step 1b, never write a non-enum literal to the SubmitInput field).
    - entity-subtype step: options map the four type* labels to the exact enum literals organization/military_unit/security_branch/official_body; confirm SET_FIELDs entityType to that literal.
    - conduct step: options are CONDUCT_SLUGS mapped to {value:slug, title:t("conduct_"+slug), desc:t("conduct_"+slug+"_def")} in tuple order (Other last); confirm SET_FIELDs allegationClassification=slug.
    - role-in-act step: options are ROLE_SLUGS mapped to {value:slug, title:t("role_"+slug)}; confirm SET_FIELDs entityRole = encodeRoleClause(stripRoleClause(currentEntityRole), slug).
    - the value passed to ChoiceStep per step reflects the current form so the prior card renders selected on Back (conduct→allegationClassification; role→the slug parsed from entityRole via the clause; actor-class→individual or the entity-marker; entity-subtype→entityType).
    - re-confirming the SAME card still calls onConfirm → still auto-advances (tap = advance).
  </behavior>
  <action>
    Replace the hard-coded scaffold `choiceOptions` with a per-step option builder keyed on
    `state.currentStep`, and replace the single `onChoiceConfirm` with a per-step confirm handler.
    Resolve all card titles/defs through `useTranslations("submit")` using the Plan 29-02 keys
    (conduct uses `t("conduct_"+slug)` / `t("conduct_"+slug+"_def")`; role uses `t("role_"+slug)`;
    actor uses actorIndividual/Hint + actorEntity/Hint; entity-subtype reuses typeOrganization/
    typeMilitaryUnit/typeSecurityBranch/typeOfficialBody). For the actor-class entity card, route to
    Step 1b WITHOUT writing a non-enum value into `entityType` (e.g. dispatch COMPLETE_STEP for
    actor-class and advance to entity-subtype, leaving entityType to be set to a real enum by Step 1b;
    if a transient "entity chosen" marker is needed for the selected-on-Back state, track it in local
    React state, never in the SubmitInput field). For conduct, SET_FIELD allegationClassification to
    the chosen CONDUCT_SLUGS value. For role-in-act, SET_FIELD entityRole to
    `encodeRoleClause(stripRoleClause(state.form.entityRole), slug)` so re-picking replaces the clause.
    Import CONDUCT_SLUGS/ROLE_SLUGS/encodeRoleClause/stripRoleClause from "@/lib/wizard/encoding".
    Compute the per-step `value` prop so ChoiceStep pre-selects the prior card on Back (parse the role
    slug out of entityRole via the clause token for the role step). Preserve the existing auto-advance
    timer + reduced-motion gate and the COMPLETE_STEP dispatch on confirm. Remove now-unused scaffold
    references (the InputStep render branch + import) only if no Phase-29 step uses them, so tsc/lint stay clean.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/tsc --noEmit` exits 0.
    - `grep -F 'from "@/lib/wizard/encoding"' src/app/[locale]/submit/WizardClient.tsx` matches (encoding helpers imported).
    - `grep -c "allegationClassification" src/app/[locale]/submit/WizardClient.tsx` ≥ 1 and `grep -c "encodeRoleClause" src/app/[locale]/submit/WizardClient.tsx` ≥ 1 (conduct slug + role clause dispatch present).
    - `grep -E "\"organization\"|\"military_unit\"|\"security_branch\"|\"official_body\"" src/app/[locale]/submit/WizardClient.tsx | wc -l` ≥ 4 (entity-subtype maps all four enum literals).
    - No non-enum literal is written to entityType: `grep -nE 'field: *"entityType", *value: *"entity-marker"' src/app/[locale]/submit/WizardClient.tsx` returns NO match (the entity sentinel never enters SubmitInput).
    - GUARDRAIL S2-S4 (grep-absence): `grep -inE "sect|alawi|nusayr|sunni|shia|loyalt|opinion|profession|sector|religion|ethnic|map.?pin|car.?plate|home.?address" src/app/[locale]/submit/WizardClient.tsx` returns NO match (no forbidden card/field token).
  </acceptance_criteria>
  <done>WizardClient renders all four real choice steps with correct per-step options and interim-encoding dispatch (slug→allegationClassification, clause→entityRole, enum→entityType); no non-enum value reaches entityType; tsc clean; no forbidden token.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Back-navigation contract + branch-switch invalidation notice</name>
  <read_first>
    - src/app/[locale]/submit/WizardClient.tsx (the file being modified — onChoiceConfirm/advance/goBack, the ?step= sync effect, the render block)
    - src/lib/wizard/state.ts (Plan 29-01 INVALIDATE_SUBTYPE action contract)
    - src/components/wizard/ChoiceStep.tsx (mount-focus behavior fires only when value is non-empty; it does NOT auto-advance on mount)
    - .planning/phases/29-choice-steps/29-CONTEXT.md (Branch-switch & Back-navigation block — Success Criterion 4)
    - .planning/UI-SPEC.md §11 (aria-live, focus-on-heading) and §3 Step 1b
  </read_first>
  <behavior>
    - On Back to a choice step the prior card renders selected + focused (ChoiceStep already focuses the matching card when value is non-empty); NO auto-advance fires on mount (only an explicit Enter/Space/pointer confirm advances).
    - Re-confirming the SAME card on a Back-visited choice step auto-advances forward.
    - Changing actor class on Back (individual↔entity) dispatches INVALIDATE_SUBTYPE so ONLY the orphaned entity-subtype is cleared; conduct (allegationClassification) and role (entityRole clause) are preserved byte-identical.
    - The invalidation surfaces a TRANSIENT inline notice rendered as a `.legal` block with `aria-live="polite"` at the top of the affected step (NOT a modal, NOT a toast); it clears on the next navigation/confirm.
    - Leaving the entity branch for individual does not strand a non-enum entityType (entityType resolves to "individual"); switching back into entity re-requires the Step 1b pick.
  </behavior>
  <action>
    Ensure the auto-advance timer is NOT armed on step mount/URL-sync — only inside the per-step
    confirm handler (so Back-return never re-advances; the existing effect that syncs `?step=` must
    not call `advance`). In the confirm handler for the actor-class step, detect when the new actor
    class DIFFERS from the prior committed branch and the user is editing via Back (the entity-subtype
    answer is now orphaned): dispatch `INVALIDATE_SUBTYPE` (carrying the new actor-class value) and set
    a transient React state flag that renders a one-line `.legal` notice with `aria-live="polite"`
    (copy via a `submit` key — reuse an existing notice key or add one; if a new key is needed it must
    land in BOTH messages files and pass Plan 29-02's parity check). Clear that flag on the next
    `goTo`/confirm so the notice is transient. Confirm (manually, against the §13/Success-Criterion-4
    checklist) that re-confirming the same card advances and that returning to conduct/role after an
    actor-class switch still shows the previously chosen card selected (conduct/role preserved). Do not
    alter ChoiceStep.tsx — its mount-focus-only-when-value behavior already delivers selected+focused
    without re-advance.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit -p tsconfig.json && node scripts/i18n-submit-parity-check.js</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/tsc --noEmit` exits 0.
    - `node scripts/i18n-submit-parity-check.js` exits 0 (any new notice key keeps EN↔AR parity).
    - `grep -F 'aria-live="polite"' src/app/[locale]/submit/WizardClient.tsx` matches (transient invalidation notice present).
    - `grep -c "INVALIDATE_SUBTYPE" src/app/[locale]/submit/WizardClient.tsx` ≥ 1 (orphan-only invalidation dispatched on actor-class switch).
    - The auto-advance `setTimeout(advance, ...)` / `advance()` calls appear ONLY inside the confirm handler, never inside the `?step=` sync effect: `grep -n "advance" src/app/[locale]/submit/WizardClient.tsx` shows no `advance` call within the searchParams effect body (no re-advance on mount).
    - <human-check>On `npm run dev` at /en/submit and /ar/submit: tap An entity → pick a subtype → conduct → role; press Back to actor-class, switch to An individual — the entity-subtype is cleared, an inline aria-live notice appears, and conduct + role remain chosen. Back to conduct shows the prior card selected+focused with no auto-advance; re-confirming it advances. Confirms Success Criterion 4 + the no-re-advance-on-mount rule.</human-check>
  </acceptance_criteria>
  <done>Back to a choice step pre-selects+focuses the prior card with no re-advance; re-confirm advances; an actor-class switch invalidates only the orphaned subtype and shows a transient aria-live .legal notice while preserving conduct/role; tsc + parity clean.</done>
</task>

<task type="auto">
  <name>Task 3: Conduct "Other" required-flag wiring + full build gate</name>
  <read_first>
    - src/app/[locale]/submit/WizardClient.tsx (the file being modified — conduct confirm handler + render)
    - src/lib/wizard/encoding.ts (the conduct "other" slug semantics documented in Plan 29-01)
    - .planning/phases/29-choice-steps/29-CONTEXT.md (conduct "Other" → allegationClassification="other" + flag Step-5 description as required-to-name-the-act; triageCategory NOT set)
    - .planning/UI-SPEC.md §3 Step 3 (Other routes to Step 5 with a required free-text conduct summary)
  </read_first>
  <action>
    When the conduct "other" slug is confirmed, set allegationClassification="other" (already from
    Task 1) AND record a transient flag (local React state or a derived `state.form.allegationClassification === "other"`
    check — prefer the derived check so it survives Back/refresh) that Phase-30's Step 5 will read to
    require the description to NAME the act. Since Step 5 is Phase 30, this plan only ensures the
    "other" selection is durably encoded and DOCUMENTS (in a code comment at the conduct handler) that
    Step-5 reads `allegationClassification === "other"` to mark the description required-to-name-the-act,
    and that `triageCategory` is NOT populated here (Phase 33). Do NOT modify validation.ts. Then run
    the full Next.js build to confirm the four real steps compile and the /en|ar/submit routes still
    build (SSG). Verify the S2-S4 closed-set absence one final time across the wizard surface.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes successfully (exit 0); /en/submit and /ar/submit build without error.
    - `grep -F '"other"' src/app/[locale]/submit/WizardClient.tsx` matches (the conduct Other slug is handled) AND a comment references the Step-5 required-to-name-the-act consumption + triageCategory deferral.
    - `git diff --stat src/lib/validation.ts` shows NO change (schema untouched).
    - GUARDRAIL S2-S4 (grep-absence, whole wizard surface): `grep -rinE "sect|alawi|nusayr|sunni|shia|loyalt|opinion|profession|sector|religion|ethnic" src/components/wizard/ src/app/[locale]/submit/WizardClient.tsx src/lib/wizard/encoding.ts | grep -v '^[^:]*:[0-9]*: *//' ` returns NO substantive match (only categorically-phrased comments, if any, survive).
    - `git diff --stat package.json` shows no dependency change (only the 29-02 check:i18n alias, already present).
  </acceptance_criteria>
  <done>Conduct "Other" durably encodes allegationClassification="other" with a documented Step-5/Phase-33 hand-off; the full `next build` passes for both locales; validation.ts untouched; S2-S4 absent across the wizard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card confirm → reducer → SubmitInput | Card picks write entityType/allegationClassification/entityRole that flow to the existing /api/submit choke point. |
| sessionStorage draft → restored state | A Back-navigated/restored draft drives the selected-card + role-clause parse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Tampering | entityType written from actor-class card | mitigate | the entity card never writes a non-enum literal to entityType; only Step 1b commits one of the five enums; server Zod enum re-validates. |
| T-29-07 | Tampering | role clause stacking on repeated Back-confirm | mitigate | encodeRoleClause strips any prior clause first (Plan 29-01), so entityRole cannot grow past max500 from re-picks; server re-validates length. |
| T-29-08 | Information Disclosure | identity/loyalty/profession card reintroduction | mitigate | grep-absence acceptance criterion across the whole wizard surface (S2-S4); design-review reject. |
| T-29-SC | Tampering | npm installs | mitigate | NO package installs in this plan; verification is tsc + parity script + next build using existing tooling — no install task, slopcheck N/A. |
</threat_model>

<verification>
- `npm run build` passes; /en|ar/submit build (SSG).
- `node_modules/.bin/tsc --noEmit` and `node scripts/i18n-submit-parity-check.js` exit 0.
- Human-check confirms Success Criterion 4 (Back-select+focus, no re-advance, re-confirm advances, branch-switch orphan invalidation + aria-live notice).
- S2-S4 grep-absence across the wizard surface; validation.ts unchanged; zero new deps.
</verification>

<success_criteria>
- All four choice steps render and auto-advance with no Next button; entity branch resolves the five enum literals and individual skips Step 1b.
- Conduct + role use the closed card sets with one-line defs; Other encodes durably and hands off to Step 5/Phase 33.
- Back pre-selects+focuses the prior card without re-advancing; re-confirm advances; an actor-class switch invalidates only the orphaned subtype with a transient aria-live notice.
- No identity/loyalty/profession card or field anywhere; validation.ts untouched; full build passes.
</success_criteria>

<output>
Create `.planning/phases/29-choice-steps/29-03-SUMMARY.md` when done.
</output>
