---
phase: 30-input-evidence-steps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizard/step-logic.ts
  - src/lib/wizard/registry.ts
  - src/lib/validation.ts
  - messages/en.json
  - messages/ar.json
  - scripts/step-logic-check.js
autonomous: true
requirements: [STEP-02, STEP-04, EV-01, EV-04]
must_haves:
  truths:
    - "Pure helpers compose coarse location, prefix source-type tokens idempotently, and run the per-step screen cascades in server order — all loadable under Node --experimental-strip-types"
    - "Registry exposes the five Phase-30 input steps (identity, describe, evidence, media, about-you) with requires predicates that gate Next exactly per UI-SPEC §3"
    - "Every Phase-30 i18n key from UI-SPEC §3 exists in both en.json and ar.json with full parity"
  artifacts:
    - path: "src/lib/wizard/step-logic.ts"
      provides: "composeLocation, SOURCE_TYPE_SLUGS, prefixSourceType, screenIdentityStep, screenDescribeStep, evidenceSourceCount, screenMediaLink, and the five step requires predicates"
      min_lines: 80
    - path: "src/lib/wizard/registry.ts"
      provides: "Five Phase-30 input StepDefs appended to STEPS with requires predicates"
      contains: "identity"
    - path: "scripts/step-logic-check.js"
      provides: "No-test-framework regression driving step-logic.ts via --experimental-strip-types"
  key_links:
    - from: "src/lib/wizard/registry.ts"
      to: "src/lib/wizard/step-logic.ts"
      via: "import of requires predicates"
      pattern: "from \"./step-logic\""
    - from: "scripts/step-logic-check.js"
      to: "src/lib/wizard/step-logic.ts"
      via: "spawn node --experimental-strip-types"
      pattern: "experimental-strip-types"
---

<objective>
Build the pure, framework-free logic layer that every Phase-30 input step consumes: coarse-location composition, the idempotent source-type title token, the per-step inline screen runners (in server order), the source-count helper for the ≥2 gate, and the five step `requires` predicates. Append the five Phase-30 input steps to the registry. Add all Phase-30 i18n keys in EN+AR parity. Add the locked `leadNote` field to the SubmitInput contract. This is the contract layer — Plans 02/03/04 build components against these functions, Plan 05 wires them into WizardClient.

Purpose: Pure TS (no JSX) is the only layer unit-testable under `--experimental-strip-types` (Phase 28 precedent). Centralizing composition/screen/gate logic here makes components thin and keeps the safety-critical logic regression-gated.
Output: `src/lib/wizard/step-logic.ts`, updated `registry.ts` + `validation.ts`, EN+AR keys, `scripts/step-logic-check.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-input-evidence-steps/30-CONTEXT.md
@.planning/UI-SPEC.md

<interfaces>
<!-- The contracts the executor builds against. Extracted from the codebase — no exploration needed. -->

From src/lib/validation.ts (the SubmitInput contract — DO NOT redeclare; only ADD leadNote):
```typescript
export type SubmitInput = {
  entityName: string; entityType: "individual"|"organization"|"military_unit"|"security_branch"|"official_body";
  entityRole: string; allegationDescription: string; allegationPeriod?: string;
  allegationLocation?: string; allegationClassification?: string;
  sourceLinks: { url: string; title?: string }[];
  sourceFiles: { hash:string; filename:string; originalName:string; url:string; size:number }[];
  submitterEmail?: string; submitterName?: string; isAnonymous: boolean;
};
```
leadNote prerequisite (locked, 30-CONTEXT.md): add `leadNote: z.string().max(5000).optional()` to `submitSchema` in src/lib/validation.ts (additive; /api/submit accepts-but-ignores until Phase 33). If already present, leave it.

From src/lib/screens.ts (re-use — never reimplement; these are the server-mirrored screens):
```typescript
export function screenText(text): { banned; incitement; hateTone; matches };
export function screenInnocentParty(text: string): boolean;
export function screenPrivateTargeting(text: string): boolean;
export function screenMismatch(entityType: string, entityRole: string): boolean;
export function isCoarseLocationClean(value: string): boolean;
export type PersistResult<T> = {ok:true;data:T} | {ok:false;code:string;message:string;field?:string};
```
runScreens server cascade order (for screenDescribeStep to mirror): NO_SOURCE → WEAK_SOURCE → GROUP_TARGET → INCITEMENT → HATE_TONE → INNOCENT_PARTY → PRIVATE_TARGETING → MISMATCH. screenDescribeStep covers only the free-text middle: GROUP_TARGET → INCITEMENT → HATE_TONE → INNOCENT_PARTY → PRIVATE_TARGETING.

From src/lib/wizard/registry.ts (the StepDef shape and current STEPS — append, do not rewrite the scaffold):
```typescript
export interface StepDef {
  readonly id: string; readonly archetype: "choice"|"input"; readonly titleKey: string;
  readonly requires?: (form: WizardState["form"]) => boolean;
  readonly completionGate?: boolean;
  readonly branchWhen?: (form: WizardState["form"]) => boolean;
}
export const STEPS = [ /* scaffold-choice, scaffold-input */ ] as const satisfies ReadonlyArray<StepDef>;
export type StepId = (typeof STEPS)[number]["id"];
```
The nav/reachability helpers (nextStep/prevStep/isReachable/firstIncompleteStep/visibleStepCount/visibleStepIndex/isCountedStep) iterate STEPS generically — appending steps needs no change to them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add leadNote to the contract + pure step-logic module</name>
  <files>src/lib/validation.ts, src/lib/wizard/step-logic.ts</files>
  <read_first>
    - src/lib/wizard/step-logic.ts (the file being created — confirm absent first)
    - src/lib/validation.ts (the file being modified — add leadNote to submitSchema only)
    - src/lib/screens.ts (re-use screenMismatch, screenText, screenInnocentParty, screenPrivateTargeting, isCoarseLocationClean — NEVER reimplement their regexes)
    - src/lib/wizard/state.ts (WizardState.form is SubmitInput — the type the predicates receive)
    - .planning/UI-SPEC.md (§3 Step 2/5/6 required fields; §8 interim source-type-in-title mapping; §9 screen order)
    - .planning/phases/30-input-evidence-steps/30-CONTEXT.md (slug set, em-dash location composition, gate-on-links-only)
  </read_first>
  <behavior>
    - composeLocation("Syria","") returns "Syria"; composeLocation("Syria","Homs") returns "Syria — Homs" (em-dash U+2014, single spaces); composeLocation("","") returns "".
    - SOURCE_TYPE_SLUGS is exactly the ordered tuple un, court, sanctions, hr, journalism, official.
    - prefixSourceType("court","UN report") returns "[TYPE: court] UN report"; re-applying it to the result does NOT double-prefix (idempotent — strips any existing leading [TYPE: <slug>] token first); prefixSourceType("","x") returns "x" unchanged (empty slug = no type chosen).
    - screenIdentityStep(form) returns a MISMATCH PersistResult when screenMismatch(entityType, entityRole) is true, else {ok:true} (non-blocking warning surface).
    - screenDescribeStep(form) runs GROUP_TARGET→INCITEMENT→HATE_TONE→INNOCENT_PARTY→PRIVATE_TARGETING in that order on the entityName+" "+entityRole+" "+allegationDescription join, returning the FIRST failing code, else {ok:true}.
    - evidenceSourceCount(form) returns the count of source-links with a non-empty url (files do NOT count — server WEAK_SOURCE uses sourceLinks.length only).
    - screenMediaLink(url): returns false (reject) when screenPrivateTargeting matches a personal social link; "" returns true (optional); a clean url returns true.
    - requiresIdentity(form) true iff entityName.trim and entityRole.trim are non-empty AND allegationLocation is non-empty (country present in the composed string).
    - requiresDescribe(form) true iff allegationDescription.trim().length >= 20.
    - requiresEvidence(form) true iff evidenceSourceCount(form) >= 2 (links only).
    - requiresMedia(form) and requiresAboutYou(form) always return true (optional steps never block Next).
  </behavior>
  <action>
    First, in `src/lib/validation.ts` add `leadNote: z.string().max(5000).optional()` to `submitSchema` (additive only; do not touch other fields) unless already present.
    Then create `src/lib/wizard/step-logic.ts` — pure module, NO `"use client"`, NO React/JSX, ONLY `import type` for SubmitInput/WizardState (strips to zero runtime deps, Phase 28 precedent). Import the screen FUNCTIONS value-side from `@/lib/screens` (runtime-pure). Export: `composeLocation(country,city)` joining with the U+2014 em-dash surrounded by single spaces, country-only when city empty, "" when both empty; `SOURCE_TYPE_SLUGS` as an `as const` tuple of the six §3 slugs in order; `prefixSourceType(slug,title)` that strips a leading `[TYPE: <one-of-the-six-slugs>] ` token via regex then re-prefixes only when slug is non-empty (single source of truth for the §8 interim encoding — idempotent); `screenIdentityStep(form)` delegating to `screenMismatch`; `screenDescribeStep(form)` reproducing the runScreens middle-screen ordering on the same `entityName + " " + entityRole + " " + allegationDescription` concatenation the server uses (call screenText/screenInnocentParty/screenPrivateTargeting — do NOT inline any regex); `evidenceSourceCount(form)` counting non-empty source-link urls; `screenMediaLink(url)` delegating to `screenPrivateTargeting`; and the five predicates `requiresIdentity`/`requiresDescribe`/`requiresEvidence`/`requiresMedia`/`requiresAboutYou` per the behavior table. Document each export with a one-line comment naming the UI-SPEC §/S-rule it enforces. NO fenced code, NO banned-pattern regex literals anywhere in this file.
  </action>
  <acceptance_criteria>
    - `node --experimental-strip-types -e "import('./src/lib/wizard/step-logic.ts').then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})"` exits 0 (module loads runtime-pure).
    - `grep -nE "composeLocation|SOURCE_TYPE_SLUGS|prefixSourceType|screenIdentityStep|screenDescribeStep|evidenceSourceCount|screenMediaLink|requiresIdentity|requiresDescribe|requiresEvidence|requiresMedia|requiresAboutYou" src/lib/wizard/step-logic.ts` returns all 12 names.
    - `grep -E "from \"@/lib/screens\"" src/lib/wizard/step-logic.ts` confirms screen re-use; `grep -v '^ *\*' src/lib/wizard/step-logic.ts | grep -cE "علوي|نصيري|اقتلوا|facebook\\.com"` returns 0 (no reimplemented regexes outside comments).
    - `grep -cE "\"use client\"|from \"react\"" src/lib/wizard/step-logic.ts` returns 0 (`import type` only).
    - `grep -n "leadNote" src/lib/validation.ts` returns the added optional field.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types -e "import('./src/lib/wizard/step-logic.ts').then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})" && grep -q "leadNote" src/lib/validation.ts && npx tsc --noEmit</automated>
  </verify>
  <done>leadNote added to submitSchema; step-logic.ts exports the 12 names, re-uses screens.ts (no reimplemented regexes), is runtime-pure; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Append the five Phase-30 input steps to the registry + regression script</name>
  <files>src/lib/wizard/registry.ts, scripts/step-logic-check.js</files>
  <read_first>
    - src/lib/wizard/registry.ts (the file being modified — append to STEPS; do NOT alter the two scaffold entries or the nav/reachability helpers)
    - src/lib/wizard/step-logic.ts (the requires predicates from Task 1)
    - scripts/wizard-reducer-check.js (the established no-test-framework regression pattern to mirror)
    - scripts/step-logic-check.js (the file being created — confirm absent)
    - .planning/UI-SPEC.md (§3 step order: Identity=Step 2, Describe=Step 5, Evidence=Step 6, Media=Step 7, About-you=Step 8)
  </read_first>
  <action>
    In `registry.ts`, import the five predicates from `./step-logic`. Append five `input`-archetype StepDefs to `STEPS` (keep `as const satisfies ReadonlyArray<StepDef>`): ids `identity`, `describe`, `evidence`, `media`, `about-you`, each with its step titleKey (`q_identity`/`q_describe`/`q_evidence`/`q_media`/`q_aboutYou`, added in Task 3) and its requires predicate (`requiresIdentity`/`requiresDescribe`/`requiresEvidence`/`requiresMedia`/`requiresAboutYou`). Append AFTER the scaffold steps in §3 order (Phase 29 inserts choice steps later; the generic helpers handle any order). Add NO `branchWhen` to these. Then create `scripts/step-logic-check.js` mirroring `wizard-reducer-check.js`: `spawnSync(process.execPath, ["--experimental-strip-types", harness])` asserting the Task-1 behavior table — composeLocation (em-dash join, country-only, empty), prefixSourceType (idempotency + empty-slug passthrough), evidenceSourceCount (counts links not files: fixture 2 files + 1 link → 1), screenMediaLink (rejects a facebook.com url, passes ""), and each requires predicate at its boundary. Exit 1 on any mismatch, 0 on all pass with a summary line.
  </action>
  <acceptance_criteria>
    - `grep -cE "id: \"(identity|describe|evidence|media|about-you)\"" src/lib/wizard/registry.ts` returns 5.
    - `grep -nE "requiresIdentity|requiresDescribe|requiresEvidence|requiresMedia|requiresAboutYou" src/lib/wizard/registry.ts` confirms all five predicates wired.
    - `grep -c "branchWhen" src/lib/wizard/registry.ts` equals the pre-edit count (no new branch skips).
    - `node scripts/step-logic-check.js` exits 0 with a passing summary.
    - `npx tsc --noEmit` exits 0 (StepId union now includes the five ids).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/step-logic-check.js && npx tsc --noEmit && test "$(grep -cE 'id: \"(identity|describe|evidence|media|about-you)\"' src/lib/wizard/registry.ts)" = "5"</automated>
  </verify>
  <done>STEPS contains the five input steps with correct requires predicates; the regression script passes; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add all Phase-30 i18n keys in EN + AR parity</name>
  <files>messages/en.json, messages/ar.json</files>
  <read_first>
    - messages/en.json (the file being modified — the `submit` namespace)
    - messages/ar.json (the file being modified — the `submit` namespace, must reach key parity)
    - .planning/UI-SPEC.md (§3 the pinned key list; §10 the AR/EN samples + parity rule)
    - .planning/phases/30-input-evidence-steps/30-CONTEXT.md (§3 specifics: exact key list)
  </read_first>
  <action>
    Add the Phase-30 keys under the existing `submit` namespace in BOTH `messages/en.json` and `messages/ar.json`, flat camelCase, full parity. The set: step titles `q_identity`, `q_describe`, `q_evidence`, `q_media`, `q_aboutYou`; identity `idName`, `idRole`, `idRoleHint`, `idCountry`, `idArea`, `idAreaHint`, `idPublicRef`, `idLocationError` (S5 street-level block message), `idMismatchNotice`; describe `descLabel`, `descHint`, `descCounter`, `descWarnGroup`, `descWarnPrivate`, `descWarnInnocent`, `descWarnIncitement`, `descWarnTone`; evidence `sourcesTitle`, `sourcesRule`, `sourceUrl`, `sourceType`, `sourceType_un`, `sourceType_court`, `sourceType_sanctions`, `sourceType_hr`, `sourceType_journalism`, `sourceType_official`, `sourceTitleField`, `removeSource`, `sourceCounter`, `leadNote`, `leadNoteHint`; media `mediaTitle`, `mediaSafety`, `mediaLink`, `mediaLinkError`, `skip`; about-you `anonTitle`, `anonToggle`, `anonHelp`. Reuse (do NOT duplicate) existing keys `period`, `addSource`, `uploading`, `removeFile`, `fullName`, `email`, `back`, `next`. EN copy terse legal-register; AR full equivalents (RTL-safe, no machine strings). `descCounter` and `sourceCounter` use an ICU `{count}` placeholder so the live counter interpolates.
  </action>
  <acceptance_criteria>
    - `node -e "const e=require('./messages/en.json').submit,a=require('./messages/ar.json').submit;const ek=Object.keys(e),ak=Object.keys(a);const miss=ek.filter(k=>!(k in a)).concat(ak.filter(k=>!(k in e)));if(miss.length){console.error('PARITY GAP:',miss);process.exit(1)}console.log('parity OK',ek.length)"` exits 0.
    - `node -e "const e=require('./messages/en.json').submit;const need=['q_identity','q_describe','q_evidence','q_media','q_aboutYou','idName','idRole','idCountry','idLocationError','idMismatchNotice','descLabel','descCounter','descWarnGroup','descWarnPrivate','descWarnInnocent','descWarnIncitement','descWarnTone','sourcesRule','sourceType_un','sourceType_court','sourceType_sanctions','sourceType_hr','sourceType_journalism','sourceType_official','leadNote','leadNoteHint','mediaSafety','mediaLinkError','anonTitle','anonToggle','anonHelp'];const miss=need.filter(k=>!(k in e));if(miss.length){console.error('MISSING EN:',miss);process.exit(1)}console.log('all present')"` exits 0.
    - `node -e "JSON.parse(require('fs').readFileSync('messages/ar.json','utf8'));JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"` exits 0 (valid JSON both files).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const e=require('./messages/en.json').submit,a=require('./messages/ar.json').submit;const ek=Object.keys(e),ak=Object.keys(a);const miss=ek.filter(k=>!(k in a)).concat(ak.filter(k=>!(k in e)));if(miss.length){console.error('PARITY GAP:',miss);process.exit(1)}console.log('parity OK',ek.length)" && npx tsc --noEmit</automated>
  </verify>
  <done>All Phase-30 keys present in EN+AR with full parity; both JSON files valid; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/submit | All wizard input is untrusted until the server re-validates via persist.ts |
| sessionStorage draft → reducer | Restored draft is untrusted (handled by existing T-28-06 allowlist) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | client screens diverging from server | mitigate | step-logic screen runners call the screens.ts functions verbatim (no reimplemented regexes); grep gate forbids banned-pattern literals in step-logic.ts; server persist.ts stays authoritative |
| T-30-02 | Information disclosure | leadNote leaking into public allegationDescription | mitigate | leadNote is a SEPARATE schema field; step-logic never folds it into allegationDescription; Plan 03 writes it to form.leadNote only |
| T-30-03 | Elevation of privilege | client gate ≥2 bypassed | accept | reachability/gate is UX-only; /api/submit WEAK_SOURCE re-checks sourceLinks.length — server remains the trust boundary (T-28-07 precedent) |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | zero new dependencies this phase; no install tasks (no package-legitimacy gate needed) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the three modified source files.
- `node scripts/step-logic-check.js` and `node scripts/screens-parity-check.js` and `node scripts/wizard-reducer-check.js` all exit 0 (no regression in the prior pure-logic gates).
- EN↔AR `submit` parity holds.
</verification>

<success_criteria>
- step-logic.ts is the single source of truth for location composition, the §8 source-type token, the per-step screens, the ≥2 source count, and the five requires predicates — all regression-gated and runtime-pure.
- Registry contains the five Phase-30 input steps; downstream component plans and WizardClient can consume them.
- leadNote is on the SubmitInput contract; all Phase-30 i18n keys ship in EN+AR parity.
</success_criteria>

<output>
Create `.planning/phases/30-input-evidence-steps/30-01-SUMMARY.md` when done.
</output>
