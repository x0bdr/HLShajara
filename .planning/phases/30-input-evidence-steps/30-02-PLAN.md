---
phase: 30-input-evidence-steps
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/components/wizard/IdentityStep.tsx
  - src/components/wizard/DescribeStep.tsx
autonomous: true
requirements: [STEP-02, STEP-04]
must_haves:
  truths:
    - "The submitter enters name (required), documented role (required), country (required select), and an optional coarse governorate/city; entering a street-level address in the city field is blocked inline with the S5 message"
    - "A type/role MISMATCH surfaces inline as a non-blocking .filter-notice as the submitter types"
    - "The description field shows a live character counter and surfaces GROUP_TARGET/PRIVATE_TARGETING/INNOCENT_PARTY/INCITEMENT/HATE_TONE warnings inline in server order"
  artifacts:
    - path: "src/components/wizard/IdentityStep.tsx"
      provides: "Step 2 identity input — name, role, country, coarse city, optional public ref, inline MISMATCH + S5 block"
      min_lines: 60
    - path: "src/components/wizard/DescribeStep.tsx"
      provides: "Step 5 describe input — textarea with live counter, coarse period, inline screen warnings in server order"
      min_lines: 50
  key_links:
    - from: "src/components/wizard/IdentityStep.tsx"
      to: "src/lib/wizard/step-logic.ts"
      via: "composeLocation + screenIdentityStep"
      pattern: "composeLocation|screenIdentityStep"
    - from: "src/components/wizard/DescribeStep.tsx"
      to: "src/lib/wizard/step-logic.ts"
      via: "screenDescribeStep"
      pattern: "screenDescribeStep"
---

<objective>
Build the two text-input step components: Step 2 (Identity) and Step 5 (Describe the act). Both port the field patterns from the legacy `SubmitClient.tsx` (label + `.ds-input`/textarea bound to the reducer via `SET_FIELD`) and wire the Plan-01 pure helpers for inline validation. Identity composes coarse location, blocks street-level input inline (S5), and surfaces a non-blocking MISMATCH notice. Describe shows a live counter and the server-ordered screen warnings.

Purpose: These are the two free-text steps where the privacy/identity guardrails are enforced inline as the submitter types — the inline screens guide rephrasing toward documented conduct.
Output: `src/components/wizard/IdentityStep.tsx`, `src/components/wizard/DescribeStep.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-input-evidence-steps/30-CONTEXT.md
@.planning/UI-SPEC.md

<interfaces>
<!-- Contracts the executor builds against. -->

From src/lib/wizard/step-logic.ts (Plan 01):
```typescript
export function composeLocation(country: string, city: string): string;   // "Syria — Homs" | "Syria" | ""
export function screenIdentityStep(form: SubmitInput): PersistResult<...>; // MISMATCH warning or {ok:true}
export function screenDescribeStep(form: SubmitInput): PersistResult<...>; // first failing middle-screen code or {ok:true}
```
From src/lib/screens.ts: `isCoarseLocationClean(value: string): boolean` — false when a street-level token is present (S5).
From src/lib/wizard/state.ts: `WizardAction` with `{ type:"SET_FIELD"; field: keyof SubmitInput; value }`; the reducer dispatch is passed in as a prop.

The InputStep archetype contract (the existing scaffold these mirror) — props the parent (WizardClient) passes to InputStep are `{ fieldId, label, value, dispatch }`. The richer step components in THIS plan receive `{ form: SubmitInput; dispatch: Dispatch<WizardAction> }` (the full controlled form + reducer dispatch). They render the step BODY only — WizardPanel supplies the heading, WizardNav supplies the gated Next. Do NOT render a Next button here.

CSS classes available (src/components/hlshajara.css — token-only, reuse; add NO new CSS):
`.form-field`, `.form-row`, `.ds-input`, `.ds-select`, `.legal`, `.legal-error`, `.filter-notice`, `.ds-caption`, `.ds-body-sm`.
MISMATCH → `.filter-notice` (non-blocking, per 30-CONTEXT). S5 street-level block + describe screen warnings → `.legal-error`.

Legacy field markup to PORT (src/app/[locale]/submit/SubmitClient.tsx): name input 170-179; type/role row 180-205; description textarea 211-221 (keep `style={{resize:"vertical"}}` — the one retained inline); period/location row 222-241. Re-use these shapes, swapping useState → props.form + dispatch SET_FIELD.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IdentityStep (Step 2) — name, role, country, coarse city, public ref, inline MISMATCH + S5 block</name>
  <files>src/components/wizard/IdentityStep.tsx</files>
  <read_first>
    - src/components/wizard/IdentityStep.tsx (the file being created — confirm absent)
    - src/components/wizard/InputStep.tsx (the scaffold input archetype this mirrors — label + ds-input + SET_FIELD)
    - src/app/[locale]/submit/SubmitClient.tsx (PORT the name/type/role markup, lines 170-205)
    - src/lib/wizard/step-logic.ts (composeLocation, screenIdentityStep)
    - src/lib/screens.ts (isCoarseLocationClean for the S5 city-field block)
    - src/lib/wizard/state.ts (WizardAction / SET_FIELD)
    - .planning/UI-SPEC.md (§3 Step 2 field table; §9 MISMATCH→.filter-notice, coarse-location enforcement; S1-S5 guardrails)
  </read_first>
  <behavior>
    - Renders exactly five controlled fields: name (text, required), documented role/title (text, required), country (select, required), governorate/city (text, optional, coarse), public identifier (text, optional). NO field for phone, plate, street address, map pin, social handle, sect, loyalty, or profession (S1-S4 — verified absent by grep).
    - Country + city compose via composeLocation and write the result to form.allegationLocation through SET_FIELD on every change (country/city held as local UI state; the composed string is the single value written to the form).
    - When isCoarseLocationClean(city) is false, a .legal-error renders idLocationError AND the dirty street token is not written into the composed location — the S5 inline block.
    - As name/role/type change, screenIdentityStep(form).ok===false renders a non-blocking .filter-notice with idMismatchNotice; it disables nothing (warning only — Next gating lives in registry requiresIdentity).
    - Country is a select with a meaningful empty-placeholder option first, then a country list, so requiresIdentity (country present) is enforceable.
  </behavior>
  <action>
    Create `src/components/wizard/IdentityStep.tsx` as a `"use client"` component taking `{ form, dispatch }`, using `useTranslations("submit")`. Hold `country` and `city` in local `useState`, seeded by splitting form.allegationLocation on " — " when present. Render: a `.form-field` name input bound to `entityName` via SET_FIELD; a `.form-row` with the documented-role input (`entityRole`, helper `idRoleHint` in `.ds-caption`) and the country `.ds-select` (label `idCountry`, empty placeholder option then a country list); a `.form-field` governorate/city input (label `idArea`, helper `idAreaHint`) — on change, if `isCoarseLocationClean(value)` is false set a local error flag and render `idLocationError` in `.legal-error`, else clear it; on any country/city change dispatch `SET_FIELD allegationLocation composeLocation(country, cleanCity)`; a `.form-field` optional public-identifier input (label `idPublicRef`). Below role/type, compute `screenIdentityStep(form)` and when `!ok` render `idMismatchNotice` in `.filter-notice`. Render NO Next button. No `dangerouslySetInnerHTML`. Logical CSS only.
  </action>
  <acceptance_criteria>
    - `grep -nE "entityName|entityRole|idCountry|idArea|idPublicRef|composeLocation|screenIdentityStep|isCoarseLocationClean" src/components/wizard/IdentityStep.tsx` returns all eight tokens.
    - `grep -nE "filter-notice" src/components/wizard/IdentityStep.tsx` confirms the MISMATCH notice; `grep -nE "legal-error" src/components/wizard/IdentityStep.tsx` confirms the S5 block.
    - S1-S4 absence: `grep -ciE "phone|plate|license plate|street|home address|map.?pin|latitude|longitude|facebook|instagram|tiktok|linkedin|whatsapp|sect|religion|ethnic|tribe|loyalt|sympath|شبيح|profession" src/components/wizard/IdentityStep.tsx` returns 0.
    - `grep -cE "left:|right:|margin-left|margin-right|padding-left|padding-right" src/components/wizard/IdentityStep.tsx` returns 0 (logical props only).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "composeLocation" src/components/wizard/IdentityStep.tsx && test "$(grep -ciE 'phone|plate|street|home address|map.?pin|latitude|longitude|facebook|instagram|tiktok|linkedin|sect|religion|ethnic|tribe|loyalt|sympath|شبيح|profession' src/components/wizard/IdentityStep.tsx)" = "0" && npx tsc --noEmit</automated>
  </verify>
  <done>IdentityStep renders the five fields, composes coarse location, blocks street-level input inline (S5), surfaces a non-blocking MISMATCH notice, contains no S1-S4 field, and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DescribeStep (Step 5) — textarea, live counter, coarse period, inline screen warnings in server order</name>
  <files>src/components/wizard/DescribeStep.tsx</files>
  <read_first>
    - src/components/wizard/DescribeStep.tsx (the file being created — confirm absent)
    - src/app/[locale]/submit/SubmitClient.tsx (PORT the description textarea + period field, lines 211-241)
    - src/components/wizard/IdentityStep.tsx (the sibling pattern from Task 1 — same {form,dispatch} contract)
    - src/lib/wizard/step-logic.ts (screenDescribeStep — server-ordered middle screens)
    - src/lib/wizard/state.ts (SET_FIELD)
    - .planning/UI-SPEC.md (§3 Step 5 field table — min20 max10000, live counter; §9 warn-but-explain screens in server order)
  </read_first>
  <behavior>
    - Renders a description textarea bound to allegationDescription (SET_FIELD) and an optional coarse time-period text input bound to allegationPeriod.
    - A live character counter renders the current length via the descCounter ICU key and updates on every keystroke; the registry requiresDescribe predicate gates Next at >=20 chars (this component renders no Next).
    - screenDescribeStep(form) runs on every change; when !ok the matching warning body renders in a .legal-error, selected by the returned code → its key: GROUP_TARGET→descWarnGroup, PRIVATE_TARGETING→descWarnPrivate, INNOCENT_PARTY→descWarnInnocent, INCITEMENT→descWarnIncitement, HATE_TONE→descWarnTone. Because screenDescribeStep returns the FIRST failing code in server order, exactly one warning shows and it matches what /api/submit would return.
  </behavior>
  <action>
    Create `src/components/wizard/DescribeStep.tsx` as a `"use client"` component taking `{ form, dispatch }`, using `useTranslations("submit")`. Render a `.form-field` with label `descLabel`, helper `descHint` in `.ds-caption`, and a textarea bound to `allegationDescription` via SET_FIELD with `style={{resize:"vertical"}}` (the one retained inline, ported from the legacy form). Render the live counter `t("descCounter",{count: form.allegationDescription.length})` in `.ds-caption`. Render a `.form-field` optional period input (label `period`) bound to `allegationPeriod`. Compute `screenDescribeStep(form)`; when `!ok`, map `result.code` to its warning key via a small lookup object and render the translated string in `.legal-error`. Render NO Next button. No `dangerouslySetInnerHTML`. Logical CSS only.
  </action>
  <acceptance_criteria>
    - `grep -nE "allegationDescription|allegationPeriod|descCounter|screenDescribeStep" src/components/wizard/DescribeStep.tsx` returns all four.
    - `grep -cE "descWarnGroup|descWarnPrivate|descWarnInnocent|descWarnIncitement|descWarnTone" src/components/wizard/DescribeStep.tsx` returns 5 (the full server-order set is mapped).
    - `grep -nE "legal-error" src/components/wizard/DescribeStep.tsx` confirms the warning surface.
    - `grep -cE "left:|right:|margin-left|margin-right|padding-left|padding-right" src/components/wizard/DescribeStep.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "screenDescribeStep" src/components/wizard/DescribeStep.tsx && test "$(grep -cE 'descWarnGroup|descWarnPrivate|descWarnInnocent|descWarnIncitement|descWarnTone' src/components/wizard/DescribeStep.tsx)" -ge "5" && npx tsc --noEmit</automated>
  </verify>
  <done>DescribeStep renders the textarea + live counter + period, maps all five server-order screen codes to inline warnings, uses logical CSS only, and compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/submit | All identity/description text is untrusted; server persist.ts re-screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Information disclosure | street-level location persisting | mitigate | isCoarseLocationClean blocks the dirty token inline (S5); the composed location never carries a street token; server PRIVATE_TARGETING re-checks |
| T-30-05 | Tampering | XSS via controlled inputs | mitigate | React text binding auto-escapes; no dangerouslySetInnerHTML (grep gate) |
| T-30-06 | Spoofing | identity/sect/profession fields reintroduced | mitigate | S1-S4 grep-absence gate on IdentityStep.tsx fails the build if a banned field label/id appears |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` completes (both step components compile under the App Router; `/en/submit` + `/ar/submit` still build).
- S1-S4 grep-absence holds on IdentityStep.tsx.
- All five describe-screen warning keys are mapped.
</verification>

<success_criteria>
- Identity step: name/role/country required, coarse city optional, street-level blocked inline (S5), MISMATCH non-blocking notice, no S1-S4 field.
- Describe step: live counter, coarse period, all five middle-screen warnings inline in server order.
</success_criteria>

<output>
Create `.planning/phases/30-input-evidence-steps/30-02-SUMMARY.md` when done.
</output>
