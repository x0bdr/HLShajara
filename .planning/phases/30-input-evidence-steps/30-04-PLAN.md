---
phase: 30-input-evidence-steps
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/components/wizard/AboutYouStep.tsx
autonomous: true
requirements: [STEP-05]
must_haves:
  truths:
    - "Anonymity is ON by default and toggling it ON disables and clears the name and email fields (zeroed, not stashed)"
    - "When anonymity is OFF, optional name and email fields are editable and labeled reviewer-follow-up-only / never published"
  artifacts:
    - path: "src/components/wizard/AboutYouStep.tsx"
      provides: "Step 8 — anonymity toggle (default ON) that disables+clears submitterName/submitterEmail"
      min_lines: 45
  key_links:
    - from: "src/components/wizard/AboutYouStep.tsx"
      to: "src/lib/wizard/state.ts"
      via: "SET_FIELD isAnonymous + clears submitterName/submitterEmail"
      pattern: "isAnonymous"
---

<objective>
Build the About-you step (Step 8): the anonymity toggle that defaults ON (already seeded `true` in Phase 28 state) and, when ON, disables and clears the optional name and email fields. This is the S7 anonymity-default-on guardrail enforced in the UI.

Purpose: Anonymity-by-default protects submitters; clearing (not stashing) name/email when anonymous means no contact data lingers in the draft for an anonymous submission.
Output: `src/components/wizard/AboutYouStep.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-input-evidence-steps/30-CONTEXT.md
@.planning/UI-SPEC.md

<interfaces>
<!-- Contracts the executor builds against. -->

From src/lib/wizard/state.ts: `isAnonymous` seeds to `true` (UI-SPEC §8 / S7). The reducer's `SET_FIELD` writes any SubmitInput field:
```typescript
| { type:"SET_FIELD"; field:keyof SubmitInput; value: SubmitInput[keyof SubmitInput] }
```
form.isAnonymous: boolean; form.submitterName?: string; form.submitterEmail?: string.

The component receives `{ form, dispatch }` (same contract as the other step components). It renders the step BODY only — WizardPanel supplies the heading, WizardNav supplies the Next (registry requiresAboutYou is always-true, so Next is never blocked here). Render NO Next button.

Legacy markup to PORT (src/app/[locale]/submit/SubmitClient.tsx): email/name row 325-344; the anonymity checkbox 345-362 (swap the inline-style checkbox label for the existing checkbox pattern + tokens where possible; the legacy used inline styles — prefer `.form-field`/`.ds-input` + a minimal checkbox).

CSS classes (reuse; add NO new CSS): `.form-field`, `.form-row`, `.ds-input`, `.legal`, `.ds-caption`.
Anonymity clearing rule (30-CONTEXT S7): toggling anonymity ON dispatches `SET_FIELD isAnonymous true` AND `SET_FIELD submitterName ""` AND `SET_FIELD submitterEmail ""` (zeroed, NOT stashed/restored). Toggling OFF only sets isAnonymous false and re-enables the fields (empty until the user types).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AboutYouStep (Step 8) — anonymity toggle default ON, disables + clears name/email</name>
  <files>src/components/wizard/AboutYouStep.tsx</files>
  <read_first>
    - src/components/wizard/AboutYouStep.tsx (the file being created — confirm absent)
    - src/app/[locale]/submit/SubmitClient.tsx (PORT the email/name row + anonymity checkbox, lines 325-362)
    - src/components/wizard/IdentityStep.tsx (sibling {form,dispatch} pattern from Plan 02, if present — else the InputStep scaffold)
    - src/lib/wizard/state.ts (isAnonymous seeds true; SET_FIELD)
    - .planning/UI-SPEC.md (§3 Step 8 field table; S7 anonymity-default-on + disable+clear)
    - .planning/phases/30-input-evidence-steps/30-CONTEXT.md (S7: zeroed, not stashed/restored)
  </read_first>
  <behavior>
    - The anonymity checkbox reflects form.isAnonymous (seeded true → checked on first render).
    - When the checkbox is toggled ON, dispatch isAnonymous=true AND clear submitterName="" AND submitterEmail="" (three SET_FIELD dispatches); the name and email inputs become disabled.
    - When toggled OFF, dispatch isAnonymous=false; name and email inputs become enabled (empty until typed); nothing is restored (no stash).
    - While isAnonymous is true, the name/email inputs are rendered disabled (the disabled attribute is bound to form.isAnonymous) so they cannot be edited.
    - Helper copy (anonHelp) states the contact is used only for reviewer follow-up and is never published.
  </behavior>
  <action>
    Create `src/components/wizard/AboutYouStep.tsx` as a `"use client"` component taking `{ form, dispatch }`, using `useTranslations("submit")`. Render the `anonTitle` area, a checkbox bound to `form.isAnonymous` (label `anonToggle`); its onChange: when newly checked, dispatch `SET_FIELD isAnonymous true`, `SET_FIELD submitterName ""`, `SET_FIELD submitterEmail ""`; when unchecked, dispatch `SET_FIELD isAnonymous false`. Render a `.form-row` with an email input (`submitterEmail`, label `email`) and a name input (`submitterName`, label `fullName`), BOTH `disabled={form.isAnonymous}` and bound via SET_FIELD. Render the `anonHelp` helper in `.legal` or `.ds-caption`. Render NO Next button. Replace the legacy inline-style checkbox label with token/class-based markup; keep a minimal checkbox. No `dangerouslySetInnerHTML`. Logical CSS only.
  </action>
  <acceptance_criteria>
    - `grep -nE "isAnonymous|submitterName|submitterEmail|anonToggle|anonHelp" src/components/wizard/AboutYouStep.tsx` returns all five tokens.
    - The clear-on-anon behavior is present: `grep -E "submitterName" src/components/wizard/AboutYouStep.tsx | grep -cE '\"\"|''' ` is >= 1 (an empty-string write to submitterName exists) — equivalently `grep -nE "field: \"submitterName\", value: \"\"" src/components/wizard/AboutYouStep.tsx` matches.
    - `grep -nE "disabled" src/components/wizard/AboutYouStep.tsx` confirms the inputs disable on anonymity.
    - `grep -cE "left:|right:|margin-left|margin-right|padding-left|padding-right" src/components/wizard/AboutYouStep.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "isAnonymous" src/components/wizard/AboutYouStep.tsx && grep -qE "disabled" src/components/wizard/AboutYouStep.tsx && grep -qE "submitterName" src/components/wizard/AboutYouStep.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>About-you step renders the default-ON anonymity toggle that disables+clears name/email when on, with reviewer-follow-up-only copy; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/submit | submitterName/email are untrusted and the server drops them when anonymous regardless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | Information disclosure | submitter contact lingering in an anonymous draft | mitigate | toggling anonymity clears submitterName/submitterEmail to "" (zeroed, not stashed); the cleared values persist to the draft |
| T-30-12 | Repudiation | anonymity default flipped off | mitigate | isAnonymous seeds true (Phase 28); checkbox reflects it; DB default flip is BE-04 (Phase 33) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` completes.
- The disable+clear behavior is present (grep) and the inputs bind `disabled` to isAnonymous.
</verification>

<success_criteria>
- Anonymity ON by default; toggling ON disables and clears (zeroes) name/email; OFF re-enables them empty; reviewer-follow-up-only copy present.
</success_criteria>

<output>
Create `.planning/phases/30-input-evidence-steps/30-04-SUMMARY.md` when done.
</output>
