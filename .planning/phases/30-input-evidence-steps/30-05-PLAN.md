---
phase: 30-input-evidence-steps
plan: 05
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03", "30-04"]
files_modified:
  - src/app/[locale]/submit/WizardClient.tsx
autonomous: false
requirements: [STEP-02, STEP-04, STEP-05, EV-01, EV-02, EV-03, EV-04]
must_haves:
  truths:
    - "Each Phase-30 input step id (identity, describe, evidence, media, about-you) renders its own component, with Next gated by the registry requires predicate"
    - "The live flow reaches Identity as Step 2 — after entity-subtype, BEFORE conduct/role — exactly per UI-SPEC §3 flow order"
    - "The full input-step flow is exercisable in a live browser in EN and AR: identity gating, S5 block, describe counter + screens, evidence 2-source gate + lead note, media upload + social-link rejection, anonymity disable+clear"
  artifacts:
    - path: "src/app/[locale]/submit/WizardClient.tsx"
      provides: "Input-step render dispatch routing each registry input id to its step component"
      contains: "IdentityStep"
  key_links:
    - from: "src/app/[locale]/submit/WizardClient.tsx"
      to: "src/components/wizard/IdentityStep.tsx"
      via: "import + render switch on currentStep"
      pattern: "IdentityStep"
    - from: "src/app/[locale]/submit/WizardClient.tsx"
      to: "src/lib/wizard/registry.ts"
      via: "requires predicate drives stepValid"
      pattern: "stepValid"
---

<objective>
Wire the five Phase-30 step components into WizardClient's render dispatch so each input-step id renders its own component (replacing the single scaffold InputStep fallback), with Next gated by the registry `requires` predicate already read into `stepValid`. Then verify the full input-step flow live in EN and AR — including that the submitter reaches Identity as Step 2, BEFORE conduct/role (UI-SPEC §3 flow order).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-input-evidence-steps/30-CONTEXT.md
@.planning/UI-SPEC.md

<interfaces>
<!-- Contracts the executor builds against. -->

From src/app/[locale]/submit/WizardClient.tsx (the file being modified — current render switch at ~lines 321-336):
```typescript
const archetype = stepDef?.archetype ?? "choice";
const stepValid = stepDef?.requires ? stepDef.requires(state.form) : true; // already gates Next per requires
// ...
{archetype === "choice" ? (
  <ChoiceStep .../>
) : (
  <InputStep fieldId="wizard-scaffold-input" label={stepTitle} value={state.form.entityName} dispatch={dispatch} />
)}
```
The input branch currently ALWAYS renders the scaffold InputStep. This plan replaces the input branch with a switch on `state.currentStep` that routes each Phase-30 input id to its component, falling back to the scaffold InputStep for `scaffold-input`.

Step components (Plans 02/03/04) all share the prop contract `{ form: state.form, dispatch }`:
```typescript
import { IdentityStep } from "@/components/wizard/IdentityStep";   // id "identity"
import { DescribeStep } from "@/components/wizard/DescribeStep";   // id "describe"
import { EvidenceStep } from "@/components/wizard/EvidenceStep";   // id "evidence"
import { MediaStep } from "@/components/wizard/MediaStep";         // id "media"
import { AboutYouStep } from "@/components/wizard/AboutYouStep";   // id "about-you"
```
The registry (Plan 01) defines these five ids with their `requires` predicates and inserts them in UI-SPEC §3 flow order (identity between entity-subtype and conduct); `stepValid` already reads `stepDef.requires(state.form)`, so wiring the render switch is sufficient — the Next gate follows automatically. WizardNav already renders the gated Next only for the `input` archetype.

DO NOT change routing, draft, beforeunload, auto-advance, branching, or submit logic — only the render-dispatch of the input branch and the new imports.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route each input-step id to its component in WizardClient's render switch</name>
  <files>src/app/[locale]/submit/WizardClient.tsx</files>
  <read_first>
    - src/app/[locale]/submit/WizardClient.tsx (the file being modified — the render switch ~321-336; imports ~62-66)
    - src/components/wizard/IdentityStep.tsx (Plan 02 — props {form,dispatch})
    - src/components/wizard/DescribeStep.tsx (Plan 02)
    - src/components/wizard/EvidenceStep.tsx (Plan 03)
    - src/components/wizard/MediaStep.tsx (Plan 03)
    - src/components/wizard/AboutYouStep.tsx (Plan 04)
    - src/lib/wizard/registry.ts (Plan 01 — the five input ids + requires predicates that already drive stepValid)
  </read_first>
  <action>
    In `WizardClient.tsx`, add the five imports (IdentityStep, DescribeStep, EvidenceStep, MediaStep, AboutYouStep). Replace the input branch of the render switch: instead of always rendering the scaffold InputStep, render a sub-switch on `state.currentStep` — `"identity" → <IdentityStep form={state.form} dispatch={dispatch}/>`, `"describe" → <DescribeStep .../>`, `"evidence" → <EvidenceStep .../>`, `"media" → <MediaStep .../>`, `"about-you" → <AboutYouStep .../>`, and the default (`scaffold-input`) → the existing scaffold `<InputStep .../>` unchanged. Keep the `archetype === "choice"` branch and ALL other wiring (routing/draft/beforeunload/auto-advance/branching/submit) byte-for-byte unchanged. Do not alter `stepValid` (it already reads the registry `requires`). No `dangerouslySetInnerHTML`.
  </action>
  <acceptance_criteria>
    - `grep -cE "IdentityStep|DescribeStep|EvidenceStep|MediaStep|AboutYouStep" src/app/[locale]/submit/WizardClient.tsx` returns >= 10 (five imports + five render sites).
    - `grep -nE "case \"identity\"|=== \"identity\"|currentStep" src/app/[locale]/submit/WizardClient.tsx` confirms the input sub-switch keys off currentStep.
    - The scaffold fallback survives: `grep -nE "InputStep" src/app/[locale]/submit/WizardClient.tsx` still matches (default branch unchanged).
    - Unchanged wiring intact: `grep -cE "saveDraft|beforeunload|onChoiceConfirm|firstIncompleteStep|/api/submit" src/app/[locale]/submit/WizardClient.tsx` is unchanged from before the edit.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` completes and `/en/submit` + `/ar/submit` build as before.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE 'IdentityStep|DescribeStep|EvidenceStep|MediaStep|AboutYouStep' src/app/[locale]/submit/WizardClient.tsx)" -ge "10" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Each Phase-30 input id renders its own component; the scaffold fallback and all other wiring are intact; tsc + build pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the live input-step flow in EN + AR</name>
  <files>src/app/[locale]/submit/WizardClient.tsx</files>
  <action>Pause for human verification after Task 1 completes. Exercise the live wizard input steps in both languages per the how-to-verify steps; this checkpoint verifies the flow ORDER (Identity reached as Step 2, before Conduct) and the interaction-driven behaviors (inline validation, upload, gating, anonymity clearing, RTL) that a build cannot prove. Do not auto-advance — wait for "approved".</action>
  <what-built>
    The five Phase-30 input steps are wired into the live wizard in UI-SPEC §3 flow order: Identity (Step 2, between entity-subtype and conduct), Describe (Step 5), Evidence (Step 6 keystone), Media (Step 7), About-you (Step 8). All auto-tasks pass tsc + build; the flow order and the inline-validation, upload, gate, and anonymity behaviors need a live browser pass in both languages.
  </what-built>
  <how-to-verify>
    Run `npm run dev`, then exercise BOTH `http://localhost:3000/en/submit` and `http://localhost:3000/ar/submit` (advance through the choice steps to reach the input steps; if Phase 29 choice steps are not yet built, deep-link via `?step=identity` etc., which the reachability guard may redirect — in that case fill prior steps first):
    0. FLOW ORDER (the order fix): walk the wizard forward from the start. After actor-class (Step 1) and — for an entity — entity-subtype (Step 1b), the NEXT step MUST be IDENTITY (`?step=identity`, "Step 2 of M"), NOT conduct. Confirm Identity comes BEFORE "What did they do?" (conduct) and "Their role in that act" (role-in-act). The visible "Step N of M" reads Identity as the step right after the actor/subtype choices and before conduct. If conduct/role appear before Identity, FAIL — the STEPS array order is wrong.
    1. IDENTITY (Step 2): Next stays disabled until name + role + country are set. Type a street-level address (e.g. "شارع الحمراء 12" / "12 Baker Street") into the governorate/city field — an inline "use city/governorate only" message appears and the dirty token is not composed into the location (S5). Enter an org descriptor as the role of an "individual" — a non-blocking MISMATCH notice appears but Next is NOT blocked by it.
    2. DESCRIBE (Step 5): the live character counter updates as you type; Next stays disabled below 20 chars. Enter text that trips a screen (e.g. a banned identity term) — the matching warning appears inline, and only one shows (server order).
    3. EVIDENCE (Step 6): the "minimum 2 independent public sources" rule shows at the TOP up front. Add one source link — Next stays disabled (1 link). Uploading a file in Step 7 does NOT unlock Step 6 (links-only gate). Add a second link — Next enables. Pick a source type from the select — the row title gets a "[TYPE: …]" token; re-picking does not double it. Type a lead note — it is clearly labeled non-public / never-a-source.
    4. MEDIA (Step 7): upload an image or PDF — a removable card appears; the file picker offers no video. Paste a personal social link (e.g. facebook.com/…) into the optional link field — it is rejected inline. Skip/Next works (optional).
    5. ABOUT YOU (Step 8): the anonymity toggle is ON by default; name/email are disabled. Toggle off — they enable; type values; toggle back on — they disable AND clear.
    6. RTL: in `/ar/submit` all of the above render right-to-left, labels are Arabic, no Latin uppercase/letter-spacing artifacts, and machine strings (URLs) stay LTR.
    Confirm `npm run build` is clean.
  </how-to-verify>
  <verify>
    <human-check>The live EN + AR input-step flow reaches Identity as Step 2 (before Conduct) and passes all seven checks above (0–6); build is clean.</human-check>
  </verify>
  <done>Human confirms the flow reaches Identity as Step 2 before Conduct AND all seven live checks pass in both EN and AR; the checkpoint returns "approved".</done>
  <resume-signal>Type "approved" when the live EN + AR flow reaches Identity as Step 2 (before Conduct) and passes all seven checks, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/submit, /api/upload | All step input is untrusted; server re-validates (persist.ts screens, upload EXIF strip + scan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-13 | Tampering | render switch leaking an unintended step | mitigate | the input sub-switch keys off the typed StepId union (registry); default falls back to the neutral scaffold InputStep; tsc enforces the known ids |
| T-30-14 | Elevation of privilege | bypassing required-field gates client-side | accept | gates are UX only; /api/submit remains authoritative (T-28-07 precedent) — verified by the live human-verify pass |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` completes; `/en/submit` + `/ar/submit` build.
- Live EN + AR human-verify pass confirms the flow reaches Identity as Step 2 (before Conduct), plus identity gating + S5 block, describe counter + screens, evidence 2-source links-only gate + token + lead note, media upload + no-video + social-link rejection, anonymity disable+clear, and RTL correctness.
</verification>

<success_criteria>
- Every Phase-30 input id renders its component with the correct Next gate.
- The live flow reaches Identity as Step 2, before Conduct/role (UI-SPEC §3 flow order).
- The full input-step flow is verified live in both languages; all seven checkpoint checks pass.
</success_criteria>

<output>
Create `.planning/phases/30-input-evidence-steps/30-05-SUMMARY.md` when done. Do NOT mark the plan complete until the human-verify checkpoint returns "approved".
</output>
