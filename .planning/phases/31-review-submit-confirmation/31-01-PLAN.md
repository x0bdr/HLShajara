---
phase: 31-review-submit-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizard/rejection-map.ts
  - src/lib/wizard/registry.ts
  - messages/en.json
  - messages/ar.json
  - scripts/i18n-submit-parity-check.js
autonomous: true
requirements: [REV-04, REV-01, REV-02, REV-03]
must_haves:
  truths:
    - "A server rejection code resolves to exactly one {messageKey, stepId} pair matching the LOCKED UI-SPEC §3 table."
    - "Every Phase-31 review/submit/confirmation string exists in EN and AR with identical key sets under the submit namespace."
    - "The registry's StepId union includes a terminal 'review' step appended AFTER 'about-you', so state.currentStep === 'review' can match and goTo/isReachable resolve it."
  artifacts:
    - path: "src/lib/wizard/rejection-map.ts"
      provides: "REJECTION_MAP Record<RejectionCode,{messageKey,stepId}> + resolveRejection() lookup"
      contains: "REJECTION_MAP"
    - path: "src/lib/wizard/registry.ts"
      provides: "Terminal 'review' StepDef appended after 'about-you' so StepId includes 'review'"
      contains: "review"
    - path: "messages/en.json"
      provides: "EN review/submit/confirmation keys under submit namespace"
      contains: "reviewTitle"
    - path: "messages/ar.json"
      provides: "AR parity for all Phase-31 submit keys"
      contains: "reviewTitle"
    - path: "scripts/i18n-submit-parity-check.js"
      provides: "Standalone EN/AR key-parity gate for the submit namespace (no test framework, mirrors screens-parity-check.js)"
      contains: "submit"
  key_links:
    - from: "src/lib/wizard/rejection-map.ts"
      to: "src/lib/wizard/registry.ts"
      via: "stepId values are registry StepId literals (tsc-enforced)"
      pattern: "StepId"
    - from: "src/lib/wizard/registry.ts"
      to: "src/app/[locale]/submit/WizardClient.tsx"
      via: "appended 'review' StepDef makes state.currentStep === 'review' a valid, reachable branch"
      pattern: "review"
    - from: "scripts/i18n-submit-parity-check.js"
      to: "messages/en.json + messages/ar.json"
      via: "reads both files, diffs submit-namespace key sets"
      pattern: "submit"
---

<objective>
Build the three pure foundations every later Phase-31 plan consumes: the terminal `review` step in the
wizard registry (so `state.currentStep === "review"` is a real, reachable branch), the central
rejection-routing lookup module (`src/lib/wizard/rejection-map.ts`), and the full EN+AR i18n key set for
review, submit, and confirmation — guarded by a standalone parity script.

Purpose: Plans 31-02 (Review component) and 31-03 (Submit/Confirmation wiring) both import the
rejection map, read the i18n keys, and assume a registered `review` step exists. Phases 29/30 register
`actor-class/entity-subtype/conduct/role-in-act` (29-01) and `identity/describe/evidence/media/about-you`
(30-01) but NEITHER adds a `review` step — so this plan must add it to `registry.ts` itself, as the final
step after `about-you`. Landing these foundations first lets each be gated by `tsc` + a parity script
before any JSX wiring, and keeps the two consuming plans free of registry, key-authoring, or
rejection-table work.

Output: the appended `review` StepDef in `registry.ts`, `rejection-map.ts`, the new EN/AR `submit` keys,
and `scripts/i18n-submit-parity-check.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-review-submit-confirmation/31-CONTEXT.md
@.planning/UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs; do NOT re-explore the codebase for these. -->

CANONICAL post-29/30 StepId set (AUTHORITATIVE — from 29-01 Task 2 + 30-01 Task 2; do NOT re-derive):
  actor-class (Step 1) · entity-subtype (Step 1b, branch-skipped) · conduct (Step 3) · role-in-act (Step 4) ·
  identity (Step 2) · describe (Step 5) · evidence (Step 6) · media (Step 7) · about-you (Step 8)
There is NO `review` step yet — THIS plan appends it (terminal, Step 9). The slugs `actor`/`you`/`review`
are NOT registry ids before Phase 31. Group→first-step Edit targets used downstream are the REAL slugs:
  Actor → actor-class · Conduct → conduct · Description → describe · Evidence → evidence · Media → media · You → about-you

Rejection codes + server cascade order (from src/lib/screens.ts runScreens / src/db/persist.ts):
  NO_SOURCE → WEAK_SOURCE → GROUP_TARGET → INCITEMENT → HATE_TONE → INNOCENT_PARTY → PRIVATE_TARGETING → MISMATCH

LOCKED UI-SPEC §3 rejection routing table (CONTEXT.md authoritative):
  NO_SOURCE        → messageKey "err_sources"  → evidence  (Step 6)
  WEAK_SOURCE      → messageKey "err_sources"  → evidence  (Step 6)
  PRIVATE_TARGETING→ messageKey "err_private"  → describe  (Step 5)
  GROUP_TARGET     → messageKey "err_group"    → describe  (Step 5)
  INNOCENT_PARTY   → messageKey "err_innocent" → describe  (Step 5)
  INCITEMENT       → messageKey "err_tone"     → describe  (Step 5)
  HATE_TONE        → messageKey "err_tone"     → describe  (Step 5)
  MISMATCH         → messageKey "err_mismatch" → identity  (Step 2)
All three rejection-map stepIds (evidence/describe/identity) are in the canonical post-29/30 set above.

StepDef shape + StepId derivation (src/lib/wizard/registry.ts):
  export interface StepDef { id; archetype: "choice"|"input"; titleKey; requires?; completionGate?; branchWhen? }
  export const STEPS = [ … ] as const satisfies ReadonlyArray<StepDef>;
  export type StepId = (typeof STEPS)[number]["id"];   // ← appending a member widens this union
  The nav/reachability helpers (nextStep/prevStep/isReachable/firstIncompleteStep/visibleStepCount/
  visibleStepIndex/isCountedStep) iterate STEPS generically — appending one member needs NO helper change.
  After 29/30 run, `about-you` is the current terminal step; the appended `review` step becomes the new terminal.

i18n shape: flat camelCase keys under the single top-level `submit` namespace in BOTH
messages/en.json and messages/ar.json. The `submit` namespace already holds the Phase 28 keys
(reviewTitle/affirm/etc. are NOT yet present). Read via `useTranslations("submit")`.

Parity-script precedent: scripts/screens-parity-check.js + scripts/wizard-reducer-check.js are
standalone `node` scripts (no test framework, no new deps). There is NO `check:i18n` npm script in
this repo; this plan creates the parity gate as a standalone node script in the same style.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the rejection-routing lookup module</name>
  <read_first>
    - src/lib/wizard/rejection-map.ts (the file being created — confirm absent first)
    - src/lib/screens.ts (rejection codes + the exact server cascade order in runScreens)
    - src/lib/wizard/registry.ts (the StepId type export the stepId values must satisfy)
    - .planning/phases/31-review-submit-confirmation/31-CONTEXT.md (LOCKED §3 routing table + canonical StepId set)
    - .planning/UI-SPEC.md (§3 Confirmation/Error rejection table)
  </read_first>
  <action>
    Create `src/lib/wizard/rejection-map.ts` as a pure, framework-free module — NO JSX, NO React,
    NO `"use client"`, NO `@/db` or server imports — mirroring the pure-module style of
    `src/lib/screens.ts`. Export a `RejectionCode` string-union type with exactly the eight codes
    `NO_SOURCE | WEAK_SOURCE | GROUP_TARGET | INCITEMENT | HATE_TONE | INNOCENT_PARTY |
    PRIVATE_TARGETING | MISMATCH` (the `screens.ts`/`persist.ts` cascade codes). Export a
    `RejectionRoute` interface `{ messageKey: string; stepId: StepId }`, importing `StepId` as a
    TYPE-only import from `./registry` so the module strips to zero runtime deps. Export a
    `const REJECTION_MAP: Record<RejectionCode, RejectionRoute>` populated EXACTLY per the LOCKED
    §3 table in the interfaces block: NO_SOURCE and WEAK_SOURCE → `{messageKey:"err_sources",
    stepId:"evidence"}`; PRIVATE_TARGETING → `err_private`/`describe`; GROUP_TARGET →
    `err_group`/`describe`; INNOCENT_PARTY → `err_innocent`/`describe`; INCITEMENT and HATE_TONE →
    `err_tone`/`describe`; MISMATCH → `err_mismatch`/`identity`. The three stepId literals used
    (`evidence`/`describe`/`identity`) are real registry ids in the canonical post-29/30 set — tsc
    enforces this against the `StepId` union. Export a
    `resolveRejection(code: string): RejectionRoute | null` that returns `REJECTION_MAP[code]` when
    `code` is a known `RejectionCode`, else `null` (so an unknown/`VALIDATION_ERROR` code falls back
    to a generic error rather than a bad goTo). Document with a header comment that the codes/order
    are grounded in `src/lib/screens.ts` and the routing in UI-SPEC §3, and that `stepId` values are
    the canonical registry slugs (`identity`/`describe`/`evidence`) registered by Phase 30 — NOT the
    friendly group names. Do NOT inline regexes or duplicate screen logic — this module is routing only.
  </action>
  <acceptance_criteria>
    - File `src/lib/wizard/rejection-map.ts` exists and imports `StepId` via `import type` only:
      `grep -qE 'import type .*StepId.* from "\./registry"' src/lib/wizard/rejection-map.ts`
    - Module is server-free: `grep -vqE '@/db|drizzle|"use client"|next/server' src/lib/wizard/rejection-map.ts`
      (no match for any of those tokens).
    - All eight codes present as map keys: `grep -c -E 'NO_SOURCE|WEAK_SOURCE|GROUP_TARGET|INCITEMENT|HATE_TONE|INNOCENT_PARTY|PRIVATE_TARGETING|MISMATCH' src/lib/wizard/rejection-map.ts`
      returns ≥ 8 (filter header comments first with `grep -v '^ \*' | grep -v '^//'`).
    - Routing tokens present: `grep -c -E 'err_sources|err_private|err_group|err_innocent|err_tone|err_mismatch' src/lib/wizard/rejection-map.ts` ≥ 6
      and `grep -c -E '"evidence"|"describe"|"identity"' src/lib/wizard/rejection-map.ts` ≥ 3.
    - `resolveRejection` exported: `grep -qE 'export function resolveRejection' src/lib/wizard/rejection-map.ts`.
    - Runs under type-stripping with the canonical routes (no test framework):
      `node --experimental-strip-types --input-type=module -e 'import {resolveRejection} from "file://'"$PWD"'/src/lib/wizard/rejection-map.ts"; const a=resolveRejection("WEAK_SOURCE"); const b=resolveRejection("MISMATCH"); const c=resolveRejection("UNKNOWN"); if(a.messageKey!=="err_sources"||a.stepId!=="evidence")throw new Error("WEAK_SOURCE route wrong"); if(b.messageKey!=="err_mismatch"||b.stepId!=="identity")throw new Error("MISMATCH route wrong"); if(c!==null)throw new Error("unknown code must be null"); console.log("OK")'`
      prints `OK` and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --input-type=module -e 'import {REJECTION_MAP,resolveRejection} from "file://'"$PWD"'/src/lib/wizard/rejection-map.ts"; const codes=["NO_SOURCE","WEAK_SOURCE","GROUP_TARGET","INCITEMENT","HATE_TONE","INNOCENT_PARTY","PRIVATE_TARGETING","MISMATCH"]; for(const c of codes){const r=REJECTION_MAP[c]; if(!r||!r.messageKey||!r.stepId)throw new Error("missing route "+c);} if(resolveRejection("nope")!==null)throw new Error("unknown not null"); console.log("PASS")'</automated>
  </verify>
  <done>rejection-map.ts exists, type-only StepId import, all 8 codes mapped to the LOCKED §3 {messageKey,stepId} pairs (stepIds evidence/describe/identity are real registry ids), resolveRejection returns null for unknown codes, runs clean under --experimental-strip-types.</done>
</task>

<task type="auto">
  <name>Task 2: Author the EN+AR review/submit/confirmation i18n keys</name>
  <read_first>
    - messages/en.json (the file being modified — the existing submit namespace + top-level order)
    - messages/ar.json (the file being modified — must mirror en key-for-key)
    - .planning/UI-SPEC.md (§3 Step 9 + Confirmation/Error keys; §6 review; §10 EN‖AR copy table)
    - .planning/phases/31-review-submit-confirmation/31-CONTEXT.md (key list: review*, affirm, success*, submitAnother, err_*)
  </read_first>
  <action>
    Add the following flat camelCase keys to the `submit` namespace in BOTH `messages/en.json` and
    `messages/ar.json`, with full EN+AR parity (same key set, both non-empty), terse legal-register
    copy (Claude's Discretion per CONTEXT). Required keys:
    `reviewTitle`, `reviewEdit`, `reviewGroupActor`, `reviewGroupConduct`, `reviewGroupDescription`,
    `reviewGroupEvidence`, `reviewGroupMedia`, `reviewGroupYou`, `reviewEmpty` (the "—" placeholder
    label / aria text), `reviewSourceTypeLabel` (the "Type: …" prefix shown after stripping the
    `[TYPE: <slug>]` token), `reviewLeadNoteLabel` (the distinct "Reviewer note (not published)"
    sub-block heading), `reviewSourcesShort` (sources count badge label), `affirm` (the affirmation
    checkbox label, copy from UI-SPEC §3/§10), `errSourcesGate` (the inline "minimum 2 sources"
    disabled-reason under the source list), `errAffirmGate` (the inline "check to confirm"
    disabled-reason under the affirmation), `successTitle`, `successBody` (the "what happens next —
    nothing public yet" note), `referenceIdLabel` (label preceding the mono submissionId),
    `submitAnother`, the terminal review step title `reviewStepTitle` (the `titleKey` the appended
    `review` StepDef references in Task 4), and the five rejection message keys `err_sources`,
    `err_private`, `err_group`, `err_innocent`, `err_tone`, `err_mismatch` (the values the
    rejection-map `messageKey`s resolve to via `t()`). Do NOT remove or rename any existing Phase 28
    `submit` key. Preserve the existing top-level namespace ordering; insert the new keys inside the
    existing `submit` object. Use logical, machine-string-safe copy (no `left/right`); the AR
    `affirm`/`successBody`/`err_*` use natural Arabic, not transliteration.
  </action>
  <acceptance_criteria>
    - All new keys exist in EN: `node -e 'const m=require("./messages/en.json").submit; const need=["reviewTitle","reviewEdit","reviewGroupActor","reviewGroupConduct","reviewGroupDescription","reviewGroupEvidence","reviewGroupMedia","reviewGroupYou","reviewEmpty","reviewSourceTypeLabel","reviewLeadNoteLabel","reviewSourcesShort","affirm","errSourcesGate","errAffirmGate","successTitle","successBody","referenceIdLabel","submitAnother","reviewStepTitle","err_sources","err_private","err_group","err_innocent","err_tone","err_mismatch"]; const miss=need.filter(k=>!(k in m)); if(miss.length)throw new Error("EN missing: "+miss.join(",")); console.log("EN OK")'`
    - All new keys exist in AR (same assertion against `./messages/ar.json`).
    - No new key has an empty value in either file: `node -e 'for(const f of ["en","ar"]){const m=require("./messages/"+f+".json").submit; for(const k of Object.keys(m)){if(typeof m[k]==="string"&&m[k].trim()==="")throw new Error(f+" empty: "+k);}} console.log("non-empty OK")'`
    - Existing Phase 28 keys preserved: `node -e 'const m=require("./messages/en.json").submit; for(const k of ["reviewTitle"]) if(!(k in m)) throw 0; for(const k of ["back","next","begin","submitButton","success","error"]) if(!(k in m)) throw new Error("dropped existing "+k); console.log("preserved OK")'`
    - Both JSON files parse: `node -e 'require("./messages/en.json");require("./messages/ar.json");console.log("parse OK")'`.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const en=require("./messages/en.json").submit,ar=require("./messages/ar.json").submit; const ek=Object.keys(en).sort(),ak=Object.keys(ar).sort(); if(JSON.stringify(ek)!==JSON.stringify(ak)){const only=(a,b)=>a.filter(k=>!b.includes(k)); throw new Error("submit key parity FAIL — EN-only:"+only(ek,ak)+" AR-only:"+only(ak,ek));} console.log("submit EN/AR parity PASS ("+ek.length+" keys)")'</automated>
  </verify>
  <done>All ~26 Phase-31 keys present in both messages files with identical submit key sets, no empty values, Phase 28 keys preserved, both files valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Add the standalone submit-namespace i18n parity gate</name>
  <read_first>
    - scripts/i18n-submit-parity-check.js (the file being created — confirm absent first)
    - scripts/screens-parity-check.js (the standalone-node-script precedent to mirror in style)
    - messages/en.json (the source of truth for the submit key set)
    - messages/ar.json (the file that must match it)
  </read_first>
  <action>
    Create `scripts/i18n-submit-parity-check.js` as a standalone Node CommonJS script (no new deps,
    no test framework), modeled on `scripts/screens-parity-check.js` style: `"use strict"`, a header
    comment explaining it gates EN↔AR key parity for the `submit` namespace (the milestone's
    `check:i18n` requirement, which has no npm-script equivalent in this repo). It `require`s
    `../messages/en.json` and `../messages/ar.json`, extracts `submit` from each, compares the sorted
    key arrays, and on any difference prints the EN-only and AR-only key lists and `process.exit(1)`;
    on full parity it prints `submit EN/AR parity PASS (<n> keys)` and `process.exit(0)`. Also assert
    no value in either `submit` object is an empty/whitespace-only string (exit 1 listing offenders).
    Keep it dependency-free and runnable as `node scripts/i18n-submit-parity-check.js`. Do NOT wire it
    into package.json (this repo has no test/lint pipeline beyond `next build`); it is an executable
    verification gate invoked directly, exactly like the existing parity scripts.
  </action>
  <acceptance_criteria>
    - File exists and is dependency-free: `grep -qE 'require\("\.\./messages/en\.json"\)' scripts/i18n-submit-parity-check.js`
      and `grep -vqE "require\(['\"](?!node:|\.\.?/)" scripts/i18n-submit-parity-check.js` (no third-party requires).
    - Exits 0 against the now-parity-correct messages: `node scripts/i18n-submit-parity-check.js` prints a line containing `PASS` and exit code is 0.
    - Detects a deliberately-broken parity: temporarily proven by the verify command below (adds then removes a stray AR key in a tmp copy), which must make the checker exit non-zero.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/i18n-submit-parity-check.js && node -e 'const {execSync}=require("child_process"); const fs=require("fs"); const ar=JSON.parse(fs.readFileSync("messages/ar.json","utf8")); const bak=JSON.stringify(ar); ar.submit.__stray_test_key__="x"; fs.writeFileSync("messages/ar.json",JSON.stringify(ar,null,2)); let failed=false; try{execSync("node scripts/i18n-submit-parity-check.js",{stdio:"ignore"});}catch(e){failed=true;} fs.writeFileSync("messages/ar.json",bak); if(!failed)throw new Error("checker did not flag injected stray key"); console.log("parity-gate negative-test PASS")'</automated>
  </verify>
  <done>scripts/i18n-submit-parity-check.js exists, exits 0 on the parity-correct submit namespace, and exits non-zero when a stray key is injected (negative test confirms it actually gates).</done>
</task>

<task type="auto">
  <name>Task 4: Append the terminal `review` step to the wizard registry</name>
  <read_first>
    - src/lib/wizard/registry.ts (the file being modified — STEPS array, StepDef shape, StepId derivation, nav/reachability helpers)
    - .planning/phases/29-choice-steps/29-01-PLAN.md (Task 2 — registers actor-class/entity-subtype/conduct/role-in-act; confirms NO review step)
    - .planning/phases/30-input-evidence-steps/30-01-PLAN.md (Task 2 — appends identity/describe/evidence/media/about-you; confirms about-you is the pre-31 terminal step, NO review step)
    - .planning/phases/31-review-submit-confirmation/31-CONTEXT.md (canonical StepId set — review is the Phase-31-added terminal step)
    - .planning/UI-SPEC.md (§3 Step 9 Review = terminal step; §6 review screen)
  </read_first>
  <action>
    Edit `src/lib/wizard/registry.ts`. After Phases 29 and 30 have run, `STEPS` ends with `about-you`
    and there is NO `review` step — verified against 29-01 Task 2 (actor-class/entity-subtype/conduct/
    role-in-act) and 30-01 Task 2 (identity/describe/evidence/media/about-you). Append ONE final
    `StepDef` to the END of the `STEPS` array literal (after the `about-you` entry), keeping the
    `as const satisfies ReadonlyArray<StepDef>` annotation so `StepId` widens to include `"review"`:
      `{ id: "review", archetype: "input", titleKey: "reviewStepTitle" }`
    Use archetype `"input"` (NOT `"choice"`) so the step does NOT auto-advance — the review screen is a
    terminal page the submitter dwells on, and Plan 31-03 renders `<ReviewStep>` for it instead of the
    Choice/Input scaffolds. Do NOT add a `requires` predicate (it is the terminal step — nothing follows
    it to gate, and the affirmation/≥2-source gate is enforced in ReviewStep/handleSubmit, not via
    registry reachability), do NOT add `branchWhen` (it is always shown and counted), do NOT add
    `completionGate`. Reference the `titleKey` "reviewStepTitle" added in Task 2. Leave ALL nav and
    reachability helpers (`nextStep`/`prevStep`/`isReachable`/`firstIncompleteStep`/`isCountedStep`/
    `visibleStepCount`/`visibleStepIndex`) byte-for-byte unchanged — they iterate STEPS generically, so
    appending one member needs no helper edit. Do NOT remove or reorder any Phase 29/30 step. Keep the
    `state.ts ↔ registry.ts` circular TYPE ref intact (`import type { WizardState }`). After this edit,
    `nextStep` from `about-you` returns `"review"`, `review` is the new terminal step, and
    `state.currentStep === "review"` is a valid, reachable branch.
  </action>
  <acceptance_criteria>
    - The `review` StepDef is present with the right id/archetype/titleKey: `grep -qE 'id:\s*"review"' src/lib/wizard/registry.ts`
      and the same logical entry references `archetype: "input"` and `titleKey: "reviewStepTitle"`
      (`grep -qE 'reviewStepTitle' src/lib/wizard/registry.ts`).
    - `review` is the LAST member of STEPS (appears AFTER `about-you` in source order):
      `node -e 'const s=require("fs").readFileSync("src/lib/wizard/registry.ts","utf8"); const a=s.indexOf("\"about-you\""); const r=s.lastIndexOf("\"review\""); if(a<0)throw new Error("about-you missing"); if(r<0)throw new Error("review missing"); if(r<a)throw new Error("review must come AFTER about-you"); console.log("order OK")'`
    - The pre-existing Phase 29/30 ids are all still present and unreordered (no regression):
      `grep -cE 'id:\s*"(actor-class|entity-subtype|conduct|role-in-act|identity|describe|evidence|media|about-you)"' src/lib/wizard/registry.ts` returns 9.
    - The `review` step carries NO requires/branchWhen/completionGate (terminal, always shown):
      the appended object literal for `review` must NOT contain `requires`/`branchWhen`/`completionGate`
      (manual confirm; tsc still passes either way — this guards the dwell/no-gate intent).
    - StepId now includes "review" and tsc is clean: `npx tsc --noEmit` exits 0 (the rejection-map.ts
      stepIds and any `"review"` literal downstream typecheck against the widened union).
    - Terminal-step behavior holds under type-stripping (review follows about-you, nothing follows review):
      `node --experimental-strip-types --input-type=module -e 'import {STEPS,nextStep} from "file://'"$PWD"'/src/lib/wizard/registry.ts"; const ids=STEPS.map(s=>s.id); if(ids[ids.length-1]!=="review")throw new Error("review not terminal"); console.log("terminal OK")'`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -qE 'id:\s*"review"' src/lib/wizard/registry.ts && node -e 'const s=require("fs").readFileSync("src/lib/wizard/registry.ts","utf8"); if(s.lastIndexOf("\"review\"")<s.indexOf("\"about-you\""))throw new Error("review must be appended after about-you"); console.log("PASS")'</automated>
  </verify>
  <done>registry.ts STEPS ends with a terminal `review` input-archetype step (titleKey reviewStepTitle, no requires/branchWhen/completionGate); StepId includes "review"; all 9 Phase 29/30 ids preserved and unreordered; nav helpers untouched; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server `/api/submit` response → client | The rejection `code` string is untrusted attacker-influenceable input that drives a client `goTo`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | rejection-map `resolveRejection(code)` | mitigate | Closed `Record<RejectionCode,...>` keyed only by the eight known codes; `resolveRejection` returns `null` for any unknown code so an injected/garbage `code` can never produce an attacker-chosen `goTo` target. |
| T-31-02 | Information disclosure | i18n message values | accept | Keys hold static bilingual UI copy only — no PII, no secrets, no submitter data. |
| T-31-11 | Tampering | appended `review` StepDef widening StepId / reachability | mitigate | The `review` step is appended via the same `as const satisfies ReadonlyArray<StepDef>` literal so tsc validates its shape; it carries no `requires`/`branchWhen`, so it cannot silently skip or hide a prior step or open an unreachable branch — `isReachable` still requires every prior step complete. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies in this plan (constraint). No install tasks → no package-legitimacy gate needed. |
</threat_model>

<verification>
- `node --experimental-strip-types` drives rejection-map.ts with the LOCKED routes (Task 1 verify) and proves `review` is the terminal step (Task 4 verify).
- `node -e` parity assertion proves EN↔AR submit key-set equality (Task 2 verify).
- `node scripts/i18n-submit-parity-check.js` passes AND a negative test proves it gates (Task 3 verify).
- `npx tsc --noEmit` must pass with the new module + widened StepId union (no type errors introduced).
</verification>

<success_criteria>
- `src/lib/wizard/registry.ts` STEPS ends with a terminal `review` input step; `StepId` includes `"review"`; all 9 Phase 29/30 ids preserved.
- `src/lib/wizard/rejection-map.ts` exists, is server-free, maps all 8 codes per LOCKED UI-SPEC §3 to real registry stepIds (evidence/describe/identity), and resolves unknown codes to null.
- All Phase-31 review/submit/confirmation keys (incl. `reviewStepTitle`) exist in EN and AR with identical submit key sets and no empty values.
- `scripts/i18n-submit-parity-check.js` is the executable parity gate (passes now; proven to fail on injected drift).
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/phases/31-review-submit-confirmation/31-01-SUMMARY.md` when done.
</output>
</content>
