---
phase: 31-review-submit-confirmation
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/components/wizard/ReviewStep.tsx
autonomous: true
requirements: [REV-01, REV-02]
must_haves:
  truths:
    - "The review screen shows every value that will be sent, grouped into six flow-order groups, with empty optionals rendered as '—' (never hidden)."
    - "Each group has a single Edit link routing to the first step of that group via ?step=<id>, using the REAL registry slug (actor-class/conduct/describe/evidence/media/about-you)."
    - "The source-type [TYPE: <slug>] token is stripped for display (shown as 'Type: …') while the raw title is left untouched for submission."
    - "The lead note renders in the Evidence group inside a distinct 'Reviewer note (not published)' sub-block."
    - "Submit is disabled until the affirmation checkbox is checked AND sourceLinks.length ≥ 2, with two independent inline .legal-error blocks each shown only when its own gate is unmet."
  artifacts:
    - path: "src/components/wizard/ReviewStep.tsx"
      provides: "Read-only six-group review summary + source list + affirmation gate (pure presentational; emits onEdit/onAffirmChange/onSubmit)"
      min_lines: 80
      contains: "review-group"
  key_links:
    - from: "src/components/wizard/ReviewStep.tsx"
      to: "src/lib/validation.ts SubmitInput"
      via: "props.form typed as SubmitInput & { leadNote?: string }"
      pattern: "SubmitInput"
    - from: "src/components/wizard/ReviewStep.tsx"
      to: "messages submit namespace"
      via: "useTranslations('submit') for review*/affirm/err* keys"
      pattern: "useTranslations\\(\"submit\"\\)"
---

<objective>
Build `ReviewStep` — the Step 9 read-only summary component. It renders six semantic groups in flow
order, each with a `.ds-eyebrow` label and ONE Edit link, shows empty optionals as "—", strips the
source-type token for display, surfaces the lead note in a distinct non-public sub-block, and owns
the affirmation checkbox plus the two independent inline disabled-reason gates.

Purpose: This is the presentational keystone of REV-01/REV-02. Built as a pure stateless component
(props-in, callbacks-out) it typecheck-gates independently of WizardClient and is the single piece
Plan 31-03 mounts and wires to routing/submit. No new CSS — every class (`.review-group`,
`.review-row`, `.review-sources`, `.review-affirm`, `.ds-eyebrow`, `.ds-mono`, `.filter-badge`,
`.legal-error`, `.btn ghost btn-sm`) already exists from Phase 28.

Output: `src/components/wizard/ReviewStep.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-review-submit-confirmation/31-CONTEXT.md
@.planning/UI-SPEC.md
@.planning/phases/31-review-submit-confirmation/31-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs; do NOT re-explore the codebase for these. -->

SubmitInput (from src/lib/validation.ts) — the form shape props.form carries:
  entityName: string; entityType: "individual"|"organization"|"military_unit"|"security_branch"|"official_body";
  entityRole: string; allegationDescription: string; allegationPeriod?: string; allegationLocation?: string;
  allegationClassification?: string;
  sourceLinks: { url: string; title?: string }[]; sourceFiles: {hash;filename;originalName;url;size}[];
  submitterEmail?: string; submitterName?: string; isAnonymous: boolean;
  leadNote?: string   (added by Phase 30 fork-point prerequisite — optional, may be undefined)

Existing chrome component patterns to mirror (props-in, callbacks-out, no router import):
  WizardProgress / WizardNav / WizardPanel are stateless leaves taking resolved (already-translated)
  strings and callbacks. ReviewStep follows the same convention.

Existing CSS classes (all in src/components/hlshajara.css / src/styles/tokens.css — DO NOT add CSS):
  .review-group, .review-group .head, .review-row, .review-row .k, .review-row .v,
  .review-sources (mono, LTR-forced under RTL), .review-affirm, .filter-badge,
  .ds-eyebrow (Latin-only uppercase reset under :lang(ar)), .ds-mono (LTR machine strings),
  .legal / .legal-error, .btn ghost / .btn-sm, .flex-between, .flex-between-start.

i18n keys available after Plan 31-01 (submit namespace), read via useTranslations("submit"):
  reviewTitle, reviewEdit, reviewGroupActor, reviewGroupConduct, reviewGroupDescription,
  reviewGroupEvidence, reviewGroupMedia, reviewGroupYou, reviewEmpty, reviewSourceTypeLabel,
  reviewLeadNoteLabel, reviewSourcesShort, affirm, errSourcesGate, errAffirmGate.
  Plus existing: idName, idRole, idCountry, descLabel, period, conduct_*, role_*, anonToggle, etc.
  (use existing field-label keys for row labels where present; reviewGroup* for group eyebrows).

CANONICAL registry slugs (AUTHORITATIVE — from 29-01 Task 2 + 30-01 Task 2 + 31-01 Task 4; do NOT use
the friendly group names as slugs). The six groups (flow order) → group eyebrow key → Edit target
?step= (FIRST step of group, the REAL registry id):
  Actor       reviewGroupActor       → step "actor-class"  (group spans steps 1/1b/2; FIRST registered = actor-class)
  Conduct     reviewGroupConduct     → step "conduct"      (steps 3/4; first = conduct)
  Description reviewGroupDescription → step "describe"     (step 5)
  Evidence    reviewGroupEvidence    → step "evidence"     (step 6: sources + lead note)
  Media       reviewGroupMedia       → step "media"        (step 7)
  You         reviewGroupYou         → step "about-you"    (step 8)
WARNING: the slugs are `actor-class` and `about-you`, NOT `actor`/`you` — those friendly names are NOT
registered step ids and a goTo to them would land on a non-existent step (broken edit-back, REV-01 fail).
ReviewStep's onEdit emits these REAL slug strings; the parent (Plan 03) calls goTo(slug).

Source-type token (Phase 30 §8 interim encoding): a source row title may be prefixed with
`[TYPE: <slug>] ` where slug ∈ un|court|sanctions|hr|journalism|official. For DISPLAY only, strip
the leading `[TYPE: <slug>] ` token and surface the slug as a "Type: …" label; the raw title string
is NEVER mutated (submission uses it verbatim). Idempotent strip; absent token = show title as-is.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the title-token strip + group-model helpers (pure)</name>
  <files>src/components/wizard/ReviewStep.tsx</files>
  <read_first>
    - src/components/wizard/ReviewStep.tsx (the file being created — confirm absent first)
    - src/lib/validation.ts (the SubmitInput shape the helpers operate over)
    - .planning/phases/31-review-submit-confirmation/31-CONTEXT.md (token-strip + empty-optional "—" rules + canonical slug set)
    - .planning/UI-SPEC.md (§6 review spec — empty optional = "—"; machine strings .ds-mono LTR)
  </read_first>
  <behavior>
    - stripSourceType("[TYPE: un] UN COI report") → { type: "un", title: "UN COI report" }
    - stripSourceType("Court filing") → { type: null, title: "Court filing" }  (no token)
    - stripSourceType("[TYPE: hr] ") → { type: "hr", title: "" }  (token only, empty remainder)
    - stripSourceType("") → { type: null, title: "" }
    - The original input string is never mutated; the returned title is the remainder after the token.
    - displayValue(undefined) and displayValue("") and displayValue("   ") → the em-dash "—" sentinel;
      displayValue("Damascus") → "Damascus" unchanged.
  </behavior>
  <action>
    In the new `"use client"` `src/components/wizard/ReviewStep.tsx`, define two exported pure helper
    functions ABOVE the component (so they are unit-testable via type-stripping without rendering JSX):
    `stripSourceType(rawTitle: string): { type: string | null; title: string }` that matches a leading
    `[TYPE: <slug>] ` prefix (slug captured from `un|court|sanctions|hr|journalism|official`), returns
    the captured slug and the trimmed remainder when present, else `{ type: null, title: rawTitle }`;
    and `displayValue(v: string | undefined | null): string` returning a single em-dash `"—"` when `v`
    is nullish or trims to empty, else `v`. Neither helper mutates its input. Add a header comment that
    `stripSourceType` is DISPLAY-only and the raw title is submitted verbatim by Plan 03. Do not import
    React state — these are pure functions; the component body comes in Task 2.
  </action>
  <acceptance_criteria>
    - Both helpers exported: `grep -qE 'export function stripSourceType' src/components/wizard/ReviewStep.tsx`
      and `grep -qE 'export function displayValue' src/components/wizard/ReviewStep.tsx`.
    - The six valid type slugs appear in the strip regex: `grep -qE 'un\|court\|sanctions\|hr\|journalism\|official' src/components/wizard/ReviewStep.tsx`.
    - Helpers run pure under type-stripping with the behavior fixtures (the JSX body is not reached by a
      named-import driver):
      `node --experimental-strip-types --input-type=module -e 'import {stripSourceType,displayValue} from "file://'"$PWD"'/src/components/wizard/ReviewStep.tsx"; const a=stripSourceType("[TYPE: un] UN COI report"); if(a.type!=="un"||a.title!=="UN COI report")throw new Error("strip un"); const b=stripSourceType("Court filing"); if(b.type!==null||b.title!=="Court filing")throw new Error("no-token"); if(displayValue("")!=="—"||displayValue("  ")!=="—"||displayValue(undefined)!=="—")throw new Error("empty sentinel"); if(displayValue("Damascus")!=="Damascus")throw new Error("value passthrough"); console.log("OK")'`
      prints `OK`. (If the driver fails because the file imports React/JSX at module top, move the
      helpers to a non-JSX-evaluating position or a sibling pure import — but they MUST remain in this
      file's exports.)
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --input-type=module -e 'import {stripSourceType,displayValue} from "file://'"$PWD"'/src/components/wizard/ReviewStep.tsx"; const a=stripSourceType("[TYPE: hr] "); if(a.type!=="hr"||a.title!=="")throw new Error("token-only"); if(displayValue("Damascus")!=="Damascus")throw new Error("passthrough"); console.log("PASS")'</automated>
  </verify>
  <done>stripSourceType + displayValue exported, pure, mutation-free, pass all behavior fixtures under --experimental-strip-types.</done>
</task>

<task type="auto">
  <name>Task 2: Render the six-group summary + source list + affirmation gate</name>
  <read_first>
    - src/components/wizard/ReviewStep.tsx (the file being modified — the Task-1 helpers)
    - src/components/wizard/WizardNav.tsx (stateless-leaf props-in/callbacks-out convention to mirror)
    - src/lib/validation.ts (SubmitInput fields rendered per group)
    - src/components/hlshajara.css (the .review-* / .legal-error / .filter-badge classes — reuse, add none)
    - .planning/UI-SPEC.md (§3 Step 9 + §6 review spec)
    - .planning/phases/31-review-submit-confirmation/31-CONTEXT.md (six groups, REAL edit slugs actor-class/about-you, lead-note sub-block, two gates, submit-disabled rule)
  </read_first>
  <action>
    Complete `ReviewStep` as a stateless `"use client"` component (default + named export, mirroring the
    Phase 28 chrome). Props: `form: SubmitInput & { leadNote?: string }`, `affirmed: boolean`,
    `submitting: boolean`, `onEdit: (stepId: string) => void`, `onAffirmChange: (checked: boolean) => void`,
    `onSubmit: () => void`. Read labels via `useTranslations("submit")`. Render an `<h-less>` set of six
    `.review-group` blocks IN FLOW ORDER — Actor, Conduct, Description, Evidence, Media, You — each with a
    `.head` containing a `.ds-eyebrow` group label (`reviewGroupActor`…`reviewGroupYou`) and ONE Edit
    control (`<button className="btn ghost btn-sm">{t("reviewEdit")}</button>`). Each Edit control calls
    `onEdit(<REAL registry slug>)` where the slug is the FIRST registered step of that group using the
    CANONICAL ids — `onEdit("actor-class")` for Actor, `onEdit("conduct")` for Conduct,
    `onEdit("describe")` for Description, `onEdit("evidence")` for Evidence, `onEdit("media")` for Media,
    and `onEdit("about-you")` for You. Do NOT use the friendly names `actor`/`you` — those are not
    registered step ids and would route goTo to a non-existent step (broken edit-back). Inside each group
    render `.review-row`s as `.k`/`.v` pairs for the group's fields, passing every optional field through
    `displayValue` so empties show "—" (Actor: name, role, type, location; Conduct:
    allegationClassification + role-in-act; Description: allegationDescription + period; Evidence: the
    source list + lead-note sub-block; Media: source file names or "—"; You: anonymity state, and when not
    anonymous the submitterName/submitterEmail). For the Evidence group render the sources in a
    `.review-sources` block: for each `sourceLinks` row, call `stripSourceType(row.title ?? "")`, show the
    `url` in `.ds-mono`, and when a type slug is present show a `reviewSourceTypeLabel` + slug "Type: …"
    line; show the source count via a `.filter-badge` using `reviewSourcesShort`. Below the sources,
    render the lead note (if `form.leadNote` non-empty) in a DISTINCT sub-block whose heading is
    `t("reviewLeadNoteLabel")` ("Reviewer note (not published)"); if empty, render `displayValue` "—" so
    the submitter still sees it is absent. Then render the `.review-affirm` block: a checkbox bound to
    `affirmed`/`onAffirmChange` labelled `t("affirm")`, and the Submit button
    (`<Button variant="primary">`), disabled when `submitting || !affirmed || form.sourceLinks.length < 2`.
    Render TWO INDEPENDENT inline `.legal-error` blocks: one shown only when `form.sourceLinks.length < 2`
    (text `t("errSourcesGate")`), one shown only when `!affirmed` (text `t("errAffirmGate")`) — each gated
    on its OWN condition so the user can resolve either independently. No `dangerouslySetInnerHTML`; all
    submitter values bind as React text (auto-escaped). Use logical classes only — no left/right, no inline
    color, no new CSS.
  </action>
  <acceptance_criteria>
    - Exactly six review groups rendered: `grep -c 'className="review-group"' src/components/wizard/ReviewStep.tsx` returns 6
      (or a single mapped block over a 6-entry group array — in that case grep the array length: a `GROUPS` array literal with 6 entries, asserted by `node --experimental-strip-types` reading an exported `REVIEW_GROUP_IDS` constant of length 6).
    - All six group eyebrow keys referenced: `grep -c -E 'reviewGroupActor|reviewGroupConduct|reviewGroupDescription|reviewGroupEvidence|reviewGroupMedia|reviewGroupYou' src/components/wizard/ReviewStep.tsx` ≥ 6.
    - Edit links route to the six first-of-group REAL registry slugs (actor-class/conduct/describe/evidence/media/about-you — NOT actor/you): `grep -cE 'onEdit\("(actor-class|conduct|describe|evidence|media|about-you)"\)' src/components/wizard/ReviewStep.tsx` returns 6, AND the friendly bare names are absent as edit targets: `grep -vqE 'onEdit\("(actor|you)"\)' src/components/wizard/ReviewStep.tsx`.
    - Token strip is applied to displayed source titles, not the raw form: `grep -q 'stripSourceType' src/components/wizard/ReviewStep.tsx` and the component must NOT reassign `row.title` (no `\.title =` assignment): `grep -vqE '\.title\s*=' src/components/wizard/ReviewStep.tsx`.
    - Lead-note sub-block present and labelled: `grep -q 'reviewLeadNoteLabel' src/components/wizard/ReviewStep.tsx` and `grep -q 'leadNote' src/components/wizard/ReviewStep.tsx`.
    - Submit disabled condition includes all three gates: `grep -qE 'submitting' src/components/wizard/ReviewStep.tsx` and `grep -qE 'sourceLinks\.length\s*<\s*2' src/components/wizard/ReviewStep.tsx` and `grep -qE '!\s*affirmed|affirmed\s*===\s*false|!affirmed' src/components/wizard/ReviewStep.tsx`.
    - Two independent inline error blocks, both using .legal-error and each its own gate: `grep -c 'legal-error' src/components/wizard/ReviewStep.tsx` ≥ 2; `grep -q 'errSourcesGate' src/components/wizard/ReviewStep.tsx`; `grep -q 'errAffirmGate' src/components/wizard/ReviewStep.tsx`.
    - displayValue used for empty-optional rendering: `grep -q 'displayValue' src/components/wizard/ReviewStep.tsx`.
    - No raw HTML injection / no inline color / no physical-direction CSS: `grep -vqE 'dangerouslySetInnerHTML' src/components/wizard/ReviewStep.tsx`; `grep -vqiE 'style=\{\{[^}]*color' src/components/wizard/ReviewStep.tsx`; `grep -vqE '\b(left|right):' src/components/wizard/ReviewStep.tsx`.
    - `npx tsc --noEmit` exits 0 with the new component.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ReviewStep renders six flow-order groups with one Edit link each routing to the first-of-group REAL registry slug (actor-class/conduct/describe/evidence/media/about-you), empties as "—" via displayValue, source titles displayed via stripSourceType (raw untouched), lead note in a distinct non-public sub-block, affirmation checkbox + Submit disabled until affirmed && sources≥2, two independent inline .legal-error gates; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submitter-entered form values → rendered review DOM | Free-text fields (name, role, description, titles, lead note) are untrusted and rendered back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering (XSS) | ReviewStep value rendering | mitigate | All submitter values bind as React text children (auto-escaped); no `dangerouslySetInnerHTML` — grep-absence asserted. |
| T-31-05 | Information disclosure | lead-note sub-block | mitigate | Lead note is rendered under an explicit "Reviewer note (not published)" label so the submitter is shown it is non-public; it is display-only here (persistence is Phase 33). |
| T-31-06 | Tampering | source-type token strip | mitigate | `stripSourceType` is display-only and never mutates `row.title`; the raw `[TYPE: <slug>]`-prefixed title is left intact for verbatim submission — grep-absence of `.title =` assignment asserted. |
| T-31-12 | Tampering | edit-back routing to a non-existent step | mitigate | onEdit emits only the REAL registry slugs (actor-class/conduct/describe/evidence/media/about-you), asserted by grep returning exactly 6 and the bare actor/you names absent; goTo therefore always targets a registered, reachable step. |
| T-31-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies (constraint). No install tasks → no package-legitimacy gate needed. |
</threat_model>

<verification>
- `node --experimental-strip-types` drives the pure helpers (stripSourceType/displayValue) against the behavior fixtures.
- `npx tsc --noEmit` typechecks the full component against SubmitInput + the submit i18n keys.
- grep gates confirm six groups, six first-of-group REAL registry Edit slugs (and absence of bare actor/you), both independent error blocks, no raw-HTML/inline-color/physical-direction.
- JSX cannot run under `--experimental-strip-types`; component wiring is verified via tsc + grep + (Plan 03) `next build`.
</verification>

<success_criteria>
- ReviewStep is a stateless component rendering six flow-order groups, each with a `.ds-eyebrow` label and one first-of-group Edit link routing to the REAL registry slug (actor-class/conduct/describe/evidence/media/about-you — never actor/you).
- Empty optionals render as "—" via displayValue; source-type token stripped for display while raw title preserved.
- Lead note shown in a distinct "Reviewer note (not published)" sub-block.
- Submit disabled until affirmation checked AND sourceLinks.length ≥ 2; two independent inline `.legal-error` gates each shown only when its own condition is unmet.
- `npx tsc --noEmit` passes; no new CSS, no raw HTML, no physical-direction props.
</success_criteria>

<output>
Create `.planning/phases/31-review-submit-confirmation/31-02-SUMMARY.md` when done.
</output>
</content>
