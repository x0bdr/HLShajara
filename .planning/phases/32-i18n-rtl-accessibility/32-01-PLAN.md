---
phase: 32-i18n-rtl-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - messages/ar.json
  - scripts/i18n-parity-check.js
  - package.json
autonomous: true
requirements: [INTL-01]
must_haves:
  truths:
    - "Every submit.* key present in messages/en.json is present in messages/ar.json and vice versa (full parity)"
    - "npm run check:i18n exits 0"
    - "The full UI-SPEC §3 submit key set (~70 keys) exists in both locales"
    - "No machine/interim token (e.g. '[TYPE:', '— role in act:') appears as a translation VALUE in either locale"
  artifacts:
    - path: "scripts/i18n-parity-check.js"
      provides: "Standalone EN/AR key-parity + placeholder + empty-value checker (no test framework)"
      contains: "messages/en.json"
    - path: "messages/en.json"
      provides: "Full submit namespace EN strings at parity"
      contains: "\"submit\""
    - path: "messages/ar.json"
      provides: "Full submit namespace AR strings at parity"
      contains: "\"submit\""
    - path: "package.json"
      provides: "check:i18n npm script"
      contains: "check:i18n"
  key_links:
    - from: "package.json"
      to: "scripts/i18n-parity-check.js"
      via: "npm run check:i18n script entry"
      pattern: "check:i18n.*i18n-parity-check"
    - from: "scripts/i18n-parity-check.js"
      to: "messages/en.json + messages/ar.json"
      via: "require/readFileSync + recursive key diff"
      pattern: "messages/(en|ar)\\.json"
---

<objective>
Reconcile the entire `submit` i18n namespace to FULL EN/AR key parity (the union of every key Phases 28–31 introduced plus any UI-SPEC §3 key still missing, ~70 total) and stand up the `check:i18n` gate the milestone requires. No machine/interim token is ever a user-visible untranslated value.

Purpose: INTL-01 — every wizard string exists in EN and AR under `submit` with full key parity; the gate (`npm run check:i18n`) passes and the `i18n-checker` agent is clean. This is the i18n half of the Phase 32 cross-cutting hardening pass.

Output: a standalone `scripts/i18n-parity-check.js` parity checker, a `check:i18n` npm script wired to it, and reconciled `messages/{en,ar}.json` `submit` namespaces at full parity with no interim tokens as values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/32-i18n-rtl-accessibility/32-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Testing precedent (Phase 28): standalone Node checkers, NO test framework, NO new deps. -->
<!-- scripts/screens-parity-check.js and scripts/wizard-reducer-check.js are the model:
     a "use strict" CommonJS script under scripts/, run via `node scripts/<name>.js`,
     exit(1) on failure / exit(0) on pass, printing a human-readable diff. -->

package.json scripts block currently:
  "dev": "next dev", "build": "next build", "build:static": "node scripts/build-static.js",
  "start": "next start", "lint": "eslint"
  (NO check:i18n exists yet — this plan creates it.)

messages/en.json + messages/ar.json shape:
  top-level namespaces: metadata, nav, home, mission, faq, terms, privacy, record,
  submit, reply, reviewer, entity, publications, legal, footer, labels, login, dashboard, policy
  `submit` is a flat camelCase key→string object. Current count: 41 keys, already at parity.
  ICU placeholders use `{name}` curly-brace form (e.g. submit.stepCounter uses {n} and {m}).

UI-SPEC §3 enumerates the FULL submit key set per step (the authoritative target list):
  introTitle, introRule1, introRule2, introRule3, introGate, begin,
  q_actorClass, actorIndividual, actorIndividualHint, actorEntity, actorEntityHint,
  q_entitySubtype (reuse typeOrganization/typeMilitaryUnit/typeSecurityBranch/typeOfficialBody),
  idName, idRole, idRoleHint, idCountry, idArea, idAreaHint, idPublicRef,
  q_conduct, conduct_detention, conduct_torture, conduct_disappearance, conduct_killing,
  conduct_sexualViolence, conduct_financing, conduct_arms, conduct_laundering, conduct_propaganda,
  conduct_informing, conduct_seizure, conduct_detentionSite, conduct_command, conduct_other
  (+ a `_def` suffix definition key for each conduct_*),
  q_roleInAct, role_perpetrator, role_commander, role_financier, role_supplier, role_informant,
  role_owner, role_other,
  descLabel, descHint, descCounter, period,
  sourcesTitle, sourcesRule, sourceUrl, sourceType, sourceType_un, sourceType_court,
  sourceType_sanctions, sourceType_hr, sourceType_journalism, sourceType_official,
  sourceTitleField, addSource, leadNote, leadNoteHint,
  mediaTitle, mediaSafety, mediaLink, skip,
  anonTitle, anonToggle, anonHelp,
  reviewTitle, reviewEdit, affirm, reviewSourcesShort,
  successTitle, successBody, submitAnother,
  err_sources, err_private, err_group, err_innocent, err_tone, err_mismatch
  (§3 also reuses existing keys: typeOrganization/MilitaryUnit/SecurityBranch/OfficialBody,
   fullName, email, submitButton, back, next, begin, stepCounter, period).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the EN/AR parity checker + check:i18n npm script</name>
  <read_first>
    - scripts/screens-parity-check.js (the standalone-checker precedent: "use strict" CommonJS, spawnSync/require, exit codes, human-readable output — mirror its structure and header-comment style)
    - scripts/wizard-reducer-check.js (second checker precedent — confirms the no-test-framework, exit(1)-on-fail convention)
    - messages/en.json (the EN message tree to diff)
    - messages/ar.json (the AR message tree to diff)
    - package.json (the scripts block to extend)
    - ./CLAUDE.md (i18n rule: full EN+AR key parity always; check:i18n gate is mandatory before UI work is done)
  </read_first>
  <action>
    Create scripts/i18n-parity-check.js as a "use strict" CommonJS Node script (NO new dependency, NO test framework — mirror scripts/screens-parity-check.js). It require()s ./messages/en.json and ./messages/ar.json, recursively flattens both objects to dot-path key sets (handle nested namespaces generically, not just `submit`), and reports: (a) keys present in EN but missing in AR, (b) keys present in AR but missing in EN, (c) ICU placeholder mismatches — extract `{token}` occurrences from each value and require the EN and AR sets to match per key, (d) empty-string or whitespace-only values, (e) AR values byte-identical to their EN counterpart for any key under the `submit` namespace (stub/untranslated detector — warn, do not fail, since some keys like proper nouns may legitimately match). Print a grouped, human-readable report; process.exit(1) if any of (a)–(d) found, exit(0) otherwise. Add a `"check:i18n": "node scripts/i18n-parity-check.js"` entry to the package.json scripts block (alphabetical-adjacent to existing entries; do not reorder unrelated keys). Do NOT use a code fence in implementation discussions — write the file directly.
  </action>
  <verify>
    <automated>node scripts/i18n-parity-check.js; echo "exit=$?"</automated>
    <automated>node -e "const s=require('./package.json').scripts; process.exit(s['check:i18n']&&s['check:i18n'].includes('i18n-parity-check')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/i18n-parity-check.js exists and is "use strict" CommonJS with no `require(` of any package outside node: builtins / the two message JSONs (grep: `grep -E "require\(['\"](?!node:|\./messages|node:)" scripts/i18n-parity-check.js` returns no third-party package require).
    - `node -e "const s=require('./package.json').scripts; process.exit(s['check:i18n']?0:1)"` exits 0 (script entry present).
    - The script exit code is 0 when run against the current parity-clean messages, and the script flattens nested objects (grep the file for a recursive flatten helper, e.g. `flatten` or a recursion over Object.entries — pattern `Object.entries` present).
    - `npm run check:i18n` runs the new script (its package.json value contains the substring `i18n-parity-check`).
  </acceptance_criteria>
  <done>scripts/i18n-parity-check.js exists, has no third-party require, exits 0 against current messages; package.json has a check:i18n script invoking it; npm run check:i18n runs clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the full submit namespace to ~70-key EN/AR parity with no interim tokens</name>
  <read_first>
    - messages/en.json (the `submit` namespace to extend to the full §3 set)
    - messages/ar.json (the `submit` namespace to extend with matching AR strings)
    - .planning/UI-SPEC.md §3 + §10 (the authoritative key list per step and the representative EN‖AR phrasings for the new keys)
    - .planning/phases/32-i18n-rtl-accessibility/32-CONTEXT.md (Claude's-discretion note: terse legal-register, non-sectarian AR phrasing for newly-discovered keys; no machine/interim tokens user-visible)
    - scripts/i18n-parity-check.js (the gate this task must satisfy — Task 1 output)
  </read_first>
  <action>
    Compute the union of (a) the `submit` keys currently present after Phases 28–31 and (b) the full UI-SPEC §3 key list (enumerated in this plan's `<interfaces>` block). For every key in that union missing from the `submit` namespace in either locale, add it to BOTH messages/en.json and messages/ar.json with full parity. EN copy comes from UI-SPEC §3/§10 verbatim where the spec gives it; otherwise author terse, legal-register, non-sectarian EN. AR copy comes from UI-SPEC §10 verbatim where given (e.g. introTitle "تقديم بلاغ موثّق"); otherwise author terse legal-register AR (Claude's discretion per CONTEXT). Reuse — do NOT duplicate — existing keys the spec reuses (typeOrganization/typeMilitaryUnit/typeSecurityBranch/typeOfficialBody, fullName, email, submitButton, back, next, begin, stepCounter, period). Add a `_def` definition key for each `conduct_*` per §3. Preserve `{n}`/`{m}` ICU placeholders in stepCounter and any counter keys identically across locales. CRITICAL: no value in either locale may contain a machine/interim token meant as encoded data — `[TYPE:`, `— role in act:`, `[CONDUCT:`, or similar bracketed/dashed machine markers are forbidden as translation VALUES (those are display-stripped at render time, never stored in messages). Keep keys flat camelCase under `submit`; do not introduce new top-level namespaces.
  </action>
  <verify>
    <automated>npm run check:i18n; echo "exit=$?"</automated>
    <automated>node -e "const e=require('./messages/en.json').submit,a=require('./messages/ar.json').submit; const ek=Object.keys(e).sort(),ak=Object.keys(a).sort(); process.exit(JSON.stringify(ek)===JSON.stringify(ak)&&ek.length>=70?0:1)"</automated>
    <automated>node -e "const e=require('./messages/en.json').submit,a=require('./messages/ar.json').submit; const bad=[...Object.values(e),...Object.values(a)].filter(v=>/\[(TYPE|CONDUCT|ROLE|SOURCE)\s*:|—\s*role in act:/.test(v)); process.exit(bad.length===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:i18n` exits 0 (zero missing keys, zero placeholder mismatches, zero empty values).
    - The `submit` namespace key sets in en.json and ar.json are identical (sorted-equal) AND total ≥ 70 keys (node assertion above exits 0).
    - No translation value in either locale matches the forbidden-machine-token regex `/\[(TYPE|CONDUCT|ROLE|SOURCE)\s*:|—\s*role in act:/` (node assertion above exits 0).
    - Every `conduct_*` key has a matching `conduct_*_def` key in both locales (grep: count of `conduct_.*_def` keys in en.json equals the count in ar.json and is ≥ 14).
    - The four reused entity-type keys (typeOrganization, typeMilitaryUnit, typeSecurityBranch, typeOfficialBody) are NOT duplicated under new key names (grep en.json submit for exactly one occurrence of each literal key name).
    - `next build` completes without an i18n/missing-message error.
  </acceptance_criteria>
  <done>The full ~70-key submit namespace exists at EN/AR parity; check:i18n exits 0; no machine/interim token is a value; conduct_* have _def pairs; reused keys are not duplicated; next build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full submit namespace reconciled to EN/AR parity (~70 keys) plus the check:i18n gate. Run the i18n-checker agent (per CONTEXT, it must be clean) as the independent second audit beyond the parity script.</what-built>
  <how-to-verify>
    1. Run `npm run check:i18n` — confirm it prints a clean report and exits 0.
    2. Invoke the `i18n-checker` agent against messages/en.json + messages/ar.json (the CONTEXT mandates the agent be clean, not only the script). Confirm verdict is PARITY OK with no stub/empty AR values and no placeholder mismatches.
    3. Spot-check 5 AR values are genuine Arabic (not English copied over): introTitle, q_actorClass, sourcesRule, affirm, err_private should read as the §10 Arabic phrasings.
  </how-to-verify>
  <resume-signal>Type "approved" once check:i18n exits 0 AND the i18n-checker agent verdict is PARITY OK; otherwise describe the findings to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npm run check:i18n` exits 0 (the gate now exists and passes).
- `submit` namespace is sorted-key-identical across en.json/ar.json with ≥70 keys.
- No machine/interim token appears as a translation value.
- The `i18n-checker` agent verdict is PARITY OK (CONTEXT requirement).
- `next build` is clean.
</verification>

<success_criteria>
INTL-01 satisfied: every wizard string exists in EN and AR under `submit` with full key parity; `npm run check:i18n` passes; the `i18n-checker` agent is clean; no interim/machine token is user-visible untranslated.
</success_criteria>

<output>
Create `.planning/phases/32-i18n-rtl-accessibility/32-01-SUMMARY.md` when done.
</output>
