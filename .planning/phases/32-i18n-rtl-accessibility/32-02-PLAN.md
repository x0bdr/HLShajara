---
phase: 32-i18n-rtl-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hlshajara.css
  - src/styles/tokens.css
  - scripts/wizard-rtl-check.js
autonomous: true
requirements: [INTL-02]
must_haves:
  truths:
    - "The wizard CSS introduced in Phases 28–31 uses logical properties only — no left/right/margin-left/padding-right physical props"
    - ".ds-mono (and the review-sources machine-string list) renders LTR + right-aligned under [dir=rtl]"
    - "Latin .ds-eyebrow / .wizard-eyebrow has no uppercase or letter-spacing in Arabic (RTL/lang reset present)"
    - "Progress pills and the Back/Next chevrons mirror correctly in RTL (logical direction, not hardcoded)"
    - "Every visible step/source count renders Arabic-Indic digits via Intl.NumberFormat(useLocale()) in AR"
  artifacts:
    - path: "scripts/wizard-rtl-check.js"
      provides: "Grep-gate audit asserting physical-prop absence + RTL-reset presence + Intl.NumberFormat usage on counters"
      contains: "hlshajara.css"
    - path: "src/components/hlshajara.css"
      provides: "RTL-correct wizard CSS (logical props, [dir=rtl] resets, mirrored chevrons)"
      contains: "WIZARD / STEPPER"
    - path: "src/styles/tokens.css"
      provides: ".ds-mono LTR-under-RTL rule"
      contains: "ds-mono"
  key_links:
    - from: "scripts/wizard-rtl-check.js"
      to: "src/components/hlshajara.css + the wizard *.tsx counters"
      via: "grep-absence of physical props + grep-presence of Intl.NumberFormat"
      pattern: "Intl\\.NumberFormat"
    - from: "src/components/hlshajara.css [dir=rtl] rules"
      to: ".ds-mono / .review-sources machine strings"
      via: "direction:ltr; text-align:right"
      pattern: "direction:\\s*ltr"
---

<objective>
Audit and correct the RTL behavior of the wizard CSS and counters built in Phases 28–31 so the entire flow is correct in Arabic: logical properties only, no Arabic uppercase/letter-spacing, LTR-forced machine strings, mirrored progress pills + Back/Next chevrons, and Arabic-Indic step/source counters everywhere a count appears.

Purpose: INTL-02 — in Arabic the wizard is RTL-correct: progress and chevrons mirror, no Arabic uppercase/letter-spacing, machine strings stay LTR, and counters show Arabic-Indic digits. This is the RTL half of the Phase 32 cross-cutting hardening pass.

Output: a standalone `scripts/wizard-rtl-check.js` grep-gate, RTL fixes in `hlshajara.css`, and a `.ds-mono` LTR-under-RTL rule in `tokens.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/32-i18n-rtl-accessibility/32-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Existing wizard CSS block lives at src/components/hlshajara.css lines ~1365–1614,
     headed `/* ===== WIZARD / STEPPER ===== */`. It already uses logical props in most
     rules and has SOME [dir=rtl] resets. This plan AUDITS the whole block + the steps
     authored in 28–31 for any residual physical prop, and adds the missing resets. -->

Established RTL precedents in this codebase (mirror these patterns, do not invent new ones):
  - src/styles/tokens.css:
      .ds-eyebrow:not(:lang(ar)) { text-transform:uppercase; letter-spacing:var(--tracking-caps); }
      .ds-eyebrow:lang(ar) { /* no uppercase, no letter-spacing */ }
      .ds-mono { font-family:var(--font-mono); letter-spacing:0; }   ← has NO LTR-under-RTL rule yet
  - src/components/hlshajara.css:
      [dir=rtl] .legal .t { letter-spacing:0; text-transform:none; }      (Latin-eyebrow reset precedent)
      [dir=rtl] .card .id { direction:ltr; text-align:right; }            (machine-string LTR precedent)
      [dir=rtl] .review-sources { direction:ltr; text-align:right; }      (already present in WIZARD block)
      [dir=rtl] .wizard-eyebrow { letter-spacing:0; text-transform:none; }
      [dir=rtl] .wizard-step-pill { letter-spacing:0; text-transform:none; }
      .wizard-nav .back::before { margin-inline-end:6px; }   (chevron via logical margin — mirrors)
      .wizard-nav .next { margin-inline-start:auto; }
      .wizard-panel.entering-fwd { transform:translateX(12px); }
      [dir=rtl] .wizard-panel.entering-fwd { transform:translateX(-12px); }  (slide mirror precedent)

Counter components (verify they all use the Intl pattern — DO NOT duplicate the formatter, just verify):
  - src/components/wizard/WizardProgress.tsx — fmt = new Intl.NumberFormat(locale, locale==="ar"?{numberingSystem:"arab"}:undefined)
  - src/components/wizard/WizardPanel.tsx — same fmt for the aria-live announcement
  - Any new counter Phases 30/31 add (description char counter, source counter) MUST use the same
    Intl.NumberFormat(useLocale(), arab) pattern; if one renders digits via plain string interpolation,
    that is the RTL defect this plan flags (fix is component-local; if found, note it for plan 32-03's
    component pass rather than editing the component here to avoid file overlap — record in SUMMARY).

next build runs Turbopack; JSX cannot run under --experimental-strip-types, so RTL correctness is
verified by grep-gates over the CSS/markup + a clean `next build`, plus the staging browser pass (CONTEXT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + fix wizard CSS for logical properties, ds-mono LTR, eyebrow resets, mirrored chevrons/slide</name>
  <read_first>
    - src/components/hlshajara.css (the full `/* ===== WIZARD / STEPPER ===== */` block ~lines 1365–1614, plus the .legal/.card.id RTL precedents earlier in the file)
    - src/styles/tokens.css (.ds-mono and .ds-eyebrow definitions — add the .ds-mono LTR-under-RTL rule here)
    - .planning/UI-SPEC.md §7 + §10 + §6 (RTL spec: logical props only; no Arabic uppercase/letter-spacing; machine strings forced LTR via [dir=rtl] .ds-mono{direction:ltr;text-align:right}; chevrons mirror)
    - .planning/phases/32-i18n-rtl-accessibility/32-CONTEXT.md (RTL decisions: Latin eyebrow [dir=rtl] reset; .ds-mono stays LTR under RTL; pills + chevrons mirror; small [dir=rtl] resets vs adjusting existing rules is Claude's discretion)
  </read_first>
  <action>
    Grep the wizard CSS block and any wizard-step CSS added in 28–31 for physical properties: `left:`, `right:`, `margin-left`, `margin-right`, `padding-left`, `padding-right`, `border-left`, `border-right`, `text-align:left`, `text-align:right` (excluding the sanctioned `[dir=rtl] … {text-align:right}` machine-string LTR rules). Convert every offending wizard-scoped declaration to its logical equivalent (`inset-inline-start/end`, `margin-inline-start/end`, `padding-inline-start/end`, `border-inline-start/end`, `text-align:start/end`). In src/styles/tokens.css, add a rule `[dir=rtl] .ds-mono { direction:ltr; text-align:right; }` so every machine string (reference id, URL, hash) stays LTR inside RTL (UI-SPEC §10) — modeled on the existing `[dir=rtl] .card .id` and `[dir=rtl] .review-sources` rules. Verify (and add if missing) the Latin-eyebrow RTL reset for any wizard eyebrow that uses uppercase/letter-spacing (`.wizard-eyebrow` already has `[dir=rtl] {letter-spacing:0;text-transform:none}` — confirm it covers every wizard eyebrow/caps-label introduced in 28–31; add the same reset for any new one). Confirm the Back/Next chevron uses a logical margin (`.wizard-nav .back::before { margin-inline-end }`) and that `.wizard-panel.entering-fwd/-back` slides have their `[dir=rtl]` translateX sign flip. Do NOT introduce new color tokens or Tailwind utilities (UI-SPEC §12); reuse existing tokens only.
  </action>
  <verify>
    <automated>grep -nE "(margin|padding|border)-(left|right)|(^|[^-])(left|right):|text-align:\s*(left|right)" src/components/hlshajara.css | grep -vE "\[dir=rtl\]|direction:\s*ltr" | sed -n '1,40p'</automated>
    <automated>grep -c "direction:ltr\|direction: ltr" src/styles/tokens.css</automated>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - In the WIZARD/STEPPER block of src/components/hlshajara.css, the physical-property grep (`grep -nE "(margin|padding|border)-(left|right)" src/components/hlshajara.css` over lines of the wizard block) returns ZERO non-`[dir=rtl]` matches for wizard-scoped selectors (`.wizard-*`, `.choice-*`, `.review-*`).
    - src/styles/tokens.css contains `[dir=rtl] .ds-mono` with `direction:ltr` and `text-align:right` (grep: `grep -A1 "\[dir=rtl\].*ds-mono" src/styles/tokens.css` shows `direction:ltr`).
    - Every wizard-scoped eyebrow/caps label with `text-transform:uppercase` has a matching `[dir=rtl]` (or `:lang(ar)`) reset zeroing `letter-spacing` and `text-transform` (grep the wizard block: each uppercase wizard selector has a paired reset).
    - `.wizard-nav .back::before` uses `margin-inline-end` (not margin-left/right); `.wizard-panel.entering-fwd` and `.entering-back` each have an `[dir=rtl]` translateX sign-flipped counterpart (grep present).
    - `npm run build` completes successfully (exit 0).
  </acceptance_criteria>
  <done>Wizard CSS uses logical props only (no wizard-scoped physical props), .ds-mono is LTR under RTL, Latin eyebrows have RTL resets, chevron + slide mirror; next build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the RTL grep-gate (physical-prop absence + RTL-reset presence + Intl.NumberFormat counters)</name>
  <read_first>
    - scripts/screens-parity-check.js (standalone-checker convention: "use strict" CommonJS, exit codes, human-readable output)
    - src/components/hlshajara.css (the wizard block this gate scans)
    - src/styles/tokens.css (.ds-mono LTR rule the gate asserts)
    - src/components/wizard/WizardProgress.tsx + src/components/wizard/WizardPanel.tsx (the Intl.NumberFormat(arab) counter pattern the gate asserts is present on every counter)
    - .planning/phases/32-i18n-rtl-accessibility/32-CONTEXT.md (verification approach: grep for physical props absence, Intl.NumberFormat usage; final manual EN/AR pass is on staging, not local)
  </read_first>
  <action>
    Create scripts/wizard-rtl-check.js as a "use strict" CommonJS Node script (NO new dependency, NO test framework — mirror scripts/screens-parity-check.js). It reads (fs.readFileSync) src/components/hlshajara.css, src/styles/tokens.css, and every file under src/components/wizard/ and asserts, exiting 1 on any failure: (1) PHYSICAL-PROP ABSENCE — no wizard-scoped selector line (`.wizard-`, `.choice-`, `.review-`) in hlshajara.css contains `margin-left`/`margin-right`/`padding-left`/`padding-right`/`border-left`/`border-right`/bare `left:`/`right:`/`text-align:left|right`, excluding lines under a `[dir=rtl]` block or containing `direction:ltr`; (2) DS-MONO LTR — tokens.css contains a `[dir=rtl] .ds-mono` rule with `direction:ltr`; (3) MACHINE-STRING LTR — hlshajara.css `[dir=rtl] .review-sources` (or equivalent) forces `direction:ltr`; (4) INTL COUNTER — every wizard component that renders a numeric count uses `Intl.NumberFormat` with the `numberingSystem: "arab"` branch for `ar` (assert the literal substring `numberingSystem` appears in each file that also contains `Intl.NumberFormat`, and that no wizard component interpolates a raw `stepIndex`/`stepTotal`/count into JSX text without going through a formatter — grep for a `{...Index}` or `{...Total}` numeric interpolation NOT preceded by `fmt.format`/`format(`). Print a grouped pass/fail report. Use `grep -v '^#'`-style comment-stripping where counting tokens to avoid header-comment self-invalidation (the project grep-hygiene rule).
  </action>
  <verify>
    <automated>node scripts/wizard-rtl-check.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/wizard-rtl-check.js exists, is "use strict" CommonJS, requires only node: builtins (grep: no third-party require).
    - `node scripts/wizard-rtl-check.js` exits 0 against the Task-1-corrected CSS + the existing Intl-using counters.
    - The script's physical-prop assertion would FAIL (exit 1) if a wizard-scoped `margin-left` were reintroduced (verifiable by the script's own logic referencing the wizard-selector prefixes `.wizard-`/`.choice-`/`.review-`).
    - The script asserts `Intl.NumberFormat` + `numberingSystem` presence on every wizard component that renders a count (grep the script for both literals).
  </acceptance_criteria>
  <done>scripts/wizard-rtl-check.js exists, exits 0, asserts physical-prop absence + ds-mono LTR + machine-string LTR + Intl.NumberFormat counters, with no third-party deps.</done>
</task>

</tasks>

<verification>
- `node scripts/wizard-rtl-check.js` exits 0 (physical-prop absence + ds-mono LTR + Intl counters all pass).
- `grep -nE "(margin|padding|border)-(left|right)" src/components/hlshajara.css` returns no wizard-scoped non-`[dir=rtl]` matches.
- tokens.css has `[dir=rtl] .ds-mono { direction:ltr; … }`.
- `npm run build` is clean.
- NOTE: final EN/AR visual RTL sign-off is a manual browser pass on staging (event.staging.sanadais.com), per CONTEXT — not local-only.
</verification>

<success_criteria>
INTL-02 satisfied: the wizard is RTL-correct — logical properties only, no Arabic uppercase/letter-spacing, LTR-forced machine strings (.ds-mono/.review-sources), mirrored progress pills + Back/Next chevrons, Arabic-Indic step/source counters via Intl.NumberFormat. Grep gate + build pass; staging browser pass noted for final sign-off.
</success_criteria>

<output>
Create `.planning/phases/32-i18n-rtl-accessibility/32-02-SUMMARY.md` when done. If any counter component was found NOT using the Intl pattern, record it explicitly for the Plan 32-03 component a11y pass to fix.
</output>
