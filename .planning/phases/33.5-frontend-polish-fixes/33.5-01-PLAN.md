---
phase: 33.5-frontend-polish-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/ar.json
  - messages/en.json
  - src/components/hlshajara.css
  - src/components/Footer.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
must_haves:
  truths:
    - "Header CTA reads 'تقديم معلومات' in AR and 'Submit Information' in EN"
    - "Header CTA button is visibly larger and uses a rounded-rectangle shape instead of a pill"
    - "Footer founded day (30 May / 30 مايو) is rendered at font-weight 800"
    - "Footer TikTok link points to @HLShajaraa while other social handles stay @HLShajara"
  artifacts:
    - path: "messages/ar.json"
      provides: "Arabic header CTA label"
      contains: '"submit": "تقديم معلومات"'
    - path: "messages/en.json"
      provides: "English header CTA label"
      contains: '"submit": "Submit Information"'
    - path: "src/components/hlshajara.css"
      provides: "Header CTA + footer day styles"
      contains: ".site-nav-cta"
    - path: "src/components/Footer.tsx"
      provides: "TikTok handle override"
      contains: "HLShajaraa"
  key_links:
    - from: "Header.tsx"
      to: "messages/nav.submit"
      via: "useTranslations('nav')"
      pattern: "t\\(\"submit\"\\)"
    - from: "Footer.tsx"
      to: "TikTok URL"
      via: "SOCIAL_LINKS href"
      pattern: "tiktok.com/@HLShajaraa"
---

<objective>
Apply the operator-requested chrome polish: rename the header "Submit report" CTA to "Submit information", make the header CTA button larger and more prominent with a rounded-rectangle shape, embolden the footer founded date, and correct the TikTok handle to `HLShajaraa`.

Purpose: These are small, high-visibility copy and styling fixes that keep the public site aligned with the operator's latest messaging and social identity.
Output: Updated i18n files, CSS, and Footer component; all gates (i18n parity, build) pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Header.tsx
@src/components/Footer.tsx
@src/components/hlshajara.css
@messages/ar.json
@messages/en.json

## Key interfaces / conventions

- `nav.submit` is consumed by `Header.tsx` (`site-nav-cta` desktop + mobile) and `HeroSection.tsx` (`btn primary`). Changing this single key updates both buttons.
- Design tokens are defined in `src/styles/tokens.css` (`--radius-lg`, `--font-display`, `--brand`, etc.).
- `site-footer-founded-day` currently sets only `font-family`, `font-size`, and `color`; it needs `font-weight: 800` added while keeping the same family.
- Footer social links use a shared `HANDLE = "HLShajara"`; TikTok should get its own override so other platforms are untouched.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update nav.submit i18n copy</name>
  <files>messages/ar.json, messages/en.json</files>
  <action>
    Change the `nav.submit` value in both locale files per the operator request.
    - `messages/ar.json`: `"submit": "تقديم معلومات"` (was "تقديم بلاغ").
    - `messages/en.json`: `"submit": "Submit Information"` (was "Submit Report").
    Do NOT change other occurrences of "بلاغ" / "Report" outside `nav.submit` (e.g., `submit.title`, `home.ctaButton`, privacy text). Run `npm run check:i18n` to verify full key parity is preserved.
  </action>
  <verify>
    <automated>npm run check:i18n</automated>
  </verify>
  <done>`nav.submit` reads "تقديم معلومات" in AR and "Submit Information" in EN; i18n parity gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Enlarge and reshape the header CTA button</name>
  <files>src/components/hlshajara.css</files>
  <action>
    Update `.site-nav-cta` and `.site-nav-cta--desktop` to be larger and use a rounded-rectangle shape instead of the current pill.
    - Increase padding to `12px 28px` (matching the hero `.btn.primary` scale).
    - Increase `font-size` to `17px` and keep `font-weight: 700`.
    - Change `border-radius` from `999px` to `var(--radius-lg)` (12px token).
    - Keep existing color (`var(--brand)`), hover/active states, and transitions; only adjust size and radius.
    Ensure the mobile nav variant (`.site-nav .site-nav-cta`) remains usable and does not overflow the menu at 860px breakpoint.
  </action>
  <verify>
    <automated>grep -A 12 "\.site-nav-cta {" src/components/hlshajara.css | grep -E "padding:.*12px 28px|font-size:.*17px|border-radius:.*var\(--radius-lg\)"</automated>
  </verify>
  <done>Header CTA is visibly larger with 12px border-radius; no layout breakage at mobile breakpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Style footer founded day and fix TikTok handle</name>
  <files>src/components/hlshajara.css, src/components/Footer.tsx</files>
  <action>
    In `src/components/hlshajara.css`, add `font-weight: 800` to `.site-footer-founded-day` while preserving `font-family: var(--font-mono)`, `font-size: 11px`, and color.
    In `src/components/Footer.tsx`, introduce a separate `TIKTOK_HANDLE = "HLShajaraa"` constant and use it only for the TikTok entry's `href`; leave the shared `HANDLE` as `"HLShajara"` for X/Twitter, Instagram, Facebook, YouTube, and Telegram.
  </action>
  <verify>
    <automated>grep -A 5 "\.site-footer-founded-day {" src/components/hlshajara.css | grep "font-weight: 800" && grep -c "HLShajaraa" src/components/Footer.tsx</automated>
  </verify>
  <done>Footer date is font-weight 800 and TikTok link targets @HLShajaraa; other social links remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public site → visitor | Only public copy and styling changes; no new data collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.5-01 | Spoofing | TikTok handle | mitigate | Use exact operator-supplied handle `HLShajaraa`; verify link resolves before closing. |
| T-33.5-02 | Information disclosure | i18n copy change | accept | Public copy change only; no PII or safety-rule relaxation. |
</threat_model>

<verification>
- `npm run check:i18n` passes.
- `npm run build:static` completes without errors.
- Header CTA text and button shape are visually larger/rectangular in both EN and AR.
- Footer founded date renders heavier (800) without changing font family.
- Footer TikTok icon links to `https://tiktok.com/@HLShajaraa`.
</verification>

<success_criteria>
- `nav.submit` is "تقديم معلومات" / "Submit Information" and i18n parity is preserved.
- `.site-nav-cta` has padding 12px 28px, font-size 17px, border-radius `var(--radius-lg)`.
- `.site-footer-founded-day` has `font-weight: 800`.
- TikTok href uses `@HLShajaraa`; all other social hrefs still use `@HLShajara`.
- `npm run build:static` succeeds.
</success_criteria>

<output>
Create `.planning/phases/33.5-frontend-polish-fixes/33.5-01-SUMMARY.md` when done.
</output>
