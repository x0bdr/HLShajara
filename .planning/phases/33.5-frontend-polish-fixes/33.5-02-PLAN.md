---
phase: 33.5-frontend-polish-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/pics/new/
  - src/components/ImageSlider.tsx
  - src/components/hlshajara.css
autonomous: true
requirements:
  - POLISH-05
must_haves:
  truths:
    - "Only the 6 yesterday-added .jpg campaign photos remain in public/pics/new/"
    - "Homepage slider images load correctly in both English and Arabic"
    - "Arabic slider does not appear blank or broken while English works"
  artifacts:
    - path: "public/pics/new/"
      provides: "Slider photo assets"
      contains: "photo_5832231186569302259_y.jpg, photo_5877261144118988365_y.jpg, photo_5877261144118988366_y.jpg, photo_5877261144118988367_y.jpg, photo_5877261144118988368_y.jpg, photo_5877261144118988369_y.jpg"
    - path: "src/components/ImageSlider.tsx"
      provides: "RTL-aware slider transform"
      contains: "dir"
    - path: "src/components/hlshajara.css"
      provides: "Slider RTL styles"
      contains: "image-slider"
  key_links:
    - from: "SliderSection.tsx"
      to: "public/pics/new/"
      via: "readdir"
      pattern: "readdir\(PHOTO_DIR\)"
    - from: "ImageSlider.tsx"
      to: ".image-slider-track"
      via: "transform: translateX"
      pattern: "translateX\(.*%\)"
---

<objective>
Clean up the campaign photo folder and fix the Arabic-specific homepage slider failure while keeping English working.

Purpose: The operator reports pictures work in English but not Arabic on staging, and wants only the 6 photos added yesterday kept. The Arabic failure is most likely an RTL layout issue with the slider track transform; the photo cleanup reduces the set to the 6 intended `_y.jpg` files.
Output: A slider that displays correctly in both locales with only the 6 yesterday-added photos, verified in static export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/SliderSection.tsx
@src/components/ImageSlider.tsx
@src/components/hlshajara.css

## Key interfaces / conventions

- `SliderSection` reads `public/pics/new/` at build/request time and filters for `/\.(jpe?g|png|webp|gif)$/i`.
- The 6 photos added yesterday are the `.jpg` files in `public/pics/new/`:
  - `photo_5832231186569302259_y.jpg`
  - `photo_5877261144118988365_y.jpg`
  - `photo_5877261144118988366_y.jpg`
  - `photo_5877261144118988367_y.jpg`
  - `photo_5877261144118988368_y.jpg`
  - `photo_5877261144118988369_y.jpg`
- All other files in `public/pics/new/` are older `.jpeg` campaign photos and should be removed.
- `ImageSlider` uses `transform: translateX(${index * -100}%)` to slide horizontally. In an RTL context (`dir="rtl"`), flex `row` lays items right-to-left, which can make the active slide appear off-screen if the transform direction is not adjusted.
- The slider buttons already use logical properties (`inset-inline-start/end`) and scale-x mirroring for RTL.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep only the 6 yesterday-added .jpg photos</name>
  <files>public/pics/new/</files>
  <action>
    Delete every file in `public/pics/new/` except the 6 `_y.jpg` files listed in the context. Keep the exact filenames; do not rename them. The `.jpeg` campaign photos (24 files such as `photo_2026-05-31 18.19.50.jpeg`) and any other non-`.jpg` assets must be removed.
    After deletion, run `ls public/pics/new/` and confirm exactly the 6 `.jpg` files remain.
  </action>
  <verify>
    <automated>ls -1 public/pics/new/ | sort | diff - <(printf '%s\n' 'photo_5832231186569302259_y.jpg' 'photo_5877261144118988365_y.jpg' 'photo_5877261144118988366_y.jpg' 'photo_5877261144118988367_y.jpg' 'photo_5877261144118988368_y.jpg' 'photo_5877261144118988369_y.jpg' | sort)</automated>
  </verify>
  <done>Only the 6 yesterday-added .jpg files remain in public/pics/new/.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Arabic RTL slider image visibility</name>
  <files>src/components/ImageSlider.tsx, src/components/hlshajara.css</files>
  <action>
    Diagnose why the slider appears blank/broken on Arabic pages but works in English. Likely cause: in `dir="rtl"`, the `.image-slider-track` flex direction is reversed, so `translateX(${index * -100}%)` moves the active slide out of the visible viewport.
    Apply the minimal fix:
    - Option A (preferred): Make the slider internally LTR so the existing transform logic works identically in both locales. Add `direction: ltr;` to `.image-slider` in CSS. The surrounding page stays RTL; only the carousel's internal layout direction is forced. Verify prev/next buttons and dots still position correctly via logical properties.
    - Option B: Make `ImageSlider` RTL-aware by reading `document.dir` and flipping the transform sign in RTL (`translateX(${index * 100}%)`).
    Do not change slide `src`, `sizes`, or `encodeURIComponent` logic unless diagnosis proves a different root cause.
  </action>
  <verify>
    <automated>npm run build:static && grep -E 'image-slider \{|direction:\s*ltr;' src/components/hlshajara.css</automated>
  </verify>
  <done>Arabic slider renders the active slide in viewport; the chosen RTL fix is applied and build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify both locales in static export</name>
  <files>out/</files>
  <action>
    Run `npm run build:static`. Inspect the generated `out/en.html` and `out/ar.html`:
    1. Both should reference only the 6 `.jpg` files under `/pics/new/`.
    2. Image URLs should include the `/HLShajara` basePath prefix (e.g., `/HLShajara/pics/new/photo_5877261144118988369_y.jpg`).
    3. Serve `out/` locally with `python3 -m http.server 3000` and request both `http://localhost:3000/HLShajara/en.html` and `http://localhost:3000/HLShajara/ar.html`. Confirm the first slider image loads with HTTP 200 in both locales and no 404s for `/pics/new/` assets.
  </action>
  <verify>
    <automated>npm run build:static && python3 -m http.server 3000 --directory out & SERVER_PID=$!; sleep 1; curl -sI "http://localhost:3000/HLShajara/pics/new/photo_5877261144118988369_y.jpg" | head -1; curl -sI "http://localhost:3000/HLShajara/ar.html" | head -1; kill $SERVER_PID</automated>
  </verify>
  <done>Both English and Arabic static exports show only the 6 kept photos and serve them successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public site → visitor | Slider displays public campaign photos only; no new data collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33.5-03 | Tampering | Deleted photo files | mitigate | Delete only from `public/pics/new/`; keep source copies in `pics/` root untouched unless explicitly requested. |
| T-33.5-04 | Information disclosure | Photo filenames | accept | Filenames are already public campaign photos; no PII. |
| T-33.5-05 | Denial of service | RTL slider blank | mitigate | Force internal LTR direction on the carousel so transform logic is locale-independent. |
</threat_model>

<verification>
- `public/pics/new/` contains exactly the 6 yesterday-added `.jpg` files.
- `npm run build:static` completes without errors.
- Generated `out/en.html` and `out/ar.html` reference only those 6 `.jpg` files.
- Local static server returns HTTP 200 for slider images in both `/HLShajara/en.html` and `/HLShajara/ar.html`.
- Arabic page does not show a blank/white slider.
</verification>

<success_criteria>
- Only these 6 files remain in `public/pics/new/`: `photo_5832231186569302259_y.jpg`, `photo_5877261144118988365_y.jpg`, `photo_5877261144118988366_y.jpg`, `photo_5877261144118988367_y.jpg`, `photo_5877261144118988368_y.jpg`, `photo_5877261144118988369_y.jpg`.
- The Arabic homepage slider displays images correctly, not blank.
- The English homepage slider continues to work.
- Static export image URLs include `/HLShajara/pics/new/` prefix.
</success_criteria>

<output>
Create `.planning/phases/33.5-frontend-polish-fixes/33.5-02-SUMMARY.md` when done.
</output>
