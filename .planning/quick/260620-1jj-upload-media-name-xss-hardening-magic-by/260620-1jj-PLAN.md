---
phase: quick-260620-1jj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/escape.ts
  - src/lib/escape.test.ts
  - src/lib/report-pdf.ts
  - src/lib/report-md.ts
  - src/app/[locale]/reviewer/ReviewerClient.tsx
  - src/lib/validation.ts
  - src/lib/validation/is-valid.ts
  - src/lib/validation/is-valid.test.ts
  - src/app/api/upload/route.ts
  - src/app/api/upload/route.test.ts
  - src/components/wizard/MediaEvidenceStep.tsx
  - messages/en.json
  - messages/ar.json
  - package.json
autonomous: true
requirements: [A.1, A.4a, A.4b, A.4c, A.5, MEDIA-NAME]
user_setup: []

must_haves:
  truths:
    - "A script/exe renamed .png with a spoofed image Content-Type is REJECTED by the upload route (server sniffs real magic bytes)."
    - "A genuine PNG/JPEG/WEBP/MP4 still uploads successfully; the stored extension is derived from the SNIFFED type, not the client filename/type."
    - "SVG uploads remain rejected (not in the allowlist; XSS vector)."
    - "A media label or originalName containing <script>/<img onerror>/angle brackets is neutralized (encoded/stripped) at the trust boundary AND escaped at every render sink."
    - "The reviewer print-HTML (document.write) escapes every user string; a javascript:/data: href is rejected, http/https hrefs pass."
    - "report-md.ts markdown escapes user strings so a crafted [label](javascript:...) cannot inject a link when rendered to HTML."
    - "New upload/validation error strings exist in BOTH messages/en.json and messages/ar.json with full key parity."
  artifacts:
    - path: "src/lib/escape.ts"
      provides: "Shared escapeHtml + escapeHtmlAttr + escapeMarkdown + safeHttpUrl single source of truth"
      exports: ["escapeHtml", "escapeMarkdown", "safeHttpUrl"]
    - path: "src/lib/escape.test.ts"
      provides: "Vitest coverage for escapers neutralizing XSS/markdown payloads"
    - path: "src/app/api/upload/route.test.ts"
      provides: "Magic-byte sniff acceptance/rejection tests (genuine vs spoofed vs SVG)"
    - path: "src/lib/validation/is-valid.test.ts"
      provides: "media-name sanitizer strips/encodes angle brackets + caps length"
  key_links:
    - from: "src/app/api/upload/route.ts"
      to: "file-type fileTypeFromBuffer"
      via: "dynamic import + sniff before allowlist decision"
      pattern: "fileTypeFromBuffer"
    - from: "src/lib/report-md.ts"
      to: "src/lib/escape.ts"
      via: "import escapeMarkdown"
      pattern: "escapeMarkdown"
    - from: "src/app/[locale]/reviewer/ReviewerClient.tsx"
      to: "src/lib/escape.ts"
      via: "import escapeHtml + safeHttpUrl in exportSubmissionToPDF"
      pattern: "escapeHtml|safeHttpUrl"
---

<objective>
Harden HLShajara file-upload and media-name handling against XSS and Content-Type spoofing, closing the EXACT gap list from the 2026-06-20 security audit (A.1, A.4a/b/c, A.5, MEDIA-NAME).

Purpose: This is a defamation-sensitive civic accountability platform with hostile traffic. The upload route currently trusts the client `file.type` for its allowlist decision (A.1 — spoofable), and several render sinks interpolate user-controlled `originalName`/`label`/`url` without escaping (A.4b reviewer print HTML; A.4c report-md). Media names must be plain text, validated + sanitized at the source and html-encoded at every sink. Fail closed on any sniff/validation uncertainty.

Output: one new prod dep (`file-type`), a shared escape utility, server-side magic-byte sniffing, source-level media-name sanitization, per-sink escaping in the two unsafe sinks, client-side magic-byte pre-check, bilingual error strings, and Vitest coverage.

Scope guard: DO NOT touch posts.body, the publications/admin editor, or the posts route — that TipTap surface is a separate follow-up. report-pdf.ts is already safe (A.4a) — reuse, do not regress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

Existing SAFE escaper to extract (src/lib/report-pdf.ts ~line 145):
```typescript
function escapeHtml(text: string): string {
  return text
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;");
}
```

Existing sanitizer to build on (src/lib/validation/is-valid.ts):
```typescript
export function sanitizeInput(value: string): string {
  // Trim + collapse whitespace + strip non-printable control chars.
  return value.trim().replace(/\s+/g, " ")
    .replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, "");
}
```

Media-name schema sites (src/lib/validation.ts, submitSchema.sourceFiles ~line 269):
```typescript
sourceFiles: z.array(z.object({
  hash: z.string(),
  filename: sanitizedString(255),
  originalName: sanitizedString(255),   // tighten to plain-name sanitizer
  url: z.string(),
  size: z.number(),
  label: optionalSanitizedString(255),  // tighten to plain-name sanitizer
})).default([]),
```
`sanitizedString(max)` = `z.string().max(max).transform(sanitizeInput)`.

Upload route allowlist decision (src/app/api/upload/route.ts ~line 66):
```typescript
const allowedExt = EXT_BY_TYPE[file.type];   // <-- trusts CLIENT file.type (A.1)
if (!allowedExt) { /* reject INVALID_FILE_TYPE */ }
```
EXT_BY_TYPE covers: image/jpeg .jpg, image/png .png, image/webp .webp, image/tiff .tiff, image/avif .avif, video/mp4 .mp4, video/webm .webm, video/quicktime .mov. SVG is intentionally absent — keep it absent.
Route returns `originalName: file.name` raw at ~line 141.

Reviewer print sink (src/app/[locale]/reviewer/ReviewerClient.tsx, exportSubmissionToPDF ~line 207-297). Unescaped user strings interpolated into `html` then `printWindow.document.write(html)`:
- `f.label`, `f.originalName`, `f.url` (text AND href) — mediaRows ~line 247
- `sub.entityName` ~line 267
- `sub.allegationDescription` ~line 272
- metaRows values via `fmtValue(...)` ~line 241-244 (owner/investor names, labour entries name/role, socialContactMethods type/value, arbitrary `String(value)` fallback)
- `<title>` uses `sub.id` (number, safe) — leave
SourceLink shape: `{ url: string; title?: string }`.

file-type (npm, sindresorhus, latest 22.x, ESM-only `type: module`): import is `fileTypeFromBuffer(uint8array|buffer)` → `Promise<{ ext, mime } | undefined>`; browser build `fileTypeFromBlob(blob)`. Returns undefined for unrecognized/text content. Dynamic `await import("file-type")` from the route (ESM).
</interfaces>

@src/lib/report-pdf.ts
@src/lib/report-md.ts
@src/app/[locale]/reviewer/ReviewerClient.tsx
@src/app/api/upload/route.ts
@src/components/wizard/MediaEvidenceStep.tsx
@src/lib/validation.ts
@src/lib/validation/is-valid.ts
@src/app/api/submit/route.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared escape util + apply to report-md and reviewer print sinks</name>
  <files>src/lib/escape.ts, src/lib/escape.test.ts, src/lib/report-pdf.ts, src/lib/report-md.ts, src/app/[locale]/reviewer/ReviewerClient.tsx</files>
  <behavior>
    - escapeHtml("&lt;script&gt;...") → all of & < > " ' encoded; identical output to the existing report-pdf escaper (byte-for-byte) so no PDF regression.
    - escapeMarkdown("[x](javascript:alert(1))") → escapes markdown-active chars (`\ ` * _ { } [ ] ( ) # + - . ! | < >` and backtick) and angle brackets so it cannot form a link/image/html when rendered to HTML.
    - safeHttpUrl("javascript:alert(1)") → "" (or "#"); safeHttpUrl("data:text/html,...") → ""; safeHttpUrl("https://x.test/a") → unchanged; safeHttpUrl("/uploads/abc.png") → unchanged (relative allowed). Decision: validate scheme is http/https OR relative (no scheme); reject everything else. Fail closed.
    - escapeHtml(undefined as any) and escapeMarkdown(null as any) coerce to "" without throwing (sinks pass possibly-undefined label).
  </behavior>
  <action>
    Create `src/lib/escape.ts` as the single source of truth exporting: `escapeHtml(text: unknown): string` (coerce to string, then the exact 5-replace sequence from report-pdf.ts &→&amp; <→&lt; >→&gt; "→&quot; '→&#039;); `escapeMarkdown(text: unknown): string` (coerce, then backslash-escape markdown-active characters AND angle brackets so crafted values cannot inject links/images/raw HTML when the markdown is rendered); `safeHttpUrl(url: unknown): string` (coerce + trim; allow only values whose scheme is http:/https: OR that are scheme-relative/path-relative with no `:` before the first `/` — i.e. reuse the platform's http/https-only policy, mirroring URL_OPTIONS in is-valid.ts; reject javascript:/data:/vbscript:/file: etc., returning "").
    In `src/lib/report-pdf.ts`: replace the local `escapeHtml` function body with an import of `escapeHtml` from `@/lib/escape` (keep the same name/usages so all existing call sites are unchanged — this is the A.4a "reuse, do not regress" step; verify the output is identical so the PDF is byte-stable).
    In `src/lib/report-md.ts` (A.4b): import `escapeMarkdown` and `safeHttpUrl` from `@/lib/escape`. Apply `escapeMarkdown` to EVERY user-controlled string interpolated into the markdown: `submission.entityName` (~line 198), `submission.allegationDescription` (~line 208), each `file.originalName` and `file.label` in the sourceFiles section (~line 186), and every `String(raw)`/name/role/value produced by `formatValue` (owner/investor names ~line 137, labourEntries name+role ~line 145, socialContactMethods type+value ~line 150, and the default `String(raw)` return ~line 161). For source-file URLs use `safeHttpUrl(toAbsoluteUrl(file.url))`. Do NOT escape the static Arabic labels/headers or system-generated values (status labels, category labels, dates, ids).
    In `src/app/[locale]/reviewer/ReviewerClient.tsx` `exportSubmissionToPDF` (A.4c): import `escapeHtml` and `safeHttpUrl` from `@/lib/escape`. Escape every user string before it enters the `html` template that is `document.write`-n: `f.label`, `f.originalName`, and `f.url` TEXT (mediaRows ~line 247) with escapeHtml; the `f.url` HREF with `safeHttpUrl` (escape the result for the attribute too); `sub.entityName` (~line 267); `sub.allegationDescription` (~line 272); and the `fmtValue(...)` output used in metaRows (~line 243) — wrap fmtValue's return with escapeHtml, or escape each user component inside fmtValue (owner/investor names, labourEntries name+role, socialContactMethods type+value, and the default `String(value)`). Leave numeric `sub.id`, static `dir`/`align`/labels, and the inline `<style>` untouched.
    Create `src/lib/escape.test.ts` covering the four behaviors above plus: escapeHtml neutralizes `<script>alert(1)</script>` and `<img src=x onerror=alert(1)>`; escapeMarkdown neutralizes `[click](javascript:alert(1))` and `![x](data:...)`; safeHttpUrl rejects javascript:/data:/vbscript: and accepts https + relative.
  </action>
  <verify>
    <automated>npx vitest run src/lib/escape.test.ts</automated>
  </verify>
  <done>escape.ts exports escapeHtml/escapeMarkdown/safeHttpUrl; report-pdf imports the shared escapeHtml (no behavior change); report-md and ReviewerClient print builder escape all user strings + validate hrefs; escape.test.ts passes; `npx tsc --noEmit` clean for the touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upload route magic-byte sniff + originalName sanitize + media-name schema tightening</name>
  <files>src/app/api/upload/route.ts, src/lib/validation/is-valid.ts, src/lib/validation.ts, src/lib/validation/is-valid.test.ts, src/app/api/upload/route.test.ts, package.json</files>
  <behavior>
    - Genuine PNG buffer (real magic bytes) with image/png Content-Type → accepted; stored extension derived from SNIFFED type (.png).
    - Genuine JPEG/WEBP/MP4 → accepted similarly.
    - A buffer of script/exe bytes named evil.png with spoofed Content-Type image/png → REJECTED (sniffed mime absent from allowlist OR mismatches client type) with a 400 INVALID_FILE_TYPE-style code.
    - An SVG buffer (sniffs to image/svg+xml or to text/undefined) → REJECTED (svg never in allowlist).
    - file-type returning undefined (unrecognized content) → REJECTED (fail closed).
    - sanitizeMediaName("<img onerror=x>héllo") → angle brackets stripped/encoded, control chars stripped, length-capped (255); plain text retained.
  </behavior>
  <action>
    Install the prod dependency: `npm install file-type@^22` (sindresorhus, ESM; STACK.md explicitly recommends it; only new dep allowed). Confirm it lands in package.json `dependencies`.
    In `src/lib/validation/is-valid.ts`: add `export function sanitizeMediaName(value: string, max = 255): string` — coerce, run `sanitizeInput` (trim/collapse/strip control chars), then strip or encode angle brackets and other markup-forming characters so the result is plain text (decision: strip `<` and `>` entirely, and drop any residual markup/backtick noise), and hard-cap to `max` chars via slice. Plain-text only, no HTML, no markdown.
    In `src/app/api/upload/route.ts` (A.1) reorder the validation: after reading the buffer (`bytes`/`buffer`) and BEFORE the EXT_BY_TYPE/allowlist decision, dynamically `const { fileTypeFromBuffer } = await import("file-type")` and `const sniffed = await fileTypeFromBuffer(buffer)`. Reject (400, code `INVALID_FILE_TYPE`) when: `sniffed` is undefined (fail closed), OR `sniffed.mime` is not a key in EXT_BY_TYPE, OR `sniffed.mime !== file.type` (client/sniff mismatch). Derive `allowedExt = EXT_BY_TYPE[sniffed.mime]` from the SNIFFED mime (not client type, not filename). Drive the downstream IMAGE_TYPES/VIDEO_TYPES branches off `sniffed.mime` too (so sharp/ffmpeg run on the true type). Keep the existing sharp → ffmpeg → clamav scanBuffer → sha256 → write flow intact and in order. SVG stays rejected (not in EXT_BY_TYPE). Before returning, sanitize the response `originalName` with `sanitizeMediaName(file.name)` (defense-in-depth at the source). Note: `buffer` is reassigned by sharp/ffmpeg; sniff the ORIGINAL pre-strip buffer (move the `arrayBuffer()` read above the sniff if needed) so we validate true intake content.
    In `src/lib/validation.ts`: replace `originalName: sanitizedString(255)` and `label: optionalSanitizedString(255)` in `submitSchema.sourceFiles` with the plain-name sanitizer — add a local helper `mediaNameString(max)` = `z.string().max(max).transform((v) => sanitizeMediaName(v, max))` (import sanitizeMediaName), use it for `originalName` and an optional variant for `label`. This keeps the Zod schema as the trust boundary even though the route already sanitizes (BOTH, per MEDIA-NAME).
    Tests — `src/lib/validation/is-valid.test.ts`: assert sanitizeMediaName strips/encodes `<`/`>`, removes control chars, caps length, retains plain text. `src/app/api/upload/route.test.ts`: mirror the mock style of submit/route.test.ts — `vi.mock` `@/lib/clamav` (scanBuffer → {clean:true}), `@/lib/media-metadata` (isFfmpegAvailable→true, stripVideoMetadata→passthrough), `sharp` (default export returning a chainable `.rotate().toBuffer()` resolving the input buffer), `@/lib/rate-limit` (rateLimitResponse→{ok:true}), and `fs/promises` (writeFile/mkdir→noop). Do NOT mock file-type — exercise the real sniffer. Build genuine fixture buffers inline: a minimal valid PNG (8-byte signature + IHDR) and a minimal JPEG (FFD8FF...FFD9); a fake "PNG" buffer that is really ASCII script text; and an SVG `<svg>` buffer. POST each via a `Request` with `formData` and assert: genuine PNG → 200 + filename ends `.png`; spoofed text-as-png → 400 INVALID_FILE_TYPE; svg → 400. If a robust minimal media buffer is impractical for a type, prefer the `file-type` package's own documented fixtures or the smallest known-valid byte sequence; never weaken the assertion to make a bad buffer pass.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/upload/route.test.ts src/lib/validation/is-valid.test.ts</automated>
  </verify>
  <done>file-type@^22 in package.json dependencies; upload route sniffs magic bytes before the allowlist decision, rejects undefined/non-allowlisted/mismatched and SVG, derives extension from sniffed type, sanitizes originalName in the response; submitSchema.sourceFiles uses the plain-name sanitizer for originalName+label; both test files pass (genuine accepted, spoofed/SVG rejected); `npx tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend MediaEvidenceStep client magic-byte pre-check + bilingual errors</name>
  <files>src/components/wizard/MediaEvidenceStep.tsx, messages/en.json, messages/ar.json</files>
  <action>
    In `src/components/wizard/MediaEvidenceStep.tsx` (A.5) strengthen client-side validation as a UX/defense-in-depth layer (server remains the authority). Replace the extension-only `isAllowedFileType` check in `uploadFiles` with a magic-byte sniff before POST: `const { fileTypeFromBlob } = await import("file-type")`, `const sniffed = await fileTypeFromBlob(file)`, and reject when sniffed is undefined OR `sniffed.mime` is not one of the accepted image/video MIME types (mirror the server EXT_BY_TYPE keys: image/jpeg|png|webp|tiff|avif, video/mp4|webm|quicktime) OR `sniffed.mime` does not match the file.type family — show a bilingual error and skip the upload. Keep the existing `file.size > MAX_FILE_SIZE` check and the MAX_FILES gate. Use `await` inside the loop (the function is already async). Add a new error message key `uploadFileSpoofed` for the sniff-mismatch case and use it; keep `uploadFileTypeNotAllowed`/`uploadFileTooLarge` for their existing cases. Fail closed: if the sniff throws, treat as not-allowed and surface `uploadFileTypeNotAllowed`.
    Add the new key `uploadFileSpoofed` under the `submit` namespace in BOTH `messages/en.json` and `messages/ar.json` with full parity. EN: e.g. "This file's contents don't match its type and was not uploaded. Please upload a genuine image or video." AR (RTL-safe, non-sectarian, serious tone): a faithful Arabic equivalent. Place it next to the other `upload*` keys. No `left`/`right` literals introduced.
  </action>
  <verify>
    <automated>npm run check:i18n:submit && npx tsc --noEmit</automated>
  </verify>
  <done>MediaEvidenceStep sniffs magic bytes client-side via file-type browser build and rejects spoofed/mismatched files before upload with a bilingual error; `uploadFileSpoofed` present in both en.json and ar.json with parity; check:i18n:submit passes; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/upload | Untrusted file bytes + client-declared Content-Type + filename cross here. |
| submitter → /api/submit (sourceFiles schema) | Untrusted originalName/label strings persisted to DB. |
| DB submission → reviewer print HTML (document.write) | Stored user strings reach a DOM sink. |
| DB submission → report-md markdown → (downstream HTML render) | Stored user strings reach a markdown→HTML sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1jj-01 | Tampering | upload route allowlist keyed on client file.type | mitigate | Magic-byte sniff (file-type) before allowlist decision; reject undefined/non-allowlisted/mismatch; derive ext from sniffed type (Task 2). |
| T-1jj-02 | Elevation/XSS | reviewer print HTML document.write | mitigate | escapeHtml every user string + safeHttpUrl on hrefs (Task 1). |
| T-1jj-03 | Tampering/Injection | report-md markdown interpolation | mitigate | escapeMarkdown all user strings + safeHttpUrl on file urls (Task 1). |
| T-1jj-04 | Spoofing | media name (originalName/label) plain-text guarantee | mitigate | sanitizeMediaName at upload-route source AND submitSchema trust boundary; strip/encode angle brackets, cap length (Task 2). |
| T-1jj-05 | Tampering | client upload bypass / spoofed file before POST | mitigate | client-side fileTypeFromBlob sniff; UX layer, server authoritative (Task 3). |
| T-1jj-SC | Tampering | npm install file-type | mitigate | file-type verified legitimate (sindresorhus, latest 22.x, STACK.md-recommended) — see Package Legitimacy note below; no [ASSUMED]/[SUS] checkpoint required. |
</threat_model>

<package_legitimacy>
Single new prod dependency: `file-type` (npm). Verified during planning against the npm registry: published by `sindresorhus`, latest `22.0.1`, ESM-only (`type: module`). Explicitly recommended by this project's STACK.md ("file-type 19.x — Magic-byte MIME sniffing — Never trust the client Content-Type"). `^22` satisfies the locked "v19+" requirement. Disposition: legitimate, no blocking human-verify checkpoint needed.
</package_legitimacy>

<verification>
- `npx vitest run` — all suites green (escape, upload route, is-valid, plus the existing challenge + submit route suites unbroken).
- `npm run check:i18n && npm run check:i18n:submit` — EN/AR parity holds with the new `uploadFileSpoofed` key.
- `npx tsc --noEmit` — strict TS clean across touched files.
- Manual spot proof of A.1: a script renamed `.png` with spoofed `Content-Type: image/png` is rejected by the route's sniff branch (covered by route.test.ts).
- Confirm report-pdf output is byte-stable (escapeHtml import is a pure refactor — no rendered-PDF change).
</verification>

<success_criteria>
- A.1: upload route sniffs magic bytes before the allowlist decision; rejects undefined/non-allowlisted/mismatched; SVG rejected; stored extension from sniffed type.
- A.4a: report-pdf reuses the shared escaper, no regression.
- A.4b: report-md escapes all user strings (markdown) + validates file urls.
- A.4c: reviewer print HTML escapes all user strings + validates hrefs (http/https only).
- A.5: MediaEvidenceStep client-side magic-byte sniff rejects spoofed files before upload, bilingual error.
- MEDIA-NAME: originalName + label are plain-text sanitized at the upload-route source AND the submit schema trust boundary; angle brackets stripped/encoded, length-capped.
- All listed Vitest assertions pass; EN/AR parity intact; posts.body / publication editor untouched.
</success_criteria>

<output>
Create `.planning/quick/260620-1jj-upload-media-name-xss-hardening-magic-by/260620-1jj-SUMMARY.md` when done.
</output>
