---
milestone: v1.1
audited: 2026-05-31T21:40:00Z
status: gaps_found
scores:
  requirements: 14/17 satisfied, 3/17 partial (see CRIT gaps)
  phases: 6/6 complete (Phases 10-15 code-complete)
  integration: 4/6 wired correctly
  flows: 3/5 end-to-end working
gaps:
  requirements:
    - id: "INTAKE-04"
      status: "partial"
      phase: "Phase 15"
      claimed_by_plans: [".planning/REQUIREMENTS.md", ".planning/ROADMAP.md"]
      completed_by_plans: ["src/lib/rate-limit.ts", "src/app/api/submit/route.ts"]
      verification_status: "gaps_found"
      evidence: "Rate limiting only on /api/submit. Missing on /api/review, /api/reply/admin, /api/upload, /api/key-decisions, /api/dashboard."
    - id: "RECORD-03"
      status: "partial"
      phase: "Phase 12"
      claimed_by_plans: [".planning/REQUIREMENTS.md"]
      completed_by_plans: ["src/db/migrations/001_published_entities_view.sql"]
      verification_status: "gaps_found"
      evidence: "Materialized view SQL exists and was applied on staging, but GET /api/entity?id=xxx does NOT use it — falls back to direct table query without publishedAt check on single-entity lookup."
    - id: "REPLY-02"
      status: "partial"
      phase: "Phase 13"
      claimed_by_plans: [".planning/REQUIREMENTS.md"]
      completed_by_plans: ["src/app/api/reply/admin/route.ts"]
      verification_status: "gaps_found"
      evidence: "Reply admin API requires 'admin' role. Requirement says 'approved, corrected, or rejected by admin' — this is correct. However, the reply intake API (POST /api/reply) matches unpublished entities, allowing replies against drafts."
  integration:
    - from: "Auth (Phase 10)"
      to: "Reviewer Console (Phase 11)"
      issue: "Page-level redirect in /reviewer/page.tsx does not check require2FA(). User without 2FA is redirected to reviewer page, then API calls 403. Poor UX but API enforces gate."
    - from: "Auth (Phase 10)"
      to: "Admin Replies (Phase 13)"
      issue: "Page-level redirect in /admin/replies/page.tsx does not check require2FA(). Same UX gap as reviewer."
    - from: "Submit (Phase 3)"
      to: "Intake Validation (Phase 15)"
      issue: "Anonymous submission broken — POST /api/submit requires authentication despite isAnonymous checkbox and privacy policy claims."
  flows:
    - name: "Anonymous Submission"
      breaks_at: "POST /api/submit"
      issue: "Returns 401 for unauthenticated users. Form UI still shows 'Submit Anonymously' checkbox."
    - name: "2FA Setup"
      breaks_at: "No UI exists"
      issue: "Reviewers must enable 2FA via browser DevTools console or direct API calls. No setup page or QR code display in the login flow after first authentication."
    - name: "Key Decision Recording"
      breaks_at: "POST /api/key-decisions"
      issue: "No authentication on the endpoint. Anyone can create or overwrite legal decisions."
tech_debt:
  - phase: "Phase 10 (Auth)"
    items:
      - "No 2FA setup UI — reviewers must use raw API or DevTools"
      - "Page-level auth checks don't enforce 2FA before rendering protected pages"
  - phase: "Phase 12 (Search)"
    items:
      - "Arabic search relevance benchmarking deferred (OpenSearch/Meilisearch)"
      - "Search is PostgreSQL ILIKE only — no stemming, no relevance ranking"
  - phase: "Phase 15 (Data Hardening)"
    items:
      - "ClamAV not installed on staging server — scan falls back to no-op with warning"
      - "Rate limiting only covers submit endpoint, not all internal APIs"
      - "IP hash in submit uses SHA-256 but stores raw IP string in rate_limits.key (different purposes, but noted)"
  - phase: "General"
    items:
      - "ROADMAP.md and PROJECT.md still show v1.1 as 'Not started' / unchecked"
      - "No formal phase VERIFICATION.md or SUMMARY.md files exist in .planning/phases/"
      - "Entity detail pages (entity/[id]) still statically prerender old demo IDs"
---

# Milestone Audit Report — v1.1 Hardening

## 1. Milestone Scope

| Phase | Name | Status | Commit |
|-------|------|--------|--------|
| 10 | 2FA & Login UI | Code-complete | cf0ed35 |
| 11 | Reviewer Console v2 | Code-complete | 537453e |
| 12 | Search & Filtering | Code-complete | 973c61d |
| 13 | Reply Processing | Code-complete | d2de6bc |
| 14 | Transparency Dashboard | Code-complete | d2de6bc |
| 15 | Data Hardening | Code-complete | 59af5e7 |

**Definition of Done:** All 17 v1.1 requirements in REQUIREMENTS.md are marked `[x]` Done, cross-referenced against deployed codebase on staging.

## 2. Phase Verification Summary

This project does not use per-phase VERIFICATION.md files. Verification was performed via:
- Code review against REQUIREMENTS.md traceability table
- UAT audit (`.planning/UAT-AUDIT-2026-05-31.md`)
- Direct deployed endpoint testing on staging.hlshajara.com

| Phase | Requirements | Code Status | Deployed | Gaps |
|-------|-------------|-------------|----------|------|
| 10 | AUTH-01, AUTH-03 | Done | Yes | No 2FA setup UI |
| 11 | VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04 | Done | Yes | Page-level 2FA check missing |
| 12 | RECORD-01, RECORD-03 | Done | Yes | Single-entity lookup bypasses published filter |
| 13 | REPLY-02 | Done | Yes | Matches unpublished entities |
| 14 | TRANS-01 | Done | Yes | None |
| 15 | INTAKE-02, INTAKE-04, INTAKE-05, LEGAL-01, LEGAL-02, SITE-02, DATA-05 | Done | Yes | Rate limit incomplete, ClamAV not installed |

## 3. Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | REQUIREMENTS.md | Code Evidence | Deployed Test | Final Status |
|--------|-----------------|---------------|---------------|--------------|
| AUTH-01 | [x] Done | `require2FA()` in session.ts, 2FA plugin in auth.ts | API returns 403 without 2FA | **satisfied** |
| AUTH-03 | [x] Done | LoginClient.tsx, login/page.tsx | /login renders AR/EN | **satisfied** |
| INTAKE-02 | [x] Done | persist.ts validateSubmission() | All 8 codes present | **satisfied** |
| INTAKE-04 | [x] Done | rate-limit.ts, submit/route.ts | Only submit limited | **partial** |
| INTAKE-05 | [x] Done | clamav.ts, upload/route.ts | Fallback on staging | **satisfied** (infra gap) |
| VERIFY-01 | [x] Done | ReviewerClient.tsx triage fields | UI shows checkboxes | **satisfied** |
| VERIFY-02 | [x] Done | sources schema + snapshotUrl | Field in schema & UI | **satisfied** |
| VERIFY-03 | [x] Done | review/route.ts dual review flow | first -> verified -> second -> ready | **satisfied** |
| VERIFY-04 | [x] Done | phrasingApproved, privacyRechecked | Enforced in second_approve | **satisfied** |
| LEGAL-01 | [x] Done | isDeceased from submission | validatePublication uses it | **satisfied** |
| LEGAL-02 | [x] Done | key-decisions table + API | POST/GET works | **partial** (no auth on POST) |
| RECORD-01 | [x] Done | RecordClient.tsx filter UI | Status/type/evidence select boxes | **satisfied** |
| RECORD-03 | [x] Done | published_entities view SQL | Created on staging | **partial** (API doesn't use it) |
| REPLY-02 | [x] Done | reply/admin/route.ts | approve/reject/correct/unpublish + audit | **partial** (matches drafts) |
| TRANS-01 | [x] Done | dashboard/page.tsx + /api/dashboard | Returns counts | **satisfied** |
| SITE-02 | [x] Done | policy/page.tsx | /policy renders AR/EN | **satisfied** |
| DATA-05 | [x] Done | seed script + dynamic homepage | Homepage queries DB | **satisfied** |

### Orphaned Requirements

None. All 17 requirements assigned to v1.1 appear in code.

## 4. Integration Check — Cross-Phase Wiring

### Working Integrations

| From | To | Wiring | Status |
|------|----|--------|--------|
| Auth (Phase 10) | Reviewer API (Phase 11) | `require2FA()` + `hasRole("reviewer")` on all review endpoints | OK |
| Reviewer Console (Phase 11) | Publish (Phase 5) | `validatePublication()` called before entity insert | OK |
| Submit (Phase 3) | Intake Validation (Phase 15) | `validateSubmission()` with 8 rejection codes | OK |
| Upload (Phase 3) | ClamAV (Phase 15) | `scanBuffer()` before `writeFile()` | OK (fallback) |
| Reply Intake (Phase 7) | Reply Admin (Phase 13) | `replies` table status flow: pending -> approved/rejected | OK |
| Dashboard (Phase 14) | DB (all) | Aggregates entities, submissions, replies | OK |

### Broken / Partial Integrations

| From | To | Issue | Severity |
|------|----|-------|----------|
| Auth (Phase 10) | Reviewer Page (Phase 11) | Page renders without 2FA check; API 403s after render | Medium |
| Auth (Phase 10) | Admin Page (Phase 13) | Same as above | Medium |
| Submit (Phase 3) | Anonymous User | API requires auth — contradicts `isAnonymous` feature | Medium |
| Public Record (Phase 6) | Materialized View (Phase 12) | List query uses view logic inline; single-entity query bypasses it | Critical |
| Key Decisions (Phase 15) | Auth | POST endpoint completely open | Critical |
| Rate Limit (Phase 15) | Internal APIs | Only submit endpoint protected | Critical |

## 5. End-to-End Flow Verification

| Flow | Steps | Status | Blocker |
|------|-------|--------|---------|
| Submit -> Review -> Publish | Submit form -> validate -> reviewer 1 approves -> reviewer 2 approves -> senior publishes -> appears on homepage | **Partial** | Anonymous submit broken; need seeded users to test full flow |
| Reply -> Admin -> Entity Update | File reply -> admin approves -> entity rightOfReplyState updated | **Working** | Tested via code review |
| Upload -> Scan -> Store | Select file -> ClamAV scan -> EXIF strip -> store -> return URL | **Partial** | ClamAV not installed on staging; fallback works |
| Login -> 2FA -> Reviewer | Email/password -> TOTP -> access reviewer console | **Partial** | No 2FA setup UI; must use API directly |
| Public Browse -> Filter -> Detail | Load record -> apply filters -> click entity -> view detail | **Partial** | Single-entity detail may leak drafts (CRIT-01) |

## 6. Nyquist Compliance

Not applicable — this project does not use GSD phase VALIDATION.md files.

## 7. Tech Debt Summary

| Area | Count | Items |
|------|-------|-------|
| Auth / 2FA | 2 | No setup UI; page-level 2FA missing |
| Search | 2 | No Arabic relevance engine; ILIKE only |
| Infra | 2 | ClamAV not installed; rate limit incomplete |
| Documentation | 3 | STATE.md stale; ROADMAP.md stale; no phase docs |

---

## Decision Required

**Status: `gaps_found`** — 3 critical gaps block production readiness.

**Options:**

1. **Close gaps inline** (recommended — ~30 min work):
   - Fix CRIT-01: Add `publishedAt` filter to entity detail lookup
   - Fix CRIT-02: Add rate limiting to all internal API routes
   - Fix CRIT-03: Add auth guards to key-decisions POST

2. **Accept tech debt** and complete milestone:
   - Track critical gaps as P0 issues for v1.1.1 hotfix
   - Complete milestone archive

3. **Plan cleanup phase**:
   - Insert Phase 16 "Security & Integration Closure"
   - Address all gaps + tech debt before tagging v1.1
