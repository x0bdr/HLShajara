---
milestone: v1.4 Report Submission Wizard
audited: 2026-06-14T00:00:00Z
auditor: Claude (gsd-verifier, goal-backward milestone audit)
head_commit: 529e7d2
status: passed
coverage: 24/24 v1.4 requirements delivered by committed code
guardrail_verdict: S1–S8 structurally intact (verified absent by grep + read)
build: next build exit 0 (compiled successfully; /en|/ar/submit SSG)
probes_run: 8/8 standalone gate scripts PASS (exit 0)
gaps: []   # no milestone-blocking gaps
recorded_deferrals:   # deferred-by-design, documented in STATE.md "v1.4 Deferred Follow-ups" — NOT gaps
  - "Human EN/AR staging E2E sign-off (operator-gated; blocks formal milestone CLOSE, not code-completeness)"
  - "Apply migrations 0000 + 0001 to prod (additive-only; back up first; owner data-dept/operator)"
  - "Install ffmpeg on hosts enabling video (BE-05 fails closed FFMPEG_UNAVAILABLE until then; video UI-hidden)"
  - "Interim→first-class swap-off: wire wizard to send conductType/roleInConduct so columns populate + triage auto-fires; columns currently dead but publish path strips interim tokens"
  - "Charter/copy CRITICAL-0 (sect-targeting language) — explicitly OUT OF v1.4 SCOPE per operator decision 2026-06-14"
minor_notes:   # low-severity, non-blocking observations
  - "ReviewStep submit gate counts sourceLinks.length (rows); registry requiresEvidence counts non-empty URLs. EvidenceStep requires-gate + server WEAK_SOURCE (links count) close the loop, so review can only be reached with ≥2 real links. Low."
  - "WizardClient.tsx:348 comment says route 'accept-but-ignores' leadNote — STALE. The committed route (api/submit/route.ts:73) PERSISTS leadNote. Code correct; comment outdated."
---

# HLShajara v1.4 — Report Submission Wizard: Goal-Backward Milestone Audit

**Milestone goal:** a one-step-per-page, card-driven, bilingual (EN/AR + RTL) accountability intake where every entry is a **named actor + documented conduct + ≥2 credible public source links**, preserving guardrails **S1–S8**.

**Verdict: PASSED.** All 24 v1.4 requirements map to real, committed implementation at `HEAD = 529e7d2`. The full 10-step wizard exists, is wired, builds clean, and passes all 8 in-repo gate probes. Guardrails S1–S8 are structurally intact. The five named deferrals are genuinely deferred-by-design and recorded in STATE.md — not accidental omissions.

---

## 1. Requirement Coverage (ID → delivering file:line → verdict)

| Req | Requirement | Delivering code (file:line) | Verdict |
|-----|-------------|------------------------------|:------:|
| **WIZ-01** | One step per page in `.page-container-narrow` | `submit/WizardClient.tsx:519-603` (single-step render dispatch); `registry.ts:83-178` (ordered STEPS) | ✓ |
| **WIZ-02** | Tap card → confirm → auto-advance, no Next on choice | `WizardClient.tsx:247-255` (`completeAndAdvance`, `ADVANCE_DELAY_MS=200`, reduced-motion immediate); `ChoiceStep.tsx` | ✓ |
| **WIZ-03** | Back from every step except first; browser Back/Fwd | `WizardClient.tsx:233-238` (`goBack`/`prevStep`), `144-159` (`goTo` object-href), `166-189` (`?step=` sync) | ✓ |
| **WIZ-04** | Progress indicator; completed pills tappable | `WizardProgress.tsx:36-52` (done/current/upcoming, `aria-current`, Arabic-Indic counter via `numberingSystem:"arab"`); `onJump`→`goTo` | ✓ |
| **WIZ-05** | Draft survives refresh; `?step=` URL; unsaved guard | `WizardClient.tsx:191-218` (restore prompt, persist-on-change, `beforeunload`); `persistence.ts` (`hls.submit.draft`); `state.ts:234-261` (RESTORE_DRAFT) | ✓ |
| **WIZ-06** | Individual skips subtype; deep-link → first incomplete | `registry.ts:99` (`branchWhen` individual), `244-266` (`isReachable`/`firstIncompleteStep`); `WizardClient.tsx:178-181` redirect | ✓ |
| **EV-05** | Client validation mirrors server screens in order; street block | `lib/screens.ts:159-213` (8-screen cascade, exact server order); `isCoarseLocationClean:124-127`; probe `screens-parity-check.js` PASS | ✓ |
| **STEP-01** | Actor class + entity subtype → one of 5 enums | `WizardClient.tsx:438-475` (actor-class/entity-subtype options); `encoding.ts` + enum literals in `validation.ts:55-61` | ✓ |
| **STEP-02** | Identity: name/role/country, coarse loc, optional ref, MISMATCH | `IdentityStep.tsx:115-217` (5 fields, S5 block `85-94`, MISMATCH `113/169`); `step-logic.ts:92-106` | ✓ |
| **STEP-03** | Conduct + role-in-act from closed card sets | `encoding.ts:42-79` (14 conduct + 7 role slugs, closed); `WizardClient.tsx:476-493` | ✓ |
| **STEP-04** | Describe ≥20 chars, coarse time, inline screens | `DescribeStep.tsx`; `step-logic.ts:116-161` (server-ordered screens), `204-206` (`requiresDescribe ≥20`) | ✓ |
| **STEP-05** | Submitter info, anon ON default, clears name/email | `AboutYouStep.tsx:40-44` (toggle clears name+email), `57/71/84` (checked+disabled); seed `state.ts:66` | ✓ |
| **EV-01** | ≥2 sources (links) before advancing; rule up front | `EvidenceStep.tsx:99-103` (rule up front), `161-164` (live counter); `step-logic.ts:171-213` (`evidenceSourceCount`/`requiresEvidence ≥2`) | ✓ |
| **EV-02** | Optional non-public lead note, never a source | `EvidenceStep.tsx:168-180` (writes `form.leadNote` only); `step-logic.ts:169` (never counted); `persist.ts:52-54` invariant | ✓ |
| **EV-03** | Optional supporting media, supporting-only/non-public, safety copy | `MediaStep.tsx:36-37` (images/docs only, no video), `84-95` (safety copy, accept whitelist), `124-137` (link screened) | ✓ |
| **EV-04** | NO field for phone/plate/address/pin/social/sect/loyalty/profession | Guardrail grep over `src/components/wizard/` + `src/lib/wizard/` = CLEAN (only a `<section>` false-positive + a screen warning-string) | ✓ |
| **REV-01** | Full summary, optionals "—", Edit link per group | `ReviewStep.tsx` (six review-groups, `displayValue` → "—", per-group `onEdit→goTo`) | ✓ |
| **REV-02** | Submit disabled until affirmation + ≥2 sources | `ReviewStep.tsx:77-78` (`submitDisabled = submitting \|\| !affirmed \|\| links<2`), `293-315` (dual inline gates) | ✓ |
| **REV-03** | POST `/api/submit`, confirmation + ref id, clears draft | `WizardClient.tsx:359-402` (POST), `383-390` (clearDraft+GTM on ok), `499-517` (success panel + `submissionId`) | ✓ |
| **REV-04** | Server rejection → bilingual message → offending step | `rejection-map.ts:61-84` (closed 8-code map → stepId, null-safe); `WizardClient.tsx:393-401` (`resolveRejection`→`goTo`) | ✓ |
| **INTL-01** | EN/AR `submit` parity; `check:i18n` passes | 163 EN = 163 AR submit keys, 0 missing, no token leak; probes `i18n-parity-check.js` + `i18n-submit-parity-check.js` PASS | ✓ |
| **INTL-02** | RTL correct: mirrored, LTR machine strings, Arabic-Indic counter | probe `wizard-rtl-check.js` PASS (`.ds-mono`/`.review-sources` LTR, Intl numberingSystem, no raw index in JSX) | ✓ |
| **INTL-03** | Keyboard-operable, focus-to-heading, aria-live, reduced motion, AA | probe `wizard-a11y-check.js` PASS (radiogroup, focus-to-h2, single aria-live, aria-current, labelled inputs, error association) | ✓ |
| **BE-01** | `conductType` enum + populate `triageCategory` on intake | `schema.ts:112/225` (pgEnum + column); `constants/conduct.ts:77-110` (triage map); `api/submit/route.ts:64-69` (`triageFromConduct`) | ✓ |
| **BE-02** | Non-public `leadNote`, never public/source | `schema.ts:231` (column); `api/submit/route.ts:73` (persist); never selected by `dashboard`/`review`/`entity` GET; never in publish path | ✓ |
| **BE-03** | Per-source `sourceType` (6-set) | `validation.ts:84` (`z.enum([un,court,sanctions,hr,journalism,official])` per source); rides JSONB through `route.ts:74-75` | ✓ |
| **BE-04** | `isAnonymous` default → true | `schema.ts:238` (`.default(true)`); migration `0001:3` (`SET DEFAULT true`, new-rows-only); `validation.ts:101` | ✓ |
| **BE-05** | Strip video metadata before enabling video | `media-metadata.ts:58-84` (`ffmpeg -map_metadata -1 -c copy`, sanitized paths); `api/upload/route.ts:69-99` (fail-closed 503 when ffmpeg absent) | ✓ |
| **BE-06** | `roleInConduct` first-class field | `schema.ts:114/227` (pgEnum + column); `validation.ts:71`; `api/submit/route.ts:65` (persist); migration `0001:2,5` | ✓ |

**Coverage: 24/24 delivered. No requirement ID lacks delivering code. No orphans.**

---

## 2. Milestone Goal — Achieved

- **Full 10-step flow exists and is wired.** `registry.ts` STEPS = `actor-class → entity-subtype → identity → conduct → role-in-act → describe → evidence → media → about-you → review` (10 steps); WizardClient render dispatch (`:569-602`) routes every step to its real component, plus the success-confirmation panel. `submit/page.tsx` renders `<WizardClient/>` inside Suspense. ✓
- **Single source of truth / no drift.** `WizardState.form IS SubmitInput` (state.ts:18); the 14-conduct/7-role tuples live once in `constants/conduct.ts` and are imported by BOTH Drizzle pgEnum (`schema.ts:112-114`) AND Zod (`validation.ts:15,69,71`). ✓
- **Source-first ≥2-links gate, client+server agree.** Client `evidenceSourceCount` (non-empty URLs) and server `validateSubmission` (`sourceCount = data.sourceLinks.length`) both count LINKS only; files strengthen but never unlock. `screens-parity-check.js` confirms identical order. ✓
- **Bilingual + RTL.** 163/163 EN/AR submit-key parity; full message tree 318/318; RTL + a11y probes pass. ✓
- **Production build clean.** `next build` exit 0, "Compiled successfully", `/en/submit` + `/ar/submit` prerendered SSG; `/api/{submit,upload,review}` built. ✓

---

## 3. Guardrail Verdict — S1–S8 Structurally Intact

| Guardrail | Verification | Verdict |
|-----------|--------------|:------:|
| **S1–S4** No phone/plate/street/map-pin/social-handle/sect/loyalty/profession field | Grep over entire wizard surface returns zero real field matches; IdentityStep collects only name/role/country/coarse-city/official-ref; no banned input anywhere | ✓ absent by design |
| **S5** Coarse location only | `IdentityStep.tsx:85-111` — street-level token fails `isCoarseLocationClean` and is NEVER composed into `allegationLocation`; inline error. `screens-parity-check.js` confirms street blocked / governorate allowed | ✓ |
| **S6** Source-first, ≥2 LINKS, lead note never a source | Client + server both gate on `sourceLinks` count (links-only); `leadNote` written to its own field, never counted (`persist.ts:52-54`, `step-logic.ts:169`) | ✓ |
| **S7** Anonymity default ON | Seed `isAnonymous:true` (`state.ts:66`), Zod default true (`validation.ts:101`), DB default true (`schema.ts:238` + migration); toggle clears name+email (`AboutYouStep.tsx:40-44`) | ✓ |
| **S8** Nothing published on submit | `/api/submit` inserts `status:"pending"` only (`route.ts:55`); publication is a separate reviewer action in `/api/review` gated by `pubCheck` | ✓ |
| **leadNote never on a public path** | `grep` shows leadNote only in: validation, state, EvidenceStep (write), submitter's own ReviewStep, submit route (persist), schema. NOT in `dashboard`/`review`/`entity` GET, NOT in publish→entities/allegations/sources | ✓ |
| **Interim tokens stripped at publish** | `/api/review/route.ts:214` `stripRoleClause(entityRole)`, `:248` `stripSourceType(title)` before creating the public entity/source | ✓ |

---

## 4. Backend (Phase 33) — Additive + Safe

- **Migration `0001` additive-only.** Statements: 2× `CREATE TYPE` (new enums), 1× `ALTER COLUMN is_anonymous SET DEFAULT true`, 3× `ADD COLUMN` (conduct_type, role_in_conduct, lead_note). No `DROP`, no destructive `ALTER COLUMN ... TYPE`, no new `NOT NULL` on existing data, no backfill. ✓
- **Shared enum const = single source of truth.** `constants/conduct.ts` `conductTypes`/`roleInConductTypes` imported by both `schema.ts` (pgEnum) and `validation.ts` (z.enum). New columns are nullable; `triageFromConduct` is typed `Record<ConductType, …>` so a missing slug fails to compile. ✓
- **`isAnonymous` flip is new-rows-only.** `SET DEFAULT` affects future inserts; existing rows keep recorded value (audit trail not rewritten) — documented in `schema.ts:236-238`. ✓

---

## 5. Probes / Gates Run (in-process, not SUMMARY claims)

| Probe | Result |
|-------|:------:|
| `next build` | exit 0, compiled successfully, submit SSG |
| `i18n-parity-check.js` | PASS (318/318 full tree) |
| `i18n-submit-parity-check.js` | PASS (163/163 submit) |
| `screens-parity-check.js` | PASS (server order + coarse-location) |
| `step-logic-check.js` | PASS |
| `wizard-reducer-check.js` | PASS |
| `wizard-choice-steps-check.js` | PASS |
| `wizard-rtl-check.js` | PASS |
| `wizard-a11y-check.js` | PASS |

No debt markers (TBD/FIXME/XXX) in any v1.4 file.

---

## 6. Deferrals vs. Gaps

All five named items are **recorded deferrals**, confirmed deferred-by-design in `.planning/STATE.md` "v1.4 Deferred Follow-ups (recorded at milestone code-complete, 2026-06-14)":

1. **Interim→first-class swap-off** — conductType/roleInConduct columns are presently dead (wizard still sends interim `allegationClassification` slug + `entityRole` clause + `[TYPE:]` title); triage does not auto-fire from a populated `conductType`. The publish path defensively strips all interim tokens so the public record is clean. **Deferred by design** (the wizard intentionally targets the existing `/api/submit` contract via §8 interim mappings so it shipped before BE-33). Not a gap.
2. **Video gated behind ffmpeg** — `/api/upload` fails closed `FFMPEG_UNAVAILABLE` (503); video UI-hidden in MediaStep. BE-05 strip helper exists and is correct. Not a gap.
3. **Migrations not applied to prod** — additive `0000`+`0001`; operator/data-dept task with backup-first. Not a gap.
4. **Pending human EN/AR staging E2E** — operator-gated sign-off; blocks formal milestone CLOSE, not code-completeness. The roadmap already marks v1.4 "code-complete; pending staging EN/AR E2E sign-off." Not a gap.
5. **CRITICAL-0 charter/copy (sect-targeting language)** — explicitly OUT OF v1.4 SCOPE per operator decision 2026-06-14 (REQUIREMENTS.md "Out of Scope"). Not a v1.4 gap.

**No genuine milestone gaps found.**

---

_Audited: 2026-06-14 · Auditor: Claude (gsd-verifier) · HEAD 529e7d2_
